# PRD: ADetect (Automated Ad & Brand Intelligence Engine)

---

## 1. 프로젝트 개요

* **프로젝트 이름**: ADetect
* **한 줄 설명**: 브랜드 정보 입력 시 시장·타겟·자사·경쟁사 데이터 수집부터 멀티모달 분석, 한 줄 인사이트 도출, 소재 캡처 패키징(ZIP)까지 자동화하는 사내 마케팅 리서치 및 경쟁사 모니터링 웹 서비스
* **유형**: Streamlit 기반 사내 공유 웹 서비스 (Python Backend + Streamlit Frontend)
* **개발 난이도**: 중상 (Apify Scraper, Playwright, Gemini Multimodal API 멀티 파이프라인 연동)
* **적용 범위** (★ 15차 개정 신규): 커머스/리테일(예: 올리브영), 금융·보험 등 서비스 브랜드(예: 메리츠화재), 멀티카테고리 단일 브랜드(예: 나이키)처럼 성격이 다른 브랜드도 **동일한 입력 폼과 동일한 분석 구조**로 처리합니다. 브랜드 유형은 사용자가 선택하지 않고 AI가 내부적으로 판단합니다(§7-3-a `brand_context`).

---

## 2. 사용자 시나리오

* **주요 사용자**: 광고대행사 퍼포먼스 마케터, AE, 마케팅 전략 기획자
* **사용 시점**:
1. 신규 경쟁사 분석 및 브랜드 핏 검토(Audit) 시
2. 광고주 제안서 작성용 시장/경쟁사 팩트 파인딩 시
3. 월간 경쟁사 광고 소재 및 프로모션 동향 모니터링 시


* **도입 목적**:
* 브랜드 검색 캡처, 메타/구글 라이브러리 소재 다운로드, 뉴스 스크랩 등 단순 반복 수작업(건당 3~4시간 소요)을 대폭 단축 — **목표: 15~20분 내외** (§6 목표 실행 시간 재산정 참고. 최초 구상이었던 "5분 이내"는 Apify actor 실행 자체가 최대 15분까지 걸릴 수 있어 비현실적이라 조정)
* 파편화된 수집 데이터를 구조화된 인사이트 문서(HTML/Excel)와 자산 패키지(ZIP) 형태로 즉시 공유 및 표준화



---

## 3. 개발 환경 폴더 계층 구조 (Directory Structure)

> ★ 이 프로젝트가 참고한 `Claudecode_MarketingOS_student` 워크스페이스의 문서화 관례(루트 README + SETUP_GUIDE + 폴더별 짧은 README)를 그대로 계승합니다. 상세 규칙은 §23 참고.

```text
ADetect/
├── README.md                          — 5분 퀵스타트 (설치→실행)
├── SETUP_GUIDE.md                     — API 키 발급·환경변수 단계별 가이드 (single source of truth)
├── docs/
│   └── REFERENCE.md                   — 레거시 워크스페이스 레퍼런스 매핑표 (§4 그대로 이식, 코드 변경 시 갱신)
├── Dockerfile / docker-compose.yml    — 사내 서버 배포용 컨테이너 설정 (Phase 5, 로컬 MVP 검증 후 착수)
├── requirements.txt                   — Python 의존성 패키지 목록
├── .env.example                       — API Key 및 환경변수 템플릿
│
├── app.py                             — Streamlit 메인 실행 엔트리포인트 (웹 UI)
│
├── config/                            — 시스템 설정 및 프롬프트
│   ├── README.md                      — 이 폴더의 역할 1페이지 요약
│   ├── settings.py                    — 환경 설정 및 API 타임아웃/경로 정의 (.env 로딩)
│   └── prompts.py                     — Gemini 분석용 마스터 프롬프트 모듈
│
├── core/                              — 핵심 수집·분석·생성 엔진 파이프라인
│   ├── scrapers/                      — 데이터 스크래핑 모듈
│   │   ├── README.md
│   │   ├── naver_api.py               — 네이버 DataLab(검색량 추이/성별·연령 필터) / 뉴스 검색 API / 검색광고 키워드도구(절대 검색량, §21-10)
│   │   ├── naver_serp.py              — Playwright 기반 브랜드검색+SA(파워링크) 캡처 (★ Residential Proxy 미사용, §21-1 참고)
│   │   ├── brand_site.py              — 브랜드 홈페이지/대표 상세페이지 크롤링 (신규, §7-3 `brand_website_facts`)
│   │   └── ad_library.py              — Apify 기반, **파일 하나에 두 개의 독립된 수집 함수를 담음**(★ 10차 개정 — §7-11-0 "Meta/Instagram 용어 정리" 참고): ① `fetch_meta_ads()` — Meta Ads Library 활성 광고 소재(페이스북+인스타그램 노출 광고 전부 포함, 별도의 "Instagram 광고 API"는 존재하지 않음) ② `fetch_instagram_profile()` — Instagram 프로필/게시물(팔로워·포스팅 수·캡션, 광고 아닌 SNS 운영현황). 서로 다른 Apify 액터를 쓰는 별개 함수이며 실패도 서로 독립(§11)
│
│   [MVP 파일 목록에 없음 — Phase 0 Go 판정 이후에만 신규 생성, §21-6·§22] google_ads.py — Google Ads Transparency Center. **Phase 0 이전에는 이 파일을 만들지 않음**(빈 파일 생성도 하지 않음) — 트리에 자리만 남겨두면 "이미 구현됨"으로 오독될 수 있어 아예 항목에서 제외
│   │
│   ├── analyzers/                     — AI 분석 및 합성 모듈
│   │   ├── README.md
│   │   ├── gemini_vision.py           — 이미지/영상 키프레임 OCR 및 USP 파싱
│   │   └── insight_synthesizer.py     — 4대 영역 종합 인사이트 및 총평 합성 (§8 FACT/AI 구분 스키마 적용)
│   │
│   └── exporters/                     — 최종 산출물 생성 모듈
│       ├── README.md
│       ├── html_builder.py            — Jinja2 기반 대화형 HTML 리포트 빌더
│       ├── excel_builder.py           — 정량 데이터 Excel(.xlsx) 파일 생성기 (§14 시트 구조)
│       └── packager.py                — 자산 및 리포트 자동 .zip 압축 모듈
│
├── database/                          — 이력 관리 및 DB
│   ├── README.md
│   ├── db.py                          — SQLite DB 연결 및 CRUD 로직 (§18 멀티유저 한계 참고)
│   └── history_manager.py             — 최근 N개 분석 이력 및 자산 복원 관리자
│
├── templates/                         — 대화형 HTML 리포트용 Jinja2 템플릿 & 에셋
│   ├── report_template.html           — HTML 리포트 메인 레이아웃 템플릿
│   └── assets/                        — 리포트용 CSS/JS 및 아이콘 자산
│
└── storage/                           — 실행 결과물 및 임시 파일 저장소 (Git Ignore)
    ├── temp/                          — 스크래핑 진행 중 임시 캐시 및 키프레임
    └── exports/                       — 생성된 HTML, Excel, ZIP 파일 저장소 (최근 N개)

```

---

## 4. 참고 소스 코드 (기존 워크스페이스 레퍼런스)

★ 이 프로젝트를 **다른 환경(별도 저장소·다른 개발자)에서 진행할 경우, 아래 파일들의 실제 사본을 함께 전달**합니다. `Claudecode_MarketingOS_student` 워크스페이스 경로만 적어두면 그 워크스페이스가 없는 환경에서는 가리키는 대상이 없으므로, **`adetect_reference/` 폴더(이 PRD와 같은 위치)에 실제 파일 사본을 담아뒀습니다** — PRD와 이 폴더를 함께 넘기면 됩니다. 상세 설명은 `adetect_reference/README.md` 참고.

| ADetect 모듈 | 번들 내 경로 (`adetect_reference/...`) | 원본 출처 (이 워크스페이스 기준) | 재사용 포인트 |
|---|---|---|---|
| `core/scrapers/ad_library.py` | `scrapers/fetch_competitor_ads.py`<br>`scrapers/fetch_ads.py` | `02_competitor/_scripts/fetch_competitor_ads.py`<br>`ad_monitor/_scripts/fetch_ads.py` | Apify actor 호출·polling(`run_actor`/`wait_run`/`get_items`)·HTTP 재시도, 광고 포맷 판별 및 이미지/영상 다운로드·분류 로직. **두 파일이 사실상 동일한 코드의 복붙 쌍**이므로 이식 시 하나로 통합. ★ 동일 패턴을 **Instagram 프로필/게시물 수집**(Apify Instagram Scraper 계열 액터)에도 그대로 재사용 — `actor_id`만 교체하면 되므로 별도 스텔스/프록시 개발 불필요 (§21-7) |
| `core/analyzers/gemini_vision.py` | 위와 동일 파일 내 `gemini_analyze_image` / `gemini_analyze_video` / `extract_keyframes` / `GeminiQuotaExceeded` | 〃 | 이미지·영상 분석 프롬프트(USP 4항목 JSON 스키마), ffmpeg 키프레임 6장(8/22/40/58/75/92%) 추출, 무료 티어 quota 초과 시 우아한 실패 처리(`_is_quota_error`) |
| `core/analyzers/insight_synthesizer.py` | `scrapers/fetch_competitor_ads.py`의 `build_trend_report` / `_synthesize_whitespace` / `_message_angle_matrix` | `02_competitor/_scripts/fetch_competitor_ads.py` | 메시지 각도 매트릭스, ad_pattern 분포, "빈틈(White Space)" 합성 — §7 Output Schema·§8 FACT/AI 구분 설계의 출발점 |
| UTM/트래킹 파라미터 분석 (§10 자사 분석 항목) | `scrapers/utm_pattern.py` | `ad_monitor/_scripts/utm_pattern.py` | 랜딩 URL UTM 관찰 + 캠페인/그룹/소재명 규칙 베스트에포트 추정 — PRD의 "UTM 구조 분석" 요구사항과 사실상 동일 기능 |
| `core/exporters/html_builder.py` | `exporters/period_report.py`, `templates_sample/period_v1/*.html.j2`, `exporters/build_dashboard_competitor.py` | `_shared/scripts/period_report.py`, `_shared/templates/period_v1/`, `02_competitor/_scripts/build_dashboard.py` | Jinja2 `Environment`/`FileSystemLoader` 세팅, Playwright headless Chromium으로 HTML→PDF 변환. 오프라인 독립형 HTML(인라인 CSS/JS+데이터, §21-4)은 `build_dashboard_competitor.py`의 `window.DATA` 인라인 주입 패턴 참고 |
| 입력/시드 관리 | `scrapers/competitor_inputs.py`<br>`scrapers/ad_inputs.py` | `02_competitor/_scripts/competitor_inputs.py`<br>`ad_monitor/_scripts/ad_inputs.py` | slugify, 입력 파일(`_inputs/*.md`) 파싱, URL 최초 입력 시 자동 시드 생성 패턴 |
| `core/scrapers/naver_serp.py` 설계 참고 | `crawlers/oliveyoung_crawler.py` | `03_customer/_scripts/oliveyoung_crawler.py` | "Playwright 브라우저 컨텍스트 안에서 내부 API/페이지를 직접 조작"하는 패턴, cursor 페이징, 트랙별 재시도·백오프(`_fetch_track`) 구조. **★ 이름에 속지 말 것**: 이 파일은 올리브영 쇼핑몰의 특정 상품 리뷰를 긁는 도구이지 "올리브영을 브랜드로 분석"하는 도구가 아닙니다 — 패턴만 참고 |
| `config/settings.py` (.env 로딩) | `lib/env_loader.py` | `_shared/scripts/lib/env_loader.py` | 외부 의존성(python-dotenv) 없이 stdlib만으로 `.env` 파싱하는 관례 — 워크스페이스 전체에서 3벌 중복 존재, 이식 시 1벌로 통합 |
| `config/prompts.py` 설계 근거 | `reference_docs/reference_analysis_method.md`<br>`reference_docs/analysis_method.md` | `02_competitor/_reference/reference_analysis_method.md`<br>`ad_monitor/_reference/analysis_method.md` | USP 3항목(User's Problem·Solution·Promotion) + Creative Key Visual + ad_pattern 분석 방법론 원문 |
| `requirements.txt` 베이스라인 | `lib/requirements_baseline.txt` | `_shared/requirements.txt` | Jinja2·playwright·requests 등 이미 검증된 버전 핀 |
| **최종 UI 디자인 레퍼런스** (★ 필수 참고) | `templates_sample/ui_ref.png` | (사용자 제공) | 입력/진행/결과·이력 4개 화면의 최종 목업 이미지. **★ 12차 개정 — 이 4개 화면은 이제 "STEP 1 브랜드 설정 + STEP 2 분석 Workspace(5개 탭, 그 안에서 진행·결과·다운로드가 전부 일어남)" 2단계로 재해석해 구현**(§16 참고, 톤앤매너·4분할 배치 자체는 그대로 1차 기준). 다크+골드 톤, FACT/AI INTERPRETATION/RECOMMENDATION 3단 구분 표시가 실제로 어떻게 배치되는지 그대로 보여줌 |

> ⚠️ `adetect_reference/`는 **스냅샷 사본**입니다. 원본이 이후 수정돼도 자동 반영되지 않으니, 원본이 크게 바뀌면 번들을 다시 복사해 갱신하세요. 시크릿(API 키) 포함 여부는 복사 시점에 스캔해 확인했습니다 — 플레이스홀더 문자열(`apify_api_...`)만 있고 실제 키는 없습니다.

---

## 5. 핵심 기능 목록

### 필수 기능 (MVP)

> ★ **(9차 개정) 모듈형 기능 구조로 전환, (12차 개정) UI를 "분석 Workspace + 5개 탭"으로 구체화** — 기존엔 브랜드명 입력 후 "분석 시작" 1회 클릭으로 4대 영역 전체가 한 파이프라인으로 도는 구조였으나, 사용성·비용·재실행 효율을 이유로 **5개의 독립 실행 가능한 기능**으로 재설계했고, 이 기능들은 화면상 **분석 Workspace의 지속되는 5개 탭**으로 표현됩니다(버튼을 누르면 화면이 전환되는 방식이 아님). 상세 설계는 §6-1(실행 단위)·§7-11(기능별 산출물 명세)·§16(Workspace UI) 참고.

* **입력 매개변수 처리** (★ 15차 개정 — 타겟 입력 제거): 브랜드명(필수), 카테고리(선택), 경쟁사(선택, 개수 고정 없음) — 미입력 항목(카테고리·경쟁사)은 §9 자동 추천 플로우로 보완. **타겟은 STEP 1에서 아예 입력받지 않습니다** — 시장분석+브랜드분석 결과가 쌓인 뒤 타겟분석 탭에서 AI가 근거와 함께 추천하고, 사용자가 확정합니다(§7-2·§9-2). 이 입력값(브랜드명/카테고리/경쟁사)은 STEP 1(브랜드 설정, §16-1)에서 1회만 받고, STEP 2 Workspace의 5개 기능 탭에 공통으로 사용됨.
* **5개 기능** (분석 Workspace의 5개 탭으로 표현, §6-1·§7-11·§16-2):
  1. **시장분석하기** — 검색량 추이/계절성, 시장 뉴스, 참고자료 출처
  2. **타겟분석하기** (★ 15차 개정 — 게이트형 + 추천/분석 2단계) — 시장분석+브랜드분석이 둘 다 최소 부분 실패 이상이어야 잠금 해제(§6-1)되며, 잠금 해제 즉시 그 결과를 근거로 **핵심 타겟을 먼저 추천**합니다(`recommended_target`). 사용자가 이 추천을 확인·확정(`confirmed_target`)하면, 그 값을 기준으로 세그먼트 검색량·관심 키워드·타겟 성향/선호상황/전환 트리거(§7-2-a)를 실제로 분석합니다 — "타겟 추천"과 "타겟분석"은 서로 다른 단계입니다(§7-2·§9-2)
  3. **브랜드분석하기** (자사+경쟁사 통합, §7-3·§9-15 반영) — 브랜드 검색량, 홈페이지 팩트, 브랜드 뉴스, SNS 프로필, 네이버 SA/브랜드검색 운영현황(대표키워드+일반키워드), 매체 운영 매트릭스
  4. **소재분석하기** (DA Creative, 자사+경쟁사 통합) — Meta Ads Library 활성 광고 소재(페이스북+인스타그램 노출 포함, §7-11-0) 수집·다운로드, 소구포인트 태깅, 운영기간 분석
  5. **종합분석하기** — 위 4개 기능의 결과를 조합해 SOV·포지셔닝맵·White Space·한 줄 총평 생성 (4개 중 최소 1개 이상 완료 데이터가 있어야 활성화, §6-1)
* **Gemini 멀티모달 파싱**: 소재 이미지/영상 키프레임 OCR, USP 3단계 파싱, 소구포인트 다중 라벨 태깅 — 결과는 §8 FACT/AI INTERPRETATION 구분 스키마로 저장
* **다중 포맷 산출물 출력**: 기능별 결과를 담은 대화형 HTML 리포트, 정량 데이터 Excel 파일(§14 시트 구조, 실행한 기능의 시트만 채워짐), 소재분석 실행 시 이미지/영상 `.zip` 패키징
* **이력 관리 (History)**: 브랜드별 "분석 세션" 아래 기능별 실행 이력을 함께 보여주고, 각 기능 실행 결과를 원클릭 재다운로드(실제 파일이 LRU로 삭제됐으면 `파일 만료`, §21-3) — 상태값은 `완료`/`부분 실패`/`취소`/`전체 실패` 4종(§6), 기능 단위로 판정

### 선택 기능 (추후 확장)

* **비동기 작업 큐(Queue) 고도화**: MVP는 §18의 단순 동시 실행 제한(락)으로 충분하며, 다중 사용자 대규모 트래픽 대비 정교한 큐(Celery/RQ 등)는 추후 확장
* **시점별 비교 리포트**: 동일 브랜드의 주간/월간 분석 이력 간 변동 사항 추적 — §13에서 모든 데이터에 `collected_at`을 저장해두는 이유가 바로 이 확장을 위함
* **카테고리별/브랜드 유형별 분석 프리셋** (★ 신규, 보류): 커머스/금융 등 카테고리마다, 또는 제품·서비스·리테일·플랫폼 등 브랜드 유형마다 강조 필드·AI 프롬프트 톤·화면 구성을 다르게 주는 프리셋 시스템은 1차 MVP 범위에서 제외합니다. 이런 차이는 §7-3-a `brand_context`(내부 판단 필드)가 프롬프트 서술 수준에서만 흡수하고, 별도 화면·별도 Excel·별도 프롬프트 체계는 만들지 않습니다. 먼저 모든 브랜드에 공통으로 적용 가능한 §7 표준 스키마로 출시하고, 실사용 피드백을 본 뒤 프리셋 도입 여부를 재검토합니다(🟢 우선순위 낮음).
* **NotebookLM 연동** (★ 신규, 보류): `notebooklm-mcp-cli`(오픈소스, 비공식 브라우저 자동화 기반)를 통해 분석 결과를 NotebookLM에 연결하는 방안을 검토했으나, NotebookLM은 자체 수집 능력이 없어 ADetect가 이미 만든 결과물을 재서술하는 부가 기능 이상의 가치를 주지 못하고, 비공식 자동화의 안정성·보안 리스크가 그 이득보다 큽니다. 1차 MVP에서는 제외하고, 도입한다면 ADetect 결과를 Google Drive 문서로 발행해 자동 동기화되게 하는 방식을 우선 검토합니다(🟢 우선순위 낮음).

---

## 6. MVP 완료 기준 (Acceptance Criteria)

> ★ "동작은 하는데 완료인지 애매한" 상태를 없애기 위한 판정 기준입니다. 아래 표의 모든 행을 만족해야 MVP(Phase 4, §22)가 완료된 것으로 간주합니다.

### 6-1. 실행 단위: "분석 세션"과 "기능 실행" (★ 모듈형 기능 구조 반영, UI 표현은 §16 Workspace 탭 참고)

§5에서 단일 파이프라인이 5개 독립 기능(시장/타겟/브랜드/소재/종합)으로 나뉘면서, 아래 개념을 새로 정의합니다. ★ 12차 개정 — 이 5개 기능은 화면상 분석 Workspace의 **지속되는 5개 탭**으로 구현되며(§16-2), 탭을 "누른다"는 것은 곧 그 탭 안의 "OO분석 시작하기" CTA를 실행하는 것을 의미합니다.

* **분석 세션(`analysis_session`)**: 특정 브랜드(+카테고리/경쟁사 입력값 확정본, §9)를 기준으로 열리는 상위 컨테이너 — 화면상으로는 하나의 Workspace 인스턴스와 대응. **타겟은 세션 생성 시점의 확정값에 포함되지 않습니다** — 타겟분석 탭에서 게이트 통과 후 별도로 추천·확정되며(§7-2), 확정되면 그 값이 세션에 `confirmed_target`으로 갱신 저장됩니다. History에는 세션 단위로 브랜드명·생성일시가 표시되고(§16-6), 그 세션을 열면 5개 탭이 각자의 마지막 상태로 복원됩니다.
* **기능 실행(`function_run`)**: Workspace의 5개 탭 중 하나에서 "OO분석 시작하기"를 누른 단위. 각 실행은 자신만의 상태(`완료`/`부분 실패`/`취소`/`전체 실패`)·비용·소요시간·결과 파일(Excel 시트 일부 + 필요 시 zip)을 가집니다. 같은 세션 안에서 같은 기능을 재실행하면 §12 캐시 정책이 그대로 적용됩니다(24시간 이내 재수집 방지). ★ 탭 안의 2차 세부탭(§16-3, 예: 브랜드분석의 "홈페이지 분석"/"SNS 분석")은 결과를 나눠 보여주는 화면일 뿐 별도의 `function_run`이 아닙니다 — `function_run`은 항상 5개 1차 기능 단위로만 존재합니다.
* **"전체 실패 vs 부분 성공" 판정은 이제 기능 실행 단위로 적용**합니다. 예: "브랜드분석" 실행 중 네이버 캡처만 실패하면 그 기능 실행은 부분 실패, 나머지 4개 기능과는 무관합니다. 세션 전체의 종합 상태는 실행된 기능들의 상태를 단순 나열해 보여주고(§16), 억지로 하나의 값으로 합치지 않습니다.
* **"종합분석" 탭의 활성화 조건**: 같은 세션 안에서 시장/타겟/브랜드/소재 4개 기능 중 **최소 1개 이상이 `완료` 또는 `부분 실패`(데이터 일부라도 존재)** 상태로 실행된 적이 있어야 활성화됩니다. 4개 모두 `데이터 없음`이면 종합분석은 🔒 상태를 유지하고 그 이유를 안내합니다. 종합분석은 4개 기능의 **최신 캐시 결과**를 그대로 조합하며, 그 자체로 별도 스크래핑을 하지 않습니다(§7-11-5).
* **"타겟분석" 탭의 활성화 조건** (★ 13차 개정 — 완전 게이트형으로 전환, §7-2): 종합분석과 달리 **"4개 중 1개"가 아니라 "시장분석 AND 브랜드분석 둘 다"**가 조건입니다 — 시장분석과 브랜드분석이 **각각** 최소 `부분 실패`(데이터 일부라도 존재) 이상 상태가 되어야 타겟분석 탭이 잠금 해제됩니다. 둘 중 하나라도 `데이터 없음`/미실행이면 🔒 상태를 유지하고 "시장분석·브랜드분석을 먼저 실행하세요"를 안내합니다. 소재분석은 이 게이트 조건에 포함되지 않음(있으면 §7-2-a의 보조 근거로만 추가 사용).
* **잠금 해제 이후의 2단계 흐름**(★ 15차 개정, §7-2): 탭이 🔒 → ○로 전환되는 즉시 AI가 `recommended_target`을 자동 생성해 보여줍니다(추가 수집 없이 이미 캐시된 시장·브랜드 데이터만 사용하므로 별도 비용·실행 버튼이 필요 없음). 사용자가 이를 확인·확정(`confirmed_target`)해야만 "타겟분석 시작하기" 버튼이 열리고, 그 이후 실행되는 것이 §6-1의 실제 `function_run`(세그먼트 검색량·관심 키워드·AI Persona 수집)입니다. 즉 "추천"은 게이트 통과 시 자동으로 일어나는 무료 단계이고, "분석"은 사용자 확정 후 실행하는 유료·시간 소요 단계입니다 — 이 둘을 같은 것으로 취급하지 않습니다.
* 아래 Acceptance Criteria 표는 "세션 안에서 5개 기능을 모두 1회 이상 실행했다"는 가정 하의 완성 기준이며, 사용자가 일부 기능만 실행하고 멈추는 것은 정상 사용 패턴입니다(부분 실행 = 실패 아님). 단 타겟분석은 위 게이트 조건이 선행되어야 "실행"이 가능합니다.

| 영역 | 완료 기준 |
|---|---|
| 입력 | 브랜드명 입력 후 분석 실행 가능 |
| 자동추천 | 카테고리/경쟁사 미입력 시 AI 추천값을 **사용자가 확인·수정한 뒤에만** 분석 실행 (§9). **타겟은 이 STEP 1 자동추천 대상이 아님** — 시장분석+브랜드분석 완료 후 타겟분석 탭에서 별도로 추천되고, 사용자가 확정한 뒤에만 타겟분석이 실행됨 (§7-2·§9-2) |
| 수집 | 선택한 수집 항목별 성공/실패 상태를 개별 표시 |
| 네이버 | 검색 결과 캡처 실패 시 전체 분석 실패가 아닌 부분 실패로 처리 |
| 광고 | Meta Ads Library 소재 수집 결과(FB+IG 노출 포함, §7-11-0)에 이미지/영상 URL 및 `collected_at` 포함 (§13) |
| AI 분석 | 이미지/영상별 분석 결과가 JSON 구조(§8 source/evidence/confidence 포함)로 저장 |
| 인사이트 | **실행한 기능**(시장/타겟/브랜드/소재/종합 중 사용자가 누른 것) 각각이 §7 Output Schema의 **핵심(항상 수집)** 필드 기준으로 결과 생성, **선택 수집** 필드는 사용자가 체크한 범위 내에서만 생성, 미실행 기능은 판정 대상이 아님 (§7-0·§6-1) |
| 결과물 | 실행한 기능마다 HTML 섹션 생성 + Excel 해당 시트 갱신, 소재분석 실행 시 추가로 ZIP 생성 — **여러 기능을 하나로 묶는 별도의 "종합 다운로드" 버튼은 두지 않음**(★ 9차 개정, §16) |
| 실패 처리 | 외부 API 하나가 실패해도 그 기능 안에서 가능한 만큼 결과 생성 (아래 "전체 실패 vs 부분 성공" 기준, 기능 실행 단위로 적용) |
| 이력 | 완료/부분실패/취소/전체실패 **4가지** 상태가 History에 저장 (§6 "분석 취소의 범위") |
| 재다운로드 | 과거 분석 결과를 다시 다운로드 가능 — 단 실제 파일이 LRU 정책으로 삭제된 경우 `[재다운로드]`는 `파일 만료`로 비활성 표시 (§21-3) |
| 취소 | §6 "분석 취소의 범위"에 따라 처리되고 History에 별도 `취소` 상태로 기록됨 (§16) |
| 시간 | 자사 1 + 경쟁사 3(§10 비용 산정 기준과 동일 가정) 기준, 목표 실행 시간 15~20분 이내 완료 |
| 비용 | 1회 분석 실제 비용이 §10 경고 기준($1.00)을 초과하면 분석은 계속 진행하되 History에 초과 경고가 표시됨 (분석 중단 아님, §10) |

### 전체 실패 vs 부분 성공 정의 (★ 반드시 지킬 원칙, ★ 9차 개정 — 기능 실행 단위로 재정의)

* 이 판정은 **기능 실행(function_run) 1건 단위**로 적용합니다(세션 전체를 하나로 합산하지 않음, §6-1).
* **전체 실패**로 처리하는 경우는 딱 두 가지뿐입니다: (1) 브랜드명 입력값 자체가 유효하지 않음, (2) 그 기능 실행의 **핵심 데이터를 전혀 얻지 못한 경우**(아래 "기능별 상태 판정" 기준으로 `데이터 없음`).
* 그 외 모든 경우(네이버 캡처 실패, Gemini 일부 quota 초과, YouTube API 실패 등)는 **부분 성공(partial success)**으로 처리합니다 — 실패한 하위 항목만 "데이터 없음/캡처 실패"로 표시하고, 그 기능 안에서 나머지 항목은 정상 출력합니다.
* History의 상태값은 `완료` / `부분 실패` / `취소` / `전체 실패` **4가지**이며(취소 정의는 아래 참고), 이 판정 로직은 §11 오류 처리 정책과 직결됩니다.

### 분석 취소의 범위 (★ Streamlit 환경 특유의 제약 반영)

"분석 취소"는 파이프라인의 **추가 진행**을 중단하는 것이지, 이미 발생한 외부 작업을 되돌리는 것이 아닙니다. Streamlit은 장시간 실행 중인 백그라운드 작업을 프로세스 레벨에서 즉시 죽이기 어렵고, Apify actor처럼 우리 서버 밖에서 도는 작업은 우리가 강제 종료할 수 없는 경우가 많습니다. 그래서 계층별로 다르게 처리합니다.

* **아직 실행되지 않은 신규 API 호출**: 실행하지 않음 (즉시 중단)
* **이미 실행 중인 API 요청**: 가능한 경우 cancel/abort 처리 (예: Gemini 요청은 클라이언트 측에서 취소 가능)
* **이미 시작된 외부 장시간 작업(Apify actor 등)**: 외부에서 강제 종료가 불가능하거나 이미 과금이 발생한 경우, **actor는 계속 돌게 두되 그 응답을 기다리지 않고 결과를 폐기**하는 방식으로 처리 — Python 쪽에서 "기다리는 것을 멈추는 것"이지 "Apify 쪽 실행을 멈추는 것"이 아님을 명확히 구분
* 취소 시점까지 **이미 수집·저장된 데이터와 발생한 비용 정보는 그대로 보존**
* History 상태는 `부분 실패`가 아니라 **별도의 `취소` 상태**를 사용 — 사용자가 스스로 중단한 것과 시스템이 실패한 것은 UX상 다르게 보여야 함 (부분 실패로 뜨면 사용자가 "내가 취소했는데 왜 실패라고 나오지?"라고 오해)
* ★ **한 문장으로 확정**: **취소된 기능 실행은 History 상태를 `취소`로 기록하며, 취소 시점까지 생성된 데이터는 리포트 화면에 `부분 결과`로 표시합니다.** 즉 "상태(취소/완료/부분실패/전체실패)"와 "화면에 보여줄 결과의 완성도(부분 결과 여부)"는 서로 다른 축이며, 이 둘을 혼동하지 않습니다 — `취소` 상태는 항상 `부분 결과` 표시를 동반하지만, `부분 결과` 표시 자체가 `부분 실패` 상태를 뜻하지는 않습니다.

### "데이터 없음"과 "수집 실패"는 다르다 (★ §7 "필수"의 정확한 의미)

§7에서 어떤 필드를 "필수"라고 표시한 것은 **"반드시 0이 아닌 값이 나와야 한다"는 뜻이 아닙니다.** "데이터가 존재하거나 수집 가능한 경우 반드시 생성해야 하는 필드"라는 뜻입니다. 예를 들어 어떤 브랜드가 공식 YouTube 채널이 없다면, `youtube` 필드는 필수이지만 결과는 "채널 없음"이 정상입니다 — 이건 실패가 아닙니다.

| 상황 | 처리 |
|---|---|
| 브랜드에 YouTube 채널 없음 | 정상 — `channel_not_found` |
| YouTube API 자체 오류(네트워크/인증 등) | 부분 실패 (§11) |
| Meta 광고 0건 | 정상 — `ad_count: 0` |
| Meta API(Apify actor) 자체 실패 | 부분 실패 (§11) |
| 경쟁사 광고 0건 | 정상 — `ad_count: 0` |

규칙으로 정리하면:
1. **필수 데이터가 "존재하지 않음"** → 정상 처리 (0건/없음으로 값 채움, 실패 아님)
2. **필수 데이터의 "수집 시도 자체가 실패"** → 부분 실패 (§11 오류 처리 정책 적용)
3. **필수 데이터 전체(그 기능의 핵심 데이터 전부)를 수집할 수 없음** → 위 "전체 실패" 기준에 따라 판단

### 기능별 상태 판정 (★ 10차 개정 — "4대 영역 상태 판정"을 대체, 기능마다 다른 완료 조건을 명시)

각 **기능 실행(function_run)**의 상태는 그 기능의 **핵심 데이터**(§7-0 "핵심(항상)" 필드) 수집 결과만으로 판정합니다. 선택 수집 필드는 이 판정에 영향을 주지 않습니다. ★ 세션 전체를 하나의 상태로 합산하는 로직은 폐지합니다 — History(§16-6)와 §16-2 Workspace 탭·§16-4 상태 스트립은 5개 기능의 상태를 각자 독립적으로 보여주며(○/●/✓/△/✕/취소), 이를 억지로 하나의 값으로 합치거나 여러 기능을 하나로 묶어 내려받는 UI(종합 다운로드 버튼 등)도 두지 않습니다.

★ **"핵심 데이터 중 최소 1개만 있으면 완료"라는 단일 규칙은 기능마다 완료 조건의 성격이 달라 그대로 적용하면 오판정이 납니다** — 예: 브랜드분석은 자사 데이터(검색량+웹사이트)와 경쟁사 데이터가 **AND로 결합**돼야 의미가 있는데, "최소 1개"만 적용하면 자사 검색량 하나만 성공해도(홈페이지 크롤링·경쟁사 전부 실패) `완료`로 잘못 판정됩니다. 그래서 기능별로 완료/부분실패/전체실패 조건을 각각 명시합니다.

| 기능 | 완료(✓) | 부분 실패(△) | 전체 실패(✕, 데이터 없음) |
|---|---|---|---|
| 시장분석 | `search_volume_trend`(§7-1) 수집 API 자체가 성공 (값이 0이어도 정상) | 검색량은 성공했으나 계절성 후처리·뉴스 등 부가 항목 일부 실패 | `search_volume_trend` 수집 시도 자체가 실패 |
| 타겟분석 (★ 15차 개정 — 게이트형 + 추천/분석 2단계) | (`confirmed_target` 확정 후 "타겟분석 시작하기" 실행) `target_search_volume`/`interest_keywords`(§7-2) 등 핵심 데이터 수집 성공 | 핵심 데이터는 성공했으나 AI Persona(§7-2-a) 등 부가 항목 일부 실패 | "타겟분석 시작하기" 실행 자체가 실패(예: Gemini/네이버 API 오류). **`recommended_target` 생성(추천 단계)은 무료 자동 단계라 이 표의 `function_run` 대상이 아니며, 게이트가 아직 안 열린 상태(🔒)나 추천만 되고 아직 확정되지 않은 상태도 "실행"이 아니므로 이 표의 대상이 아님**(§6-1·§7-2) |
| 브랜드분석 (자사+경쟁사 통합) | **자사**의 `brand_search_volume` + `brand_website_facts`가 **모두** 성공 **AND** 경쟁사 중 최소 1개 브랜드의 `brand_search_volume`도 성공 (§7-3) | 위 조건 중 일부만 충족 — 예: 자사 핵심은 다 성공했지만 경쟁사 전원 실패, 또는 자사 검색량은 성공했으나 홈페이지 크롤링 실패 | **자사**의 `brand_search_volume`과 `brand_website_facts`가 **둘 다** 수집 시도 자체에 실패 (경쟁사 상태와 무관 — 자사 데이터가 아예 없는 리포트는 성립하지 않으므로) |
| 소재분석 | 자사 또는 경쟁사 중 **최소 1개 브랜드**의 `meta_ads` 조회 API가 성공 (0건 포함) | 일부 브랜드는 조회 성공, 나머지 브랜드는 API 자체 실패 | 자사+경쟁사 **전원**의 `meta_ads` 조회가 API 자체 실패 |
| 종합분석 | AI 합성 호출 자체가 성공 (입력 데이터가 일부뿐이어도, 그 범위 안에서 생성 가능한 필드는 채움) | 합성 호출은 성공했으나 입력 부족으로 일부 필드가 `insufficient_data` | AI 합성 호출 자체가 실패(예: Gemini API 오류) — §6-1 활성화 조건 자체를 못 채워 아예 실행되지 않은 경우는 "실행"이 아니므로 이 표의 대상이 아니며, 버튼이 비활성 상태로 남을 뿐 상태값을 만들지 않음 |

공통 원칙: 위 표의 모든 판정에서 "값이 0/없음"과 "수집 시도 자체의 실패"는 항상 구분합니다(§6 "데이터 없음과 수집 실패는 다르다" 그대로 적용) — 즉 검색량이 0이거나 광고가 0건이어도 API 호출 자체가 성공했다면 그 항목은 정상입니다. §16-4 Workspace 상태 스트립의 5개 기능 아이콘(시장분석/타겟분석/브랜드분석/소재분석/종합분석)은 이 표를 기능별로 그대로 시각화한 것입니다.

---

## 7. 분석 결과 Output Schema

> ★ "4대 영역 정리"가 추상적이라는 지적을 반영해, 영역별 필드를 명시합니다. 각 필드는 §8의 `FACT`(실제 수집 데이터) / `AI`(AI 해석) / `REC`(AI 제안) 태그로 분류됩니다. 표에 없는 필드를 리포트에 추가하는 것은 자유이나, 표에 **있는** 필드는 §6의 "필수의 정확한 의미"(데이터 없음=정상, 수집실패=부분실패)에 따라 채워야 합니다.

### 7-0. 수집 옵션과 "필수"의 관계 (★ 매우 중요)

§16 UI의 "수집 옵션" 체크박스와 아래 §7 표의 "필수" 표시가 서로 다른 기준처럼 보이지 않도록, 수집 항목을 두 그룹으로 명확히 나눕니다.

* **핵심(항상 수집)** — 사용자가 체크박스를 끌 수 없음. 시장 핵심 지표(검색량 추이/증감률), **Meta 광고 소재(자사+경쟁사)**, **브랜드 검색량**(자사+경쟁사, §7-9), **브랜드 홈페이지 분석**(자사+경쟁사, §7-3). 이것들이 없으면 리포트 자체가 성립하지 않으므로 항상 실행.
* **선택(체크박스로 켜고 끄기)** — Instagram, YouTube, **네이버 SA/브랜드검색**(브랜드검색 캡처+대표키워드 SA 순위 캡처 둘 다 포함, §7-3), **뉴스**(카테고리 시장 뉴스 + 자사·경쟁사 브랜드 언급 뉴스를 모두 포함, §7-1·§7-3), 미디어 인사이트 리포트, (검증 후) Google Ads Transparency Center.
  - 체크하지 않으면 해당 §7 필드는 `not_collected`로 표시되고 **실패로 집계되지 않습니다.**
  - 체크했는데 수집이 실패하면 §11 오류 처리 정책에 따라 **부분 실패**로 집계됩니다.

즉 아래 §7 표의 "필수" 칸은 "**핵심(항상)**" 또는 "**선택 시 필수**"로 세분화해 표기합니다.

### 7-1. 시장 (Market)

| 필드 | 설명 | 분류 | 필수 |
|---|---|---|---|
| search_volume_trend | 검색량 추이 (최근 12/3/1개월) | FACT | 핵심(항상) |
| search_volume_change_rate | 검색량 증감률 | FACT | 핵심(항상) |
| search_seasonality | (★ 신규) 검색량 시즈널리티 — **월별 지수**(1~12월 평균) + **요일별 지수**(평일 평균 vs 주말 평균, 그 격차 %) | FACT | 핵심(항상 — search_volume_trend의 일별 원본 데이터를 후처리해 산출하므로 추가 API 호출 불필요, §7-1-a) |
| top_news | 주요 뉴스 TOP 10 — **최근 1년·3개월·1개월 3개 구간별로 각각 집계** (제목/링크/요약/일자) | FACT | 선택 시 필수 (§16 "뉴스" 체크) |
| reference_sources | (★ 신규) 카테고리별 전문 통계·리포트 출처 매핑 — 사이트명/URL/자료 유형(통계·협회·미디어리포트·학술)/비고. **AI가 자동 요약·분석하지 않고 링크만 제공** — 본문 스크래핑은 저작권·로그인 문제로 하지 않음(§7-12 출처 매핑 원칙 참고) | FACT | 핵심(항상 — 카테고리 기준 고정 매핑 테이블 조회, 실시간 수집 아님) |
| market_issues | 주요 시장 이슈 요약 | AI | 선택 (§16 "미디어 리포트" 체크 시 근거 보강) |
| upcoming_changes | (★ 신규) 시장에 예정된 변화 — **뉴스 원문에 날짜·공식 발표가 명시적으로 언급된 경우에만** 인용 포함. AI가 발생 가능성을 추측해 생성하는 것은 금지 — 해당 조건을 만족하는 뉴스가 없으면 빈 배열(`[]`)로 정상 처리(§6 "데이터 없음=정상" 원칙과 동일) | AI (단, evidence는 뉴스 원문 인용만 허용, 추론 문장 생성 금지) | 선택 (§16 "뉴스" 체크 시에만 산출 — 뉴스 원본이 있어야 검증 가능하므로) |
| market_trend_judgement | 시장 성장/정체/감소 판단 | AI | 선택 |

#### 7-1-a. `search_seasonality` 구현 노트 (★ 신규)

* 별도 API 호출이 아니라, `search_volume_trend`용으로 이미 받아온 네이버 DataLab `timeUnit=date`(일별) 응답을 **후처리**해서 산출합니다 — 추가 비용·쿼터 소모 없음.
* 요일별 집계: 응답값(날짜별 지수)에 요일 라벨을 붙여 평일(월~금) 평균과 주말(토·일) 평균을 계산, 그 격차를 %로 표시.
* 월별 집계: `timeUnit=month`로 별도 조회하거나, 일별 데이터를 월 단위로 롤업.
* **주의**: 브랜드 자체 검색량처럼 절대 볼륨이 작은 니치 브랜드/카테고리는 요일별 진폭이 노이즈에 묻힐 수 있음 — 이런 경우 `confidence: low`를 함께 표기.

#### 7-1-b. `reference_sources` 매핑 원칙 (★ 신규)

* 카테고리 → 추천 출처의 **고정 매핑 테이블**을 코드에 미리 정의해두고, 사용자가 선택/추천된 카테고리에 해당하는 행만 조회해서 보여줍니다(실시간 크롤링 아님, 유지보수는 매핑 테이블 갱신으로 대응).
* 예시 매핑: 커머스/유통 → 대한상공회의소, 한국온라인쇼핑협회, 통계청 KOSIS / 금융·보험 → 금융감독원 통계포털, 보험연구원 / 광고·마케팅 트렌드(전 카테고리 공통) → 메조미디어, 나스미디어, DMC미디어, 오픈서베이, 한국리서치 / 학술 자료(전 카테고리 공통) → RISS, DBpia(로그인·구독 필요 안내와 함께 링크만 제공).
* 논문 원문·리포트 본문을 직접 수집·요약하지 않는 이유는 §20(저작권) 원칙과 동일합니다 — 이 필드의 역할은 "분석"이 아니라 "어디서 찾아볼 수 있는지 안내"입니다.

### 7-2. 타겟 (Target)

> ★ 15차 개정 — **타겟은 더 이상 STEP 1에서 입력받지 않습니다.** 13차 개정에서 도입한 게이트 구조(시장분석 + 브랜드분석이 모두 최소 `부분 실패` 이상이어야 잠금 해제)는 그대로 유지하되, 게이트 통과 이후를 "**타겟 추천**"과 "**타겟분석**" 두 단계로 명확히 분리합니다.
> 1. **타겟 추천**: 게이트가 열리면 시장분석·브랜드분석의 최신 캐시 결과를 근거로 AI가 `recommended_target`을 즉시 생성합니다(추가 수집 없이 이미 확보한 데이터로만 산출하므로 비용이 들지 않는 가벼운 합성 호출입니다). 사용자는 이 추천을 검토하고 **[확정]** 또는 **[수정 후 확정]** 할 수 있습니다(§9-2·§16-2).
> 2. **타겟분석**: 사용자가 확정한 `confirmed_target`을 기준으로만 세그먼트 검색량·관심 키워드·AI Persona(§7-2-a)를 실제로 수집·분석합니다. 이 단계가 §6-1의 `function_run`(비용·소요시간이 발생하는 실행)입니다.
>
> ★ 기존 `user_target`(STEP 1 사용자 입력값)은 폐기합니다 — STEP 1에는 애초에 타겟 입력 필드가 없습니다. `core_target`은 의미가 "추천"으로 좁아졌으므로 `recommended_target`으로 이름을 바꾸고, 사용자가 확정한 최종값은 `confirmed_target`으로 별도 관리합니다. 아래 3개 상태값으로 이 탭의 타겟 라이프사이클을 표현합니다.

| 상태(`target_status`) | 의미 |
|---|---|
| `not_set` | 게이트가 아직 안 열렸거나(🔒), 열렸지만 아직 추천이 생성되지 않은 초기 상태 |
| `recommended` | AI가 `recommended_target`을 생성해 사용자 확인을 기다리는 상태 |
| `confirmed` | 사용자가 그대로 또는 수정해서 확정 — 이 값이 `confirmed_target`이 되고, 이때부터 "타겟분석 시작하기"가 활성화됨 |

| 필드 | 설명 | 분류 | 필수 |
|---|---|---|---|
| target_status | 위 3개 상태값 중 하나 (`not_set`/`recommended`/`confirmed`) | FACT | 핵심(항상) |
| recommended_target | (구 `core_target`) **시장분석의 `search_volume_trend`/`search_seasonality`/`market_issues`와 브랜드분석의 경쟁 구도(§7-3 전체 브랜드 프로필)를 반드시 근거로 삼아** AI가 추천하는 핵심 타겟 — 성별+나이 조합(예: "30~50대 남녀")과 함께, **왜 이 타겟인지에 대한 전략적 근거 문장**을 반드시 포함(아래 서술 템플릿) | AI (§8 스키마, `evidence`에 시장·브랜드 데이터 인용 필수) | 핵심(항상 — 단, 타겟분석 탭이 잠금 해제된 이후에만 생성 가능, §6-1) |
| confirmed_target | 사용자가 `recommended_target`을 그대로 확정하거나 수정한 최종값 — 이후 `target_search_volume`/`interest_keywords`/`ai_persona` 등 모든 타겟분석 필드는 이 값을 기준으로 산출됨. 사용자가 아직 확정하지 않았으면 `null`(정상 — 타겟분석 미실행 상태와 동일) | FACT (사용자 확정값, AI 해석 아님) | 핵심(항상 — 단, 타겟분석 탭이 잠금 해제된 이후에만 존재 가능) |
| target_search_volume | §7-1 카테고리 검색량을 **세그먼트(성별/연령)로 필터링**해 차이 분석 — 네이버 DataLab `gender`/`ages` 필터 파라미터 사용(§7-9와 동일 메커니즘). `confirmed_target` 세그먼트 기준으로 조회 | FACT | 선택(`confirmed_target`이 세그먼트를 특정할 수 있을 때만) |
| target_pain_point | 타겟 Pain Point | AI | 선택 |
| interest_keywords | 관심 키워드 — **네이버 연관검색어/검색량 API로 추출** (Gemini 생성 아님, 원본 키워드+검색량을 함께 저장) | FACT | 핵심(항상) |
| ai_persona | AI 페르소나 제안 (아래 §7-2-a 3개 필드로 세분화) | AI | 선택 |
| target_disposition | (★ 신규, §7-2-a) 타겟의 성향(가격 민감/가치 중시, 신중형/즉흥형 등) | AI | 선택 |
| preferred_situation | (★ 신규, §7-2-a) 타겟이 탐색·고려하는 선호 상황·맥락(언제, 어떤 상황에서) | AI | 선택 |
| conversion_trigger_context | (구 `purchase_trigger_context`, ★ 15차 개정 — 명칭 범용화) 전환(구매/상담/가입/계약 등 §7-3-a `conversion_objective` 기준) 결정을 고려·가속시키는 정황적 트리거 | AI | 선택 |

**리포트 표시 예시** (§16 타겟분석 결과 화면, 게이트 해제 후):
```
[추천 단계 — 사용자 확정 전]
추천 타겟:        40~50대 남성
                  AI Interpretation · Confidence: medium
                  "경쟁사 삼성화재·현대해상은 30~40대 남성에 신뢰 소구로 집중하고 있으나,
                   자사는 검색 세그먼트 지수상 상대적으로 공략이 낮은 40~50대 남성에서
                   시장 성장률(+12%) 대비 경쟁 밀도가 낮아 이 구간을 선점하는 것을 추천"

                  [ 확정 ]   [ 수정 ]

[확정 후 — 타겟분석 실행 가능]
확정 타겟:        40~50대 남성 (사용자 확정)
[ 타겟분석 시작하기 ]
```
`recommended_target.insight`는 항상 이 서술 템플릿(경쟁사는 OOO / 시장상황은 OOO / 그래서 우리는 OOO 타겟을 선점·탈환·고수해야 한다)을 따르도록 프롬프트에 고정합니다. 사용자가 [수정]을 선택하면 자유 텍스트로 값을 바꿀 수 있고, 수정된 값도 `confirmed_target`으로 저장되어 이후 모든 타겟분석·종합분석의 기준이 됩니다.

#### 7-2-a. 타겟 성향/선호상황/전환 트리거 분석 방법 (★ 신규 — 근거 데이터 매핑, ★ 15차 개정 — "결제"→"전환"으로 명칭 범용화)

> ★ **중요한 전제**: ADetect에는 설문·패널조사·구매/가입/계약 등 실제 전환(CRM) 데이터가 없습니다. 아래 3개 필드는 검증된 소비자 조사 결과가 아니라, 이미 수집 중인 간접 행동 신호를 조합한 **AI의 정황적 추정**입니다. 리포트에는 이 3개 필드 옆에 항상 고정 문구 **"실제 소비자 조사가 아닌 검색행동·시의성 기반 간접 추정입니다"** 를 노출합니다 — 생략 불가.

| 필드 | 근거 데이터 | 근거가 되는 이유 |
|---|---|---|
| target_disposition (성향) | §7-2 `interest_keywords`(네이버 연관검색어) | "보험료 비교"·"환급형 단점"·"가입조건" 같은 실제 동시 검색어는 실제 탐색 행동의 증거 — 3개 신호 중 신뢰도가 가장 높음 |
| preferred_situation (선호 상황) | §7-1 `search_seasonality`(월별+평일/주말) | 검색이 몰리는 시점(주말/특정 월)이 곧 탐색·상의가 이루어지는 상황의 단서 |
| conversion_trigger_context (전환 트리거) | §7-1 `top_news`/`market_issues`/`upcoming_changes` | 제도 변경·경제 이슈 등 카테고리 단위 외부 트리거가 결정을 가속시킬 수 있음. 브랜드의 실제 전환 행동(§7-3-a `conversion_objective` — 구매/상담/가입/계약 등)에 맞춰 서술 톤이 달라짐 |
| (보조, 선택) 시장 반응 신호 | 소재분석 `appeal_tags` 분포(실행된 경우만) | 경쟁사 전체가 특정 소구로 수렴하면, 시장이 그 소구가 통한다고 베팅 중이라는 간접 증거 — 소재분석이 아직 없으면 이 신호 없이 나머지 3개만으로 산출(정상, §6) |

각 필드는 §8 스키마(`insight`/`source`/`evidence`/`confidence`)를 그대로 따르며, `evidence`에는 실제 연관검색어·검색량 스파이크 시점·뉴스 제목을 원문 그대로 인용합니다(요약 금지, 기존 §8 원칙과 동일).

### 7-3. 브랜드 프로필 (자사 1건 + 경쟁사 N건 — 완전히 동일한 스키마)

> ★ 자사와 경쟁사는 데이터 소스·분석 방식이 완전히 동일합니다(§10 "경쟁사는 자사와 동일 항목 전수 분석"). 아래 필드셋 **하나**를 자사 1건 + 경쟁사 N건에 그대로 반복 적용합니다. (이전 버전에서는 자사·경쟁사가 서로 다른 필드를 가진 두 개의 표였고, 그 결과 경쟁사 쪽에 youtube/instagram/naver/google_ads가 빠지고 자사 쪽에 utm이 빠지는 등 불일치가 있었습니다 — 이번에 하나의 표로 통합해 해소했습니다.)

| 필드 | 설명 | 분류 | 필수 |
|---|---|---|---|
| brand_search_volume | 브랜드명 표기 변형 합산 검색량 — ①상대 추이(DataLab, 0~100 지수) ②최근 30일 절대 검색량(PC+모바일, 네이버 검색광고 키워드도구) ③성별/연령 비중(가능한 경우) — 상세는 §7-9 | FACT | 핵심(항상) |
| brand_website_facts | 브랜드 홈페이지/대표 상세페이지에서 발췌한 원문 카피(USP·가격·프로모션 등, 원문 인용). **대표 상세페이지 URL은 선택 입력** — 없거나(커머스형 리테일러처럼 단일 대표 상세페이지가 없는 경우) 접근 실패 시 홈페이지/회사소개·브랜드스토리 페이지로 폴백, 그마저 없으면 홈페이지 메인만으로 처리(정상, §6). **★ 카테고리 구조 분석·상품 리뷰 분석은 범위에서 제외**(사이트마다 마크업이 달라 범용 자동화가 사실상 불가능 — 대신 §7-3-c "랜딩페이지 수동 체크 팁"을 리포트에 고정 텍스트로 제공) | FACT | 핵심(항상) |
| brand_website_target_message | 위 사이트 분석 기반 주요 타겟·메시지 해석 | AI | 핵심(항상) |
| brand_news | 브랜드명 언급 뉴스 — 최근 1년·3개월·1개월 구간별 (제목/링크/요약/일자) | FACT | 선택 시 필수 (§16 "뉴스" 체크) |
| youtube | 구독자·영상 수·최근 콘텐츠 (채널 없으면 `channel_not_found`로 정상 처리, §6) | FACT | 선택 시 필수 |
| instagram | 팔로워·포스팅 수·최근 게시물 캡션 | FACT | 선택 시 필수 |
| naver_brand_search | **브랜드검색** 광고 노출 캡처 + 클릭 시 랜딩 링크 분석 + 새로고침 반복으로 소재 로테이션 개수 체크 | FACT | 선택 시 필수 (캡처 실패 시 부분실패로 허용, §6) |
| naver_sa_ranking | (★ 9차 개정 — 대상 키워드 확장) 아래 두 종류의 키워드에 대해 **SA(파워링크)** 노출 순위 + 소재 캡처를 함께 수행. ① **대표키워드**(§9에서 확정, 기본값=카테고리명) ② **일반 키워드**(★ 신규 — 브랜드명이 아닌 카테고리 내 실사용 키워드, 예: "KB카드"의 "카드발급". §9에서 AI가 후보 제안 → 사용자가 추가/삭제) — 브랜드별로 `{keyword, keyword_type: brand_rep / generic, rank, has_ad, ad_copy_snippet, captured_at}` 구조로 저장. **★ 운영 키워드 "전체 목록" 추정이나 브랜드검색 계약금액 추정은 하지 않음** — 이 둘은 비공개 데이터라 신뢰할 수 있는 근사치를 낼 수 없다고 판단해 범위에서 제외(1순위 드롭 확정) | FACT | 선택 시 필수 |
| meta_ads | 소재 목록 + 메타데이터 (0건도 정상 처리). **어떤 Page를 조회할지는 §7-10 참고**. **★ 활성(active) 광고만 수집** — 중단된 과거 광고를 우회해서 가져오려는 시도는 하지 않음(공식 API가 일반 상업광고에 제공하지 않는 데이터이며, 이를 우회하는 것은 ToS 위반 소지가 있음). 리포트에는 "현재 활성 중인 소재 기준"임을 항상 명시 | FACT | 핵심(항상) |
| ad_delivery_start_time / ad_running_days | (★ 신규) 소재별 게재 시작일 및 오늘 기준 운영 일수. Phase 0에서 한국向 소재에도 이 필드가 안정적으로 오는지 검증 필요(§22 Phase 0 추가 항목) — 검증 실패 시 이 필드만 `unavailable`로 다운그레이드(부분 실패 아님) | FACT | 핵심(항상 — meta_ads 있을 때, 단 Phase 0 검증 결과에 따라 선택으로 하향 가능) |
| meta_utm | 랜딩 URL UTM 구조 분석 (캠페인/그룹/소재명 추정) | FACT | 핵심(항상 — meta_ads 있을 때) |
| google_ads | 소재 목록 | FACT | 미탑재 (§21-6 검증 전까지 §16 UI 비활성) |
| key_message / usp / creative_type | 주요 메시지 · USP · 소재 유형 (meta_ads 기반). **단일 값으로 수렴되지 않는 브랜드(다품목 판매 리테일러 등)는 상위 N개 패턴을 배열로 반환** (예: "무료배송 프로모션" / "신제품 입점 알림" / "시즌 세일" 복수 병기) — 억지로 하나로 합치지 않음 | AI | 핵심(항상) |
| appeal_tags | (★ 신규 — §5 "소재분석하기" 전용 심화 필드, §7-11-4 참고) 소재별 소구포인트 다중 라벨 태깅(가격소구/신뢰·전문성소구/편의성소구/사회적증거/공포소구/트렌드소구 등 고정 택소노미 중 해당하는 것 전부) + 이를 집계한 브랜드별 소구 비중(%) | FACT(태그 부여 자체는 AI 판단이므로 태그 결과는 AI, 집계 비율은 FACT) | 핵심(항상 — meta_ads 있을 때) |
| ad_count | 운영 중인 광고 수 (0건도 정상 처리) | FACT | 핵심(항상) |
| format_mix | 소재 유형 분포 | FACT | 핵심(항상) |
| promotion_fact | 프로모션/혜택 실제 문구 — 브랜드에 따라 구매 할인뿐 아니라 가입 혜택·상담 예약 혜택 등도 포함(예: "첫 구매 20% 할인", "다이렉트 가입 시 보험료 5% 할인", 원문 인용) | FACT | 핵심(항상) |
| promotion_interpretation | 프로모션의 전략적 의미 (예: "신규 고객 확보 목적의 가격 프로모션으로 판단") | AI | 핵심(항상) |
| cta | CTA | FACT | 핵심(항상) |
| landing_url | 랜딩 URL | FACT | 핵심(항상) |
| creative_key_visual | Creative Key Visual | AI | 핵심(항상) |
| recent_activity | 최근 광고 집행 여부 | FACT | 핵심(항상) |
| media_operation_matrix | (★ 신규 — §7-11-3·§7-11-0 참고) 자사+경쟁사를 행으로, 매체를 열로 둔 운영 여부(●/X) 매트릭스. **확실히 조사 가능한 매체만 포함**: 네이버 SA/브랜드검색, Meta Ads Library(FB+IG 광고 통합, "Instagram Ads Library"라는 별도 열은 두지 않음 — §7-11-0), Instagram 프로필 운영 여부, YouTube 공식 채널 운영 여부, (검증 후) Google Ads Transparency Center. 카카오모먼트·당근광고·OTT/오디오 광고 등 공개 라이브러리가 없는 매체는 "조사불가"로 명시하고 억지로 추정하지 않음 | FACT | 핵심(항상 — 위 개별 필드들의 존재 여부를 조합해 산출, 별도 수집 없음) |

#### 7-3-a. `brand_context` (★ 15차 개정 신규 — 내부 판단 필드, 사용자 입력·노출 없음)

> ★ **배경**: ADetect는 올리브영(통합 리테일/멀티브랜드 플랫폼), 메리츠화재(금융·보험 서비스), 나이키(멀티카테고리 단일 브랜드)처럼 성격이 전혀 다른 브랜드를 **같은 입력 폼**으로 받아야 합니다. 이 차이를 사용자에게 브랜드 유형을 선택하게 해서 해결하지 않고(그러면 STEP 1이 복잡해지고 유형별 화면이 늘어남), 이미 수집한 데이터(브랜드명·홈페이지·검색결과·뉴스)로 AI가 내부적으로만 판단하게 합니다.

| 필드 | 설명 | 분류 |
|---|---|---|
| brand_type | AI가 추론한 브랜드 성격에 대한 자유서술+태그(예: "멀티브랜드 리테일 플랫폼", "손해보험 서비스 브랜드", "멀티카테고리 제품 브랜드") — 고정된 선택지 목록이 아니며, 사용자가 고르지 않음 | AI |
| business_model | 수익·거래 구조 해석(예: 단일 브랜드 직접판매 / 여러 브랜드가 입점하는 플랫폼형 / 상담·가입·계약형 서비스) | AI |
| offering_type | 제공물의 성격(물리적 상품 / 디지털·구독 서비스 / 금융상품 / 복합) | AI |
| analysis_scope | 이번 분석의 범위. **MVP에서는 항상 고정값 `brand`**(브랜드 전체 기준) — 나이키처럼 카테고리가 다양해도 세분화하지 않고 브랜드 전체로 분석. `brand → category/product/service` 계층 분석은 향후 확장을 위해 데이터 모델에만 자리를 남겨두며, 이번 개정에서 UI·프롬프트로 구현하지 않음 | FACT(고정값) |
| conversion_objective | 이 브랜드에서 실제로 의미 있는 전환 행동 — 예: 나이키="구매", 올리브영="상품 탐색/구매/재방문", 메리츠화재="상담/견적/가입/계약". `brand_website_facts`·업종 신호를 바탕으로 AI가 추론 | AI |
| confidence | 위 판단들의 신뢰도 | AI |

이 필드들은 **별도 화면이나 사용자 입력으로 노출하지 않습니다.** 대신 `promotion_interpretation`(§7-3)·`conversion_trigger_context`(§7-2-a)·`recommended_target`의 근거 서술(§7-2) 등 기존 필드가 브랜드 성격에 맞는 표현을 쓰도록 프롬프트에 컨텍스트로만 주입합니다 — 예를 들어 나이키 리포트는 "구매 전환"으로, 메리츠화재 리포트는 "상담/가입 전환"으로 AI가 자동으로 다르게 서술하되, **화면 구조나 입력 항목 자체는 모든 브랜드에서 동일**합니다.

#### 7-3-c. 랜딩페이지 수동 체크 팁 (★ 신규 — 자동분석 대신 고정 안내 텍스트)

카테고리 구조 분류·상품 리뷰 분석은 자동화 범위에서 제외했지만(위 `brand_website_facts` 비고 참고), 리포트 하단에 아래와 같은 **고정 체크리스트**(AI 생성 아님, 데이터 수집 아님)를 항상 포함해 사용자가 직접 확인하도록 안내합니다:
* 카테고리/메뉴 depth와 대표 카테고리 노출 순서
* 대표 상품의 리뷰 수·평점·최근 리뷰 작성 빈도
* 재구매·구독 유도 UI(정기배송, 재구매 알림 등) 존재 여부
* 첫 구매 혜택과 락인(멤버십·적립) 장치의 균형
이 항목들은 브랜드마다 자동 수집하지 않고, "이런 걸 사람이 직접 보면 좋다"는 참고용 팁으로만 제공합니다. (★ 15차 개정) 이 체크리스트는 상품 상세페이지가 있는 커머스/리테일형 브랜드를 염두에 둔 예시입니다 — 메리츠화재처럼 물리적 상품이 없는 서비스 브랜드에는 해당 항목이 `해당없음`일 수 있으며, 이는 정상입니다(§6 "데이터 없음=정상" 원칙과 동일).

### 7-4. 종합 (Synthesis) — §5 "종합분석하기" 버튼의 산출물

> ★ 이 영역은 별도 수집을 하지 않고, 시장/타겟/브랜드/소재 4개 기능의 **최신 캐시 결과를 조합**해서만 산출합니다(§6-1). 4개 중 일부만 실행된 상태라면, 없는 데이터가 필요한 필드는 `insufficient_data`로 표시하고 나머지는 있는 데이터만으로 산출합니다(부분 성공 원칙 그대로 적용).

| 필드 | 설명 | 분류 | 필수 |
|---|---|---|---|
| share_of_search | (★ 신규) **Share of Search** — §7-9 브랜드 검색량 지수(자사 앵커 정규화)를 자사+경쟁사 합계 대비 비율(%)로 환산 | FACT | 필수 (brand_search_volume 있을 때) |
| share_of_ads | (★ 신규) **Share of Ads** — 자사+경쟁사 `ad_count`(활성 소재 수) 합계 대비 비율(%). ★ 주의: 소재를 많이 뿌리는 브랜드가 유리해 보이는 착시가 있을 수 있어, 리포트에 이 한계를 각주로 명시 | FACT | 필수 (meta_ads 있을 때) |
| share_of_voice_news | (★ 신규) **Share of Voice(뉴스)** — 자사+경쟁사 브랜드 언급 뉴스 건수 합계 대비 비율(%) | FACT | 필수 (brand_news 있을 때) |
| positioning_map | (★ 신규) X/Y축 포지셔닝맵 — AI가 데이터 기반으로 축 이름 후보 제안(예: 가격↔프리미엄, 신뢰↔트렌디) → 사용자가 확정(§9와 동일한 "추천 후 사용자 확인" 원칙) → 브랜드별 정성 데이터를 정량화(프로모션 빈도, 메시지 톤 점수 등)해 좌표 산출. **자체 HTML 리포트 내 차트로 시각화**(§16) — 외부 도구(NotebookLM 등) 연동은 하지 않음(선택 기능·보류, §5) | AI (좌표 산출 로직 + evidence 포함, §8 스키마) | 필수 (브랜드분석 2개 이상 브랜드 데이터가 있을 때) |
| competitive_landscape | 경쟁 구도 | AI | 필수 |
| common_message | 공통 메시지 | AI | 필수 |
| differentiation_point | 차별화 포인트 | AI | 필수 |
| white_space | White Space | AI | 필수 |
| recommended_angle | 추천 Message Angle | REC | 필수 |
| one_line_summary | 한 줄 총평 | REC | 필수 |

### 7-9. `brand_search_volume` 구현 노트 (자사·경쟁사 공통)

> ★ 브랜드명 하나로도 실제로는 영문/한글/띄어쓰기/대소문자가 섞인 여러 표기가 함께 검색됩니다(예: "KB Pay"·"kbpay"·"케이비페이"·"kb페이"·"케이비pay"). 이걸 합산하지 않으면 검색량이 표기별로 쪼개져 실제보다 훨씬 작게 잡힙니다.

**① 상대 추이 (핵심)**
* **데이터 소스**: 네이버 DataLab **통합검색어트렌드 API**(`/v1/datalab/search`)의 `keywordGroups` 파라미터. 하나의 그룹명 아래 표기 변형 키워드들을 배열로 넣으면 API가 자동으로 합산한 시계열을 반환 — 원래 이 용도(동일 대상의 여러 표기 통합)로 설계된 기능.
* **표기 변형 후보 생성**: AI가 브랜드명을 보고 로마자 표기·한글 음차·띄어쓰기 변형 후보를 생성 → §9 자동추천 플로우와 동일하게 **자사 브랜드는 사용자가 확인/수정한 뒤 확정**(정확도가 가장 중요한 핵심 지표이므로). 경쟁사 브랜드는 별도 확인 단계 없이 자동 생성된 변형을 그대로 사용(UX 부담 최소화) — 다만 실제 사용된 변형 키워드 목록은 Excel/원본 JSON에 그대로 남겨 사후 검증 가능하게 함(§13 원칙과 동일).
* **★ 상대 지수 주의**: 응답값은 조회 기간 내 최고치를 100으로 정규화한 **상대 지수**이며, 절대 검색 횟수가 아닙니다.
* **★ 자사·경쟁사는 같은 API 호출에 그룹으로 함께 조회**: 정규화 기준(최고치=100)이 호출마다 독립적으로 잡히므로, 자사와 경쟁사를 따로 호출하면 두 결과를 비교할 수 없습니다. 자사 1개 + 경쟁사 N개 그룹을 **한 번의 요청**에 함께 담아야 비교 가능한 지수가 나옵니다.
* **★ 브랜드 수가 그룹 상한을 넘길 때 — 자사를 앵커로 고정**: 이 API가 한 번에 담을 수 있는 그룹 수에는 상한이 있습니다(§21-8, 최신 문서로 재확인 필요). 자사+경쟁사 합이 이 상한을 넘으면 여러 번 나눠 호출해야 하는데, 이때 **자사 브랜드 그룹을 모든 분할 호출에 공통으로 포함시켜 "앵커"로 고정**합니다 — 그래야 호출 A와 호출 B의 결과를 자사 지수를 기준점 삼아 재정규화해 서로 비교할 수 있습니다. 자사 없이 경쟁사끼리만 따로 호출하면 그 결과들은 서로 비교가 불가능합니다.

**② 최근 30일 절대 검색량 (핵심, 신규 추가)**
* DataLab 통합검색어트렌드는 상대 지수만 주므로, 절대 수치가 필요하면 **네이버 검색광고(광고주센터) 키워드도구 API**(`/keywordstool`)를 별도로 사용합니다 — 키워드별 최근 30일 PC/모바일 검색수(근사치)를 반환.
* 이 API는 그룹 합산 기능이 없으므로, 표기 변형 키워드를 **개별 호출 후 우리 코드에서 직접 합산**합니다.
* **★ 별도 계정·인증 필요**: DataLab과는 다른 발급 경로(네이버 검색광고 광고주센터, API Key + Secret Key + Customer ID, HMAC 서명 방식)가 필요합니다 — §21-10 신설 참고.

**③ 성별/연령 비중 (핵심, 신규 추가 — 조건부 제공)**
* DataLab 통합검색어트렌드 API는 `device`/`gender`/`ages` 필터 파라미터를 지원해 세그먼트를 좁혀 조회할 수 있습니다 (§7-2 `target_search_volume`도 동일 메커니즘 사용).
* **★ 주의**: 이 필터도 결과가 매 호출 독립적으로 정규화되므로, "남성 60% : 여성 40%" 같은 **비중(%)으로 명확히 환산 가능한지는 Phase 1 구현 중 실측 검증이 필요**합니다. 검증 결과 % 환산이 안정적이지 않으면, 이 지표는 "성별/연령별 상대 인덱스(참고용)"로 톤다운해서 제공하고 "비중"이라는 표현은 쓰지 않습니다.
* 산출 실패 시 §6 원칙에 따라 `demographics: unavailable`로 정상 처리(부분 실패 아님) — 부가 지표이므로.

### 7-10. 자사/경쟁사 광고 페이지·계정 식별 (Brand Resolution) 구현 노트

> ★ "브랜드명 텍스트 하나로 자동 실행"이 이 도구의 핵심 가치인데, Meta Ads Library·Instagram은 "브랜드명"이 아니라 **특정 Page/계정**을 대상으로 조회합니다. 브랜드명이 흔하거나 대형 리테일러처럼 지점·이벤트·해외 채널 페이지가 여럿이면 잘못된 페이지를 잡을 위험이 실재합니다 — 예: "올리브영"으로 검색하면 공식 계정 외에 매장별 페이지, 해외 채널("OliveYoung Global"), 과거 서브 브랜드의 흔적 페이지까지 함께 잡힐 수 있습니다. 기존 레퍼런스 코드(`fetch_competitor_ads.py`)는 사용자가 Ad Library URL을 직접 넣어주는 방식이라 이 문제가 없었지만, ADetect는 이 해석 단계를 새로 구현해야 합니다.

* **자사**: 브랜드명으로 Meta/Instagram 페이지 후보를 검색 → §9 자동추천 확인 화면에서 후보 목록(페이지명·팔로워 수·공식 인증 배지 여부)을 보여주고 **사용자가 정확한 페이지를 선택/확정**한 뒤에만 수집 시작. §7-9 브랜드 검색 키워드 확인과 동일한 패턴(정확도가 중요한 항목은 항상 사용자 확인).
* **경쟁사**: 별도 확인 단계 없이 **팔로워 수 최댓값 + 공식 인증 배지 우선순위**로 자동 선택(UX 부담 최소화, §7-9와 동일 원칙). 단 선택된 Page ID/계정명은 Excel·원본 JSON에 투명하게 기록(§13)해 사후 검증 가능하게 함.
* **경쟁사 활성 여부 확인**: AI가 추천한 경쟁사가 실제로는 폐업·서비스 종료 상태라 광고 페이지 자체가 존재하지 않거나 최근 활동이 전혀 없을 수 있습니다(업체 통폐합·리브랜딩이 잦은 업종일수록 빈번 — 예: H&B 리테일 카테고리는 지난 수년간 주요 플레이어가 여럿 폐업). 페이지를 찾지 못한 경우 §6 "데이터 없음=정상" 원칙과는 별개로, **§9 확인 화면에서 "페이지를 찾지 못함 — 다른 경쟁사로 교체하시겠습니까?"를 표시**해 사용자가 걸러낼 수 있게 합니다.
* **경쟁사 추천 개수는 4~5개로 고정하지 않음**: 직접 경쟁 구도가 명확한 카테고리는 4~5개 추천이 자연스럽지만, 직접 경쟁사가 그만큼 없는 카테고리(예: 독과점적 리테일러)는 2~3개만 추천하고 **인접 카테고리로 보완했다는 사실을 §9 확인 화면에 고지**합니다. 억지로 개수를 채우기 위해 관련성 낮은 업체를 끼워 넣지 않습니다.
* **사용자 입력 경쟁사 우선**(★ 15차 개정): STEP 1에서 사용자가 경쟁사를 직접 입력한 경우, §9 확인 화면에는 그 값이 우선 반영되어 미리 체크된 상태로 표시됩니다. AI는 사용자가 입력하지 않은 나머지 슬롯만 보완 추천하며, 사용자 입력값을 임의로 대체하지 않습니다. 경쟁사를 전혀 입력하지 않았다면 AI가 전체를 추천합니다.
* **경쟁사 유형 분류(내부 판단, 비노출)**: AI가 경쟁사 후보를 고를 때 내부적으로 `direct_competitor`(직접 경쟁)/`adjacent_competitor`(인접 경쟁)/`alternative_channel`(대체 채널)로 구분해 판단할 수 있습니다(예: 올리브영 기준 — 직접 경쟁: 세포라·시코르, 대체 채널: 쿠팡, 인접 경쟁: 무신사). 이 분류는 추천 근거 문구에 자연어로 녹여 설명하는 용도로만 쓰이며, 사용자에게 이 3개 값을 직접 입력·선택하게 하는 UI는 MVP에 두지 않습니다.

### 7-11. 기능별 최종 산출물 명세 (★ 9차 개정 신규 — 모듈형 버튼 구조에 따른 Deliverable Spec)

> ★ §5의 5개 기능(§16 Workspace에서는 5개 탭으로 표현) = 5개의 독립된 산출물 단위입니다. 아래 "테이블"은 §14 Excel 시트와 1:1 대응하고, DB에는 `collected_at` 기준으로 세션을 넘어 누적 저장됩니다(§13) — 동일 브랜드를 여러 번 분석하면 시계열이 쌓여 §2 시나리오 3(월간 모니터링)의 기반이 됩니다. "최종 인사이트"는 모두 §8의 FACT/AI/REC 스키마를 따르되, 리포트 상단에는 그중 `one_line`(1~2문장)만 배지로 노출하고 나머지는 펼침 상세로 둡니다.

#### 7-11-0. Meta Ads / Instagram 용어 정리 (★ 10차 개정 — 데이터 소스 3종을 명확히 구분)

기존 PRD에서 "Meta/Instagram"이라는 표현이 반복되면서, 광고 데이터와 SNS 프로필 데이터가 하나처럼 읽히는 문제가 있었습니다. 실제로는 서로 다른 3가지 개념이며, 아래처럼 명확히 구분합니다.

| 개념 | 정의 | 수집 방법 | 저장 위치 |
|---|---|---|---|
| **Meta Ads (광고 소재)** | Meta Ads Library에서 조회되는 활성 광고. **페이스북·인스타그램·메신저·오디언스 네트워크 노출을 전부 포함하는 하나의 데이터**입니다 — "인스타그램 전용 광고 라이브러리"는 별도로 존재하지 않습니다(메타의 광고 시스템 자체가 지면 통합형이기 때문). 개별 광고가 어느 지면에 노출됐는지는 `publisher_platforms` 메타데이터로 구분(예: "facebook,instagram") | `ad_library.py`의 `fetch_meta_ads()` (Apify `facebook-ads-library-scraper` 액터) | §7-3 `meta_ads`, `09_Ads` 시트 |
| **Instagram Profile (SNS 운영현황)** | 광고가 아니라 브랜드의 인스타그램 **계정 자체**의 팔로워 수·포스팅 수·최근 게시물 캡션. Meta Ads와 완전히 무관한 별도 데이터 | `ad_library.py`의 `fetch_instagram_profile()` (별도 Apify 인스타그램 프로필 액터) | §7-3 `instagram`, `06_Brand_Profile` 시트 |
| **"Instagram Ads"라는 별도 수집원** | **불필요 — 만들지 않음.** 위 표처럼 인스타그램에 노출되는 광고도 이미 Meta Ads Library 안에 포함되어 있으므로, 이를 따로 수집하는 3번째 파이프라인은 없습니다. §7-3 `media_operation_matrix`의 열도 `meta_ads`(광고, FB+IG 통합) / `instagram_profile`(SNS 운영여부) 두 개로만 구성하고, "instagram_ads"라는 열은 두지 않습니다 |

Phase 0 스파이크에서는 "Instagram 광고를 별도로 수집해야 하는가"를 검증하는 게 아니라(위 표로 이미 확정), **`publisher_platforms` 필드가 한국向 광고에도 안정적으로 채워지는지만** 확인하면 됩니다(§22).

#### (1) 시장분석하기 — 테이블 4개

| 테이블(Excel 시트) | 주요 컬럼 |
|---|---|
| `02_Market_Trend` | `collected_at`, `category`, `period_unit`(date/week/month), `period_date`, `search_index`, `mom_change_rate`, `yoy_change_rate` |
| `03_Market_Seasonality` (신규) | `category`, `weekday_avg_index`, `weekend_avg_index`, `weekday_vs_weekend_gap_pct`, `month`(1~12), `month_avg_index`, `peak_month`, `trough_month` |
| `04_Market_References` (신규) | `category`, `source_name`, `source_type`, `url`, `note` |
| `13_News` (scope=`market`) | `collected_at`, `category`, `period_bucket`(1년/3개월/1개월), `title`, `url`, `summary`, `published_at`, `press` |

**최종 인사이트 포맷** (`market_trend_judgement` + `market_issues` + `upcoming_changes`):
```json
{
  "one_line": "손해보험 시장은 최근 3개월 검색량이 전년 대비 12% 증가하며 완만한 성장세",
  "insight": "...(3~5문장 서술)...",
  "upcoming_changes": ["2027년 X월 OO법 개정 시행 예정(OO뉴스 2026-08-01 보도)"],
  "source": ["Naver DataLab 12개월", "뉴스 18건"],
  "evidence": ["...원문 인용..."],
  "confidence": "medium"
}
```
`upcoming_changes`는 뉴스 원문에 날짜·공식 발표가 없으면 빈 배열 — 추측 문장을 만들어 채우지 않습니다(§7-1).

#### (2) 타겟분석하기 — 테이블 2개 (★ 15차 개정 — 게이트형 + 추천/분석 2단계, §7-2)

> ★ 이 탭은 시장분석 + 브랜드분석이 둘 다 최소 `부분 실패` 이상이어야 잠금 해제됩니다(§6-1). 잠금 해제 전에는 이 아래 테이블/인사이트가 전혀 생성되지 않습니다(🔒, §16-2). 잠금 해제 직후에는 `recommended_target`만 자동 생성되고, 사용자가 확정(`confirmed_target`)한 뒤에야 나머지 필드(세그먼트 검색량·관심 키워드·AI Persona)가 생성됩니다(§9-2).

| 테이블 | 주요 컬럼 |
|---|---|
| `05_Target` | `collected_at`, `category`, `target_status`(not_set/recommended/confirmed), `recommended_target`(AI 추천, 성별+연령), `recommended_target_rationale`(추천 근거 서술, §7-2 서술 템플릿), `confirmed_target`(사용자 확정값), `gender`, `ages`, `segment_search_index`, `interest_keyword`, `interest_keyword_volume` |
| `05b_AI_Persona` (신규, §7-2-a) | `target_disposition`, `preferred_situation`, `conversion_trigger_context`, 각 필드의 `evidence`·`confidence`, `disclaimer`(고정 문구: "실제 소비자 조사가 아닌 검색행동·시의성 기반 간접 추정") |

**최종 인사이트**: `recommended_target`의 `insight`가 §7-2 서술 템플릿(경쟁사는 OOO / 시장상황은 OOO / 그래서 우리는 OOO 타겟을 선점·탈환·고수)을 따르는 `one_line` 헤드라인이 됩니다. 그 아래 `target_pain_point` + `05b_AI_Persona`의 3개 필드가 펼침 상세로 이어지며, 이 3개 필드에는 항상 위 `disclaimer` 문구가 함께 노출됩니다(§7-2-a, 생략 불가). 리포트 상단에는 `target_status`에 따라 추천 단계(`recommended_target`만 표시 + [확정]/[수정])와 확정 단계(`confirmed_target` 표시)를 구분해 보여주며, 최초 사용자 입력 타겟이라는 개념 자체가 없으므로 "입력값과 비교"하는 표현은 쓰지 않습니다(§7-2 표시 예시).

#### (3) 브랜드분석하기 — 테이블 3개 (자사+경쟁사 통합, §9-15 반영)

| 테이블 | 주요 컬럼 |
|---|---|
| `06_Brand_Profile` | `brand`, `is_own`(bool), `collected_at`, `brand_search_index`, `brand_search_absolute_30d`, `gender_ratio`(옵션), `website_usp_summary`, `youtube_subs`, `instagram_followers`, `recent_activity` |
| `07_Naver_Operation` (신규) | `brand`, `is_own`, `keyword`, `keyword_type`(brand_rep/generic), `rank`, `has_ad`(bool), `ad_copy_snippet`, `captured_at` |
| `08_Media_Matrix` (신규) | `brand`, `naver_sa`(●/X), `naver_brand_search`(●/X), `meta_ads`(●/X — **페이스북+인스타그램 노출 광고 통합**, §7-11-0), `instagram_profile`(●/X — 광고 아닌 SNS 프로필 운영 여부), `youtube_channel`(●/X), `google_ads`(조건부/미검증) |

브랜드별 뉴스는 `13_News`(scope=`brand`)에 `brand` 컬럼과 함께 누적됩니다. UTM 구조는 `14_UTM`(brand, campaign, group, creative_name_guess, landing_url)에 누적됩니다.

**최종 인사이트**: 브랜드별 `brand_website_target_message`/`promotion_interpretation`(§8 스키마, 브랜드당 1건) + 전체 브랜드를 모은 `one_line` 비교 총평(예: "경쟁사 3사 중 2사가 신뢰 소구, 자사만 가격 소구 중심").

#### (4) 소재분석하기 — 테이블 4개 + ZIP (자사+경쟁사 통합, DA 전용)

| 테이블 | 주요 컬럼 |
|---|---|
| `09_Ads` (Canonical Ad Schema 확장) | `brand`, `platform`(`meta`/`google` — 수집 출처, §7-11-0), `publisher_platforms`(예: "facebook,instagram" — 노출 위치, Meta 응답 그대로), `ad_id`, `format`, `headline`, `body`, `cta`, `landing_url`, `ad_delivery_start_time`, `ad_running_days`, `collected_at` |
| `10_CreativeAnalysis` | `ad_id`, `brand`, `headline`, `body_copy`, `appeal_tags`(다중 라벨), `product`, `image_characteristics`(예: 인물컷/제품컷/텍스트비중/컬러톤) |
| `11_Appeal_Distribution` (신규, 집계) | `brand`, `appeal_tag`, `count`, `pct_of_brand_total` |
| `12_LongRunning_Analysis` (신규) | `brand`, `running_days_bucket`(단기<14일/중기14~90일/장기90일+), `dominant_appeal_tags`, `avg_running_days` |

원본 이미지/영상 파일은 `packager.py`로 브랜드별 폴더 구조 ZIP으로 별도 다운로드됩니다(기존 §16 산출물 다운로드에 통합).

**최종 인사이트 포맷** (`creative_key_visual` + 소구 비중 종합):
```json
{
  "one_line": "메리츠화재는 광고의 62%가 '신뢰·전문성' 소구이며, 90일 이상 장기 운영 소재는 대부분 가격 프로모션 없이 브랜드 메시지 위주",
  "source": ["Meta Ads Library 42건"],
  "evidence": ["..."],
  "confidence": "medium"
}
```

#### (5) 종합분석하기 — 테이블 3개 (§7-4 그대로, 별도 수집 없음)

| 테이블 | 주요 컬럼 |
|---|---|
| `15_SOV` (신규) | `brand`, `share_of_search`, `share_of_ads`, `share_of_voice_news`, `collected_at` |
| `16_Positioning` (신규) | `brand`, `x_axis_score`, `y_axis_score`, `x_axis_label`, `y_axis_label` |
| `17_AI_Insights` | `field`, `insight`, `source`, `evidence`, `confidence` (§8 스키마 그대로, `competitive_landscape`/`common_message`/`differentiation_point`/`white_space`/`recommended_angle`/`one_line_summary` 각 1행) |

**최종 인사이트**: `one_line_summary`(REC) — 리포트 최상단 배너 문구, §16 "AI Recommendation · Confidence" 캡션과 함께 노출.

---

## 8. AI 판단 vs 사실 데이터 구분 (Source / Confidence / Evidence)

> ★ AI가 임의로 추론한 정보와 실제 수집된 정보가 섞이면 리포트 신뢰성이 무너집니다. 이건 단순 개발 이슈가 아니라 **이 도구의 신뢰도를 결정하는 요구사항**이므로 스키마 레벨에서 강제합니다.

### 8-1. 3단계 분류

* **FACT** — 실제 수집된 원본 데이터 그대로(예: "광고 12건 수집됨", "CTA 버튼 문구: 지금 구매하기").
* **AI INTERPRETATION** — Gemini가 수집 데이터를 보고 추론한 해석(예: "20대 여성을 주요 타겟으로 함").
* **RECOMMENDATION** — Gemini가 자사 입장에서 제안하는 액션(예: "가격 소구보다 저자극 소구를 권장").

§7 Output Schema의 모든 필드는 이 3단계 중 하나로 태깅되어 있습니다. 리포트(HTML)에서도 이 3종을 색상/배지로 시각적으로 구분해서 표시합니다(§16).

### 8-2. AI INTERPRETATION / RECOMMENDATION 필드의 JSON 스키마

`AI`/`REC`로 분류된 모든 필드는 아래 스키마를 반드시 포함합니다 — `insight` 텍스트만 저장하는 것은 금지합니다.

```json
{
  "insight": "가격 경쟁 중심의 광고 전략을 사용하고 있음",
  "source": [
    "Meta Ads Library 12건",
    "Landing Page 3건"
  ],
  "evidence": [
    "최대 50% 할인",
    "첫 구매 혜택",
    "기간 한정 프로모션"
  ],
  "confidence": "high"
}
```

* `source`: 이 판단의 근거가 된 원본 데이터의 출처(플랫폼 + 건수).
* `evidence`: 실제로 확인된 문구/수치 인용 (요약 금지, 원문 그대로).
* `confidence`: `high` / `medium` / `low` — 근거 데이터 건수가 적거나 상충되는 신호가 있으면 `low`.
* `FACT` 필드는 이 스키마가 필요 없으며, 대신 §13의 `collected_at`/`source_url`을 그대로 사용.

---

## 9. 입력값 자동 추천 & 사용자 확인 플로우

> ★ "카테고리·경쟁사 미입력 시 AI가 추천"이라는 문구만으로는 개발자가 "추천 후 자동 확정"과 "추천 후 사용자 확인" 중 무엇을 구현해야 할지 알 수 없습니다. **반드시 후자입니다 — AI는 추천만 하고, 확정은 항상 사용자가 합니다.** (★ 15차 개정) **타겟은 이 STEP 1 플로우의 대상이 아닙니다** — STEP 1에서는 카테고리·경쟁사만 추천하며, 타겟 추천은 시장분석+브랜드분석이 끝난 뒤 타겟분석 탭에서 별도로 이뤄집니다(§7-2·§9-2).

### 플로우

1. 사용자가 브랜드명만 입력하고 "다음" 클릭
2. AI가 카테고리·경쟁사·광고 페이지 후보를 추천 (예: 브랜드명 = "메리츠화재" → 카테고리: 손해보험, 경쟁사: 삼성화재·현대해상·DB손해보험·KB손해보험)
   - **경쟁사 개수는 4~5개 고정이 아닙니다.** 직접 경쟁사가 그만큼 없는 카테고리(예: 독과점적 리테일러)는 2~3개만 추천하고 인접 카테고리로 보완했음을 아래 화면에 고지합니다. 추천 경쟁사의 광고 페이지를 찾지 못하면(폐업·서비스 종료 등) "페이지를 찾지 못함 — 교체 필요"로 표시합니다 (§7-10).
3. 아래와 같은 확인 화면을 보여주고, **사용자가 체크박스 해제/직접 수정 후 확정해야만** 다음 단계(§16 STEP 2 — 분석 Workspace)로 진입함. ★ 12차 개정 — 이 화면에서 확정한다고 곧바로 수집·분석이 시작되는 것은 아닙니다(구 "이대로 분석하기"는 단일 파이프라인 시절의 표현). 확정 후에는 아직 아무 기능도 실행되지 않은 Workspace로 들어가고, 그 안에서 사용자가 원하는 기능 탭을 선택해 각각 시작합니다(§16-1·§16-2).
   - **타겟은 이 화면에서 입력받지 않습니다.** 데이터 기반 타겟 추천(`recommended_target`)은 시장분석+브랜드분석이 완료된 뒤 타겟분석 탭에서 별도로 생성되며, 사용자가 확정(`confirmed_target`)해야 타겟분석이 실행됩니다(§7-2·§9-2).

```
브랜드명을 기반으로 다음 정보를 추천했습니다. (★ 15차 개정 — 타겟 입력/추천 없음, §7-2·§9-2)

카테고리
[ 손해보험                 ]

대표키워드 (네이버 SA/파워링크 순위 확인용, §7-3 naver_sa_ranking)
[ 손해보험                 ]  (기본값 = 카테고리, 수정 가능)

일반 키워드 (★ 신규 — 브랜드명이 아닌 카테고리 실사용 키워드의 SA 운영 여부·순위 모니터링, §7-3 naver_sa_ranking)
☑ 다이렉트 보험
☑ 자동차보험 비교
☑ 실손보험 추천
[+ 키워드 추가]  (AI가 카테고리 기반으로 3~5개 후보 제안, 경쟁사에도 동일 키워드셋 적용)

광고 페이지 확인 (자사, §7-10)
◉ 메리츠화재 공식 (팔로워 12만, 인증 배지)
○ 메리츠화재 다이렉트 (팔로워 8천)
[다른 페이지 검색]

경쟁사 (사용자가 STEP 1에서 직접 입력했다면 그 값이 우선 반영됨, §7-10)
☑ 삼성화재
☑ 현대해상
☑ DB손해보험
☑ KB손해보험
[+ 경쟁사 추가]

브랜드 검색 키워드 (검색량 합산 대상, §7-9)
☑ KB Pay
☑ kbpay
☑ 케이비페이
☑ kb페이
☑ 케이비pay
[+ 키워드 추가]

[수정]  [확정하고 Workspace로 이동]
```

4. 사용자가 "확정하고 Workspace로 이동"을 누른 시점의 값이 이후 모든 기능 탭(§16)의 공통 입력값으로 고정됨 (Workspace 진입 후에는 변경 불가 — 값을 바꾸려면 새 세션으로 다시 시작). "브랜드 검색 키워드"·"광고 페이지 확인"은 자사에만 이 확인 단계가 적용되며, 경쟁사의 표기 변형·광고 페이지는 별도 확인 없이 자동 선택값을 사용합니다(§7-9·§7-10) — 단, 사용된 값은 Excel/원본 JSON에 투명하게 기록됩니다(§13).

이 단계는 §6 Acceptance Criteria의 "자동추천" 행으로 강제됩니다.

### 9-2. 타겟 추천 플로우 (★ 15차 개정 신규 — 게이트 통과 후, §7-2)

STEP 1에는 타겟 입력·추천이 전혀 없습니다. 타겟은 시장분석과 브랜드분석 결과가 쌓인 뒤, 타겟분석 탭 안에서만 추천됩니다. 경쟁사 추천(§9 위 플로우)이 브랜드/시장 데이터 수집보다 먼저 일어나는 것과 달리, 타겟 추천은 그 데이터가 실제로 쌓인 **뒤**에 일어난다는 순서를 반드시 지킵니다.

1. 시장분석 탭과 브랜드분석 탭이 각각 최소 `부분 실패`(데이터 일부 확보) 이상 상태가 됨 → 타겟분석 탭 🔒 → ○ 전환(§6-1).
2. 탭을 열면 AI가 그 즉시 `recommended_target`을 생성해 보여줌 — 시장분석의 검색량 추이·계절성·시장 이슈, 브랜드분석의 자사·경쟁사 브랜드 검색량·관심 키워드·메시지를 근거로 사용(§7-2). 이미 확보된 캐시 데이터만 사용하므로 추가 수집·비용이 발생하지 않음.
3. 사용자는 추천 타겟을 **[확정]** 하거나 **[수정]** 후 확정. 확정된 값이 `confirmed_target`으로 저장됨.
4. `confirmed_target`이 저장된 이후에만 **[타겟분석 시작하기]** 버튼이 활성화되고, 이 실행이 §6-1의 실제 `function_run`(세그먼트 검색량·관심 키워드·AI Persona 수집, 비용·소요시간 발생)이 됨.
5. 경쟁사 데이터가 일부만 있어도(예: 경쟁사 전원 실패, 자사만 성공) 게이트 조건(시장+브랜드 각각 부분 실패 이상)만 충족하면 타겟 추천은 진행됩니다 — "모든 데이터가 완벽해야 한다"는 조건은 두지 않습니다(§6 "데이터 없음과 수집 실패는 다르다" 원칙과 동일 정신).

이 플로우는 §6-1(게이트 정의)·§7-2(필드 스키마)·§16-2(①-a 잠금 화면·②③ 상태 화면)·§16-7(User Flow)과 1:1로 대응합니다.

---

## 10. 상세 기능 명세 & 예상 비용

| 대구분 | 세부 내용 | 구현 방법 | 예상 비용 (1회 실행 기준) |
| --- | --- | --- | --- |
| **시장 조사** | 카테고리별 네이버 검색량 추이 분석 (미입력 시 Gemini가 카테고리 자동 분류) | 네이버 DataLab API + Gemini 2.5 Flash | **무료** (일 한도 내, §21-8) |
| **시장 조사** | 해당 카테고리의 1년/3개월/1개월 주요 뉴스 기사 스크랩 (제목, 링크, 요약) | 네이버 뉴스 API / Playwright + Gemini | **무료** |
| **시장 조사** | 메조미디어 등 주요 미디어 최신 인사이트 리포트 수집 및 주요 내용 요약 | Playwright PDF 자동 다운로드 + Gemini Multimodal | 약 **$0.02** |
| **타겟 조사** | 시장분석+브랜드분석 결과를 근거로 한 타겟 추천, 사용자 확정 후 세그먼트별 검색량 추이 비교 (★ 15차 개정 — 카테고리 추측이 아닌 게이트 통과 후 데이터 기반 추천, §7-2) | Gemini 타겟 추천 합성 + 네이버 검색 API 세그먼트 필터링 | 약 **$0.01** |
| **자사·경쟁사 분석** | 브랜드명 표기 변형 합산 검색량 — 상대추이 + 최근 30일 절대치 + 성별/연령(가능한 경우), 자사+경쟁사 동일 호출로 함께 조회(자사 앵커 고정) | 네이버 DataLab `keywordGroups`(§21-8) + 네이버 검색광고 키워드도구(§21-10) | **무료** (양쪽 다 조회 자체는 무료, 계정 등록만 필요) |
| **자사·경쟁사 분석** | 브랜드 홈페이지/대표 상세페이지 크롤링 → 주요 타겟·메시지 정리 (§7-3 `brand_website_facts`/`brand_website_target_message`) | Playwright 크롤링 + Gemini API | 약 **$0.02** |
| **자사·경쟁사 분석** | 브랜드명 언급 뉴스 스크랩 — 최근 1년/3개월/1개월 (§7-3 `brand_news`) | 네이버 뉴스 API | **무료** |
| **자사 분석** | 유튜브 채널 구독자·영상 수·주요 콘텐츠 정리 | YouTube Data API(공식, 무료 쿼터 1만 유닛/일) + Gemini API | **무료** (쿼터 내) |
| **자사 분석** | 인스타그램 프로필 팔로워·포스팅 수·최근 게시물 캡션 수집 | Apify Instagram 프로필/게시물 스크래퍼 액터 (Meta Ads Library와 동일 통합 패턴, §4) + Gemini API | 약 **$0.02** |
| **자사 분석** | 네이버 대표키워드+일반 키워드(★ 신규, §9) SA(파워링크) 노출 순위/소재 캡처 및 브랜드검색 캡처+랜딩 링크 분석+새로고침 반복 로테이션 체크 (§7-3 `naver_brand_search`/`naver_sa_ranking`) | Playwright(공개 검색결과 페이지, ★ Residential Proxy 미사용 — §21-1) | **무료** (차단 시 캡처 실패로 처리, 재시도 비용 없음. 일반 키워드 추가로 페이지 로드 수 증가 → §21-7 순차 처리 소요시간에 반영 필요) |
| **자사 분석** | 메타/구글 소재 저장, OCR/키프레임 추출, USP 파싱 및 UTM 구조 분석 | Apify Scraper + ffmpeg + Gemini Multimodal Vision API | 약 **$0.15** (Apify + AI) |
| **경쟁사 분석** | 경쟁사(4~5개) 입력 또는 미입력 시 자동 추정 후 **자사와 완전히 동일한 §7-3 필드셋**으로 전수 분석 (웹사이트·뉴스·SNS·네이버·Meta/Google 광고 모두 포함) | 경쟁사 N개 루프 처리 (Apify + Playwright + Gemini) | 약 **$0.25** |
| **데이터 패키징** | 대화형 HTML 리포트, 정량 Excel 파일, 이미지/영상 Keyframe ZIP 자동 압축 패키징 | Jinja2 (HTML Template) + openpyxl + shutil | **무료** |
| **이력 관리** | 최근 N개 분석 결과(HTML, Excel, ZIP) 저장 및 대시보드 내 재다운로드 제공 | SQLite DB + Streamlit State / Local Storage | **무료** |
| **합계** | **1회 실행 시 종합 예상 비용** | - | **약 $0.45 (약 610원)** — 아래 산정 기준 가정 하의 추정치 (브랜드 홈페이지 크롤링 비용 $0.02 반영) |

### 비용 산정 기준 (가정)

```
1회 분석 비용 = Apify 실행 비용 + Gemini input/output 비용 + 기타 API 비용
```

* **기준 가정**: 자사 1 + 경쟁사 3, 소재 최대 50개, 영상 최대 10개. 이 가정을 벗어나면(예: 경쟁사 5개·소재 100개) 비용은 비례해서 증가합니다.
* 위 표의 값은 이 가정 하의 **추정치**이며, Phase 1 구현 후 실측값으로 반드시 갱신해야 합니다(§22).
* **운영 정책**: History에 분석 건별 비용을 단일 숫자가 아니라 아래 구조로 저장합니다 — breakdown을 남겨야 나중에 어느 API가 비용을 견인하는지 추적할 수 있습니다.

```json
{
  "estimated_cost": 0.43,
  "actual_cost": 0.51,
  "cost_breakdown": {
    "apify": 0.21,
    "gemini": 0.27,
    "other_api": 0.03
  },
  "cache_summary": {
    "raw_hit": 18, "raw_miss": 4,
    "creative_hit": 36, "creative_miss": 6,
    "ai_hit": 38, "ai_miss": 4
  },
  "cache_saving_estimate": 0.33
}
```

* Apify는 API 응답만으로 정확한 과금을 계산하기 어려운 경우가 있습니다 — 이런 항목은 `actual_cost` 대신 **`estimated_actual_cost`**(Apify 콘솔의 compute-unit 단가 기준 추정치)로 표시해도 됩니다. Gemini처럼 응답에 `usage` 필드가 있는 API만 정확한 `actual_cost` 계산이 가능합니다.
* `cache_summary`/`cache_saving_estimate`(★ 14차 신설, §12-6)는 §12 캐시 정책의 히트/미스 결과를 그대로 담습니다 — **최초 실행(캐시 미스 위주)은 이 예상 비용(약 $0.45)에 가깝고, 같은 세션·인접 기간 재실행(캐시 히트 위주)은 이보다 상당히 낮아집니다.** 또한 9~13차에서 추가된 필드(계절성·소구포인트 태깅·SOV·AI Persona 등)는 이 $0.45 추정치에 아직 반영되지 않은 항목이라, Phase 1 실측 시 "최초 실행 상한"과 "캐시 적중 시 하한"을 함께 갱신해야 합니다(§22).

### 비용 경고 기준 (★ "예상 비용"과 "상한"은 다른 개념)

* **예상 비용**(약 $0.45)은 §10 비용 산정 기준 가정 하의 추정치일 뿐, 그 자체가 상한이 아닙니다.
* **MVP 1회 실행 비용 경고 기준: $1.00.** 실제 비용(`actual_cost` 또는 `estimated_actual_cost`)이 이 기준을 초과하면:
  - 분석 자체는 **계속 진행**합니다 (중단하지 않음 — 이미 Apify/Gemini 작업이 시작된 뒤 끊는 것은 비용만 날리고 결과도 못 얻는 최악의 선택).
  - History에 "비용 초과 경고" 배지를 표시합니다.
* 실제 비용이 이 기준을 지속적으로 초과하면(예: 최근 5건 중 3건 이상) §10 비용 산정 기준의 가정(자사 1+경쟁사 3, 소재 최대 50개 등) 자체를 재조정합니다.

### 목표 실행 시간 재산정

* 최초 구상("5분 이내")은 Apify actor 실행 로직(§4 레퍼런스 코드 기준 최대 15분 대기)과 Gemini 순차 분석 소요 시간을 고려하면 비현실적입니다.
* §21-7 동시성 정책(병렬 vs 순차)을 적용하면, 위 비용 산정 기준(자사 1 + 경쟁사 3) 기준 **목표 15~20분 이내** 완료가 현실적인 목표입니다.
* 이 수치는 Phase 1 구현 후 실측하여 §22 로드맵에 반영·조정합니다.

---

## 11. 오류 처리 정책 (스크래핑/API 실패)

> ★ 각 오류가 "시스템적으로 어떻게 처리되는지"만으로는 부족하고, "사용자에게 무엇이 보이는지"까지 정의해야 개발자가 UI/로직을 동시에 구현할 수 있습니다. 아래 값(재시도 횟수·backoff)은 초안이며 Phase 1 구현 중 실측 후 조정합니다.

| 오류 | 시스템 처리 | 사용자 표시 | Retry / Timeout |
|---|---|---|---|
| 네이버 캡차 감지 | 해당 수집 즉시 중단, 재시도 없음 (§21-1) | "네이버 검색 캡처 실패" | retry 0회 |
| 네이버 API quota 초과 | 해당 모듈 중단 | "검색량 데이터 수집 실패" | retry 0회, 익일 재시도 안내 |
| Meta/Instagram Apify 실행 실패 | N회 재시도 후 실패 처리 | "Meta 광고 수집 실패" / "Instagram 수집 실패" | retry 2회, timeout 15분(actor 기존 관례) |
| Gemini 429 (RPM 초과) | exponential backoff | "AI 분석 대기 중" | retry 3회, 5·10·20초 backoff |
| Gemini 일일 quota 초과 | 미완료 건 상태만 저장, 이후 재실행 시 자동 재시도 | "일부 소재 AI 분석 미완료" | retry 0회(즉시), 다음날 재실행 시 자동 |
| YouTube API 실패 | 해당 영역만 skip | "YouTube 데이터 없음" | retry 1회 |
| 네트워크 오류 (일시적) | N회 재시도 | "일시적 오류 — 재시도 중" | retry 3회, 2·4·8초 backoff |
| 사용자 입력 오류 | 실행 전 차단 (validation) | 입력창 하단 에러 메시지 | - |

이 표의 결과는 §6 "전체 실패 vs 부분 성공" 판정 로직의 입력값입니다 — 위 오류들은 전부 **부분 실패**로만 처리되며, 그 자체로 전체 실패를 유발하지 않습니다.

---

## 12. 캐시 및 재실행 정책 (★ 14차 개정 — 3단계 캐시 구조로 확장)

> ★ **핵심 원칙: `function_run`과 데이터 수집을 1:1로 묶지 않습니다.** 5개 기능은 독립 실행되지만, 한 번 수집한 원본 데이터는 다른 기능이나 재실행에서 최대한 재사용합니다. 같은 브랜드를 하루에 여러 번 분석하면 같은 광고를 또 Apify로 가져오고, 같은 이미지를 Gemini로 또 분석하는 비용 낭비가 발생합니다 — 특히 비용이 큰 Apify 실행과 Gemini 멀티모달 분석을 중복 실행하지 않는 것이 비용 최적화의 핵심입니다. §6의 "15~20분 목표"에도 직결됩니다.

### 12-1. 캐시를 3단계로 분리

| 단계 | 저장 대상 | 재사용 기준 | 비용 효과 |
|---|---|---|---|
| **Raw Data Cache** | API 응답, Meta 광고 메타데이터, 웹페이지 추출 텍스트, 뉴스 원문 메타데이터 | 동일 `source + query/input + 수집조건` | 외부 API/스크래핑 재호출 방지 |
| **Creative Cache** | 광고 이미지·영상의 다운로드 파일 및 SHA-256 hash | 동일 `asset_hash` | 동일 소재 재다운로드 방지 |
| **AI Analysis Cache** | 이미지/영상별 Gemini OCR·USP·appeal 분석 JSON | 동일 `asset_hash + model + prompt_version` | Gemini 호출 비용 방지 |

### 12-2. 기능 실행과 캐시의 관계

* `function_run`은 **사용자가 탭에서 "OO분석 시작하기"를 누른 실행 이력**이고(§6-1·§16-2), 캐시는 **이미 확보한 데이터의 재사용 단위**입니다.
* 따라서 같은 세션에서 "브랜드분석" → "소재분석"을 연속 실행하더라도, 앞선 실행에서 확보한 브랜드 정보·광고 Page 정보·광고 소재 등은 가능한 범위에서 재사용합니다.
* 동일 브랜드를 다른 날짜에 다시 분석해도 **캐시 유효기간 내 데이터는 재수집하지 않고 재사용**합니다.
* "종합분석"은 원칙적으로 신규 수집을 하지 않고, 다른 4개 기능의 최신 결과를 조합합니다(§7-4). 따라서 종합분석 실행 자체의 외부 API/스크래핑 비용은 최소화됩니다.
* ★ "타겟분석"의 게이트 조건(§6-1, 시장분석+브랜드분석 완료)은 그 두 기능의 **최신 캐시 결과**로도 충족됩니다 — 게이트 통과를 위해 시장분석·브랜드분석을 매번 새로 실행할 필요는 없습니다.

### 12-3. 소스별 기본 TTL

> 아래 TTL은 초기값이며 Phase 1 실측 후 조정합니다. **최종 목적은 "정확도가 필요한 데이터는 짧게, 변화가 느린 데이터는 길게" 보관하는 것**입니다.

| 데이터 | 기본 TTL | 비고 |
|---|---:|---|
| Meta 활성 광고 목록/메타데이터 | **24시간** | 일간 경쟁사 모니터링을 고려 |
| 광고 이미지/영상 원본 | **7일** | 동일 asset hash가 있으면 재다운로드하지 않음 |
| Gemini 소재 분석 결과(소구포인트 태깅 포함) | **30일** | `asset_hash + prompt_version`이 같으면 재사용 |
| 브랜드 홈페이지 추출 데이터 | **7일** | 변경 가능성이 있으므로 비교적 짧게 |
| Instagram 프로필/게시물 | **24시간** | 최신성 우선 |
| YouTube 채널/콘텐츠 메타데이터 | **7일** | 공식 API 쿼터 절약 |
| Naver DataLab 검색량(계절성 포함) | **24시간** | 동일 기간/키워드 조건이면 재사용 |
| Naver 뉴스 검색 결과 | **24시간** | 신규 기사 반영 |
| Naver SA/브랜드검색 캡처(대표+일반 키워드) | **24시간** | 당일 재실행 시 재사용 |
| 참고자료 출처 매핑(§7-1-b) | **영구** | 코드 내 고정 매핑이므로 API 호출 없음 |
| AI 종합/타겟 추천 인사이트 | **입력 데이터가 동일한 동안 재사용** | `input_fingerprint + source_snapshot` 기준(종합분석·타겟분석의 AI Persona 포함) |

### 12-4. 캐시 키 설계

캐시 키는 단순히 브랜드명만 사용하지 않고 **실제 결과를 바꾸는 입력값을 모두 포함**합니다.

```text
raw_cache_key =
source + brand_slug + query/URL + country
+ date_range + collection_options + keyword_set_hash

creative_cache_key =
source_platform + ad_id + asset_hash

ai_cache_key =
asset_hash + model + prompt_version
```

* 자사/경쟁사 여부(`is_own`)는 원본 데이터 자체가 같다면 Raw Data Cache의 키에 포함하지 않고, 결과를 조립할 때 메타데이터로 붙입니다.
* 프롬프트가 변경되면 `prompt_version`을 변경해 과거 AI 결과와 섞이지 않도록 합니다.
* 브랜드 키워드 변형(§7-9), 경쟁사 목록, 대표키워드/일반키워드(§7-3 `naver_sa_ranking`) 등 분석 입력이 바뀌면 `input_fingerprint`도 변경되어 필요한 항목만 새로 수집합니다.

### 12-5. 강제 새로고침도 "전체 삭제"가 아닌 "범위 지정"

기존의 `[강제 새로고침]`은 전체 데이터를 무조건 재수집하는 방식으로 구현하지 않습니다.

* `[이 데이터만 새로 수집]` — 선택한 소스의 캐시만 무효화 (§16-2 완료 탭 화면, 소스별 항목 옆에 배치)
* `[이 기능 새로고침]` — 해당 기능에 필요한 캐시만 무효화 (§16-6 History의 `[재실행]`이 이 동작을 그대로 수행)
* `[전체 새로고침]` — 사용자가 명시적으로 선택한 경우에만 모든 관련 캐시 무효화

이를 통해 단순히 소재분석을 다시 실행하려는 경우 시장 검색량·브랜드 뉴스·홈페이지 데이터까지 다시 호출하는 비용 낭비를 방지합니다.

### 12-6. 캐시 히트/미스와 비용 기록

모든 `function_run`에는 §10 비용 구조에 통합된 `cache_summary`/`cache_saving_estimate`(§10 JSON 예시 참고)를 저장합니다.

* 실제 과금 정보를 제공하는 API는 실제 비용을 사용하고, Apify처럼 정확한 과금 산출이 어려운 경우 `estimated_actual_cost`를 사용합니다(§10).
* History에서는 **총 비용뿐 아니라 캐시 절감액/캐시 적중률도 확인 가능**하게 합니다.
* Phase 1에서 실제 실행 데이터를 모아 `평균 1회 기능 실행 비용`과 `캐시 적중 시 비용`을 재산정합니다(§22).

### 12-7. 비용 절감 우선순위

1. **Gemini 중복 분석 방지** — 동일 소재 hash는 AI 재호출 금지
2. **Apify 중복 실행 방지** — 동일 브랜드/Page + 동일 조건의 활성 광고 수집은 TTL 내 재사용
3. **기능 간 데이터 공유** — 한 기능에서 받은 원본 데이터를 다른 기능이 재사용
4. **파생 데이터 재계산** — `search_seasonality`·`appeal_distribution`·SOV·`positioning` 등은 원본 데이터에서 로컬 계산하고 API 재호출 금지
5. **종합분석 무수집 원칙** — 기존 4개 기능 결과만 조합(§7-4)
6. **선택 기능은 기본 OFF** — Instagram/YouTube/뉴스/네이버 캡처 등은 사용자가 필요한 경우에만 실행(§7-0)

### 12-8. 부분 실패 재실행도 "미완료 항목만"

* Gemini quota 초과, 네트워크 오류 등으로 일부 소재만 분석되지 않은 경우 **완료된 소재는 다시 분석하지 않습니다**(§11과 동일 원칙의 캐시 계층 적용).
* 다음 실행에서는 `status != completed`인 항목과 캐시 만료 항목만 대상으로 재시도합니다.
* 사용자가 기능 전체를 다시 실행해도 캐시가 유효한 항목은 자동으로 건너뜁니다.

### 12-9. 캐시와 History/결과 파일은 별도 관리

* **캐시**: 비용 절감을 위한 재사용 데이터. 결과 파일이 삭제되어도 캐시가 살아 있으면 리포트를 저비용으로 재생성할 수 있습니다.
* **History**: 누가/언제/어떤 기능을 실행했는지에 대한 실행 이력(§6-1 `analysis_session`/`function_run`).
* **Export 파일**: 사용자가 다운로드하는 HTML/Excel/ZIP(§16-5).
* 따라서 Export 파일 LRU 삭제(§21-3)와 캐시 만료는 서로 독립적으로 관리합니다.

### 12-10. 비용 최적화 데이터 흐름 요약

> ★ 개발 시 §12-1~12-9의 관계를 한눈에 보기 위한 요약 다이어그램입니다.

```text
[사용자 입력] → [analysis_session] → [기능 실행: function_run]
      ↓
┌───────────────────────────────────────┐
│ 먼저 Cache Lookup                     │
│ ① Raw Data Cache                      │
│ ② Creative Cache (asset hash)         │
│ ③ AI Analysis Cache (hash+prompt ver.)│
└───────────────────────────────────────┘
      ↓ 캐시 MISS만
[API / Apify / Playwright 수집] → [원본 데이터 저장]
      ↓
[Gemini 분석 — 미분석 소재만] → [기능별 결과 생성] → [HTML / Excel(/ZIP)]
```

**비용 최적화 핵심 규칙**:
* **수집과 분석을 분리**합니다. 같은 원본 데이터가 있으면 다시 수집하지 않고, 같은 소재가 있으면 다시 Gemini에 보내지 않습니다.
* **원본 → 분석 → 리포트**의 3단계 결과를 각각 재사용할 수 있어야 합니다.
* `function_run`을 다시 실행해도 캐시 유효 항목은 외부 호출하지 않습니다.
* 파생 지표(`search_seasonality`, `appeal_distribution`, SOV, `positioning` 등)는 저장된 원본 데이터로 계산합니다.
* 프롬프트 변경 시에만 `prompt_version`을 변경해 필요한 AI 분석만 재실행합니다.
* 캐시는 결과 파일보다 오래 살아 있을 수 있으며, 결과 파일이 삭제되어도 캐시를 이용해 저비용 재생성이 가능해야 합니다.

---

## 13. 데이터 수집일 / 출처 메타데이터 원칙

> ★ "경쟁사 A는 할인 메시지를 사용하고 있다"는 문장은 **언제 수집된 광고인지** 없이는 신뢰할 수 없습니다. 월간 모니터링(§2 사용 시나리오 3번)에서 "지난달 vs 이번 달" 비교를 하려면 처음부터 시간축이 있어야 합니다.

모든 수집 레코드는 최소 다음 4개 필드를 필수로 저장합니다:

| 필드 | 설명 |
|---|---|
| `collected_at` | 이 도구가 실제로 수집한 일시 |
| `source` | 플랫폼/API 이름 (예: `meta_ad_library`, `naver_datalab`) |
| `source_url` | 원본 URL (가능한 경우) |
| `published_at` | 원본 콘텐츠의 실제 게시일 (가능한 경우, 없으면 null) |

이 필드들은 §14 Excel 스키마와 `database/db.py` 테이블 정의에 그대로 반영됩니다.

---

## 14. Excel 산출물 데이터 구조

### 14-1. Sheet 구성 (★ 9차 개정 — 모듈형 5기능 구조 반영, §7-11과 1:1 대응)

> ★ 세션에서 실행하지 않은 기능의 시트는 빈 시트(또는 "미실행"으로 표기)로 남습니다 — 실패가 아니라 §6-1의 "부분 실행"이 정상 상태이기 때문입니다.

```
01_Overview                    — 세션 요약 (실행한 기능·상태·비용)

[시장분석하기]
02_Market_Trend
03_Market_Seasonality          — 신규
04_Market_References           — 신규

[타겟분석하기 — 시장분석+브랜드분석 완료 후 잠금 해제, 이후 추천→확정→분석 2단계로 진행 (§6-1·§7-2, ★13차+15차)]
05_Target
05b_AI_Persona                 — 신규 (성향/선호상황/전환 트리거, §7-2-a)

[브랜드분석하기 — 자사+경쟁사 통합, §9-15]
06_Brand_Profile                — 기존 04_OwnBrand+05_Competitors 병합
07_Naver_Operation              — 신규 (대표키워드+일반키워드 SA 모니터링)
08_Media_Matrix                 — 신규

[소재분석하기 — 자사+경쟁사 통합, DA 전용]
09_Ads                          — 기존 06_Ads + ad_delivery_start_time/ad_running_days 컬럼 추가
10_CreativeAnalysis             — appeal_tags 컬럼 추가
11_Appeal_Distribution          — 신규
12_LongRunning_Analysis         — 신규

[공통 — 브랜드분석/시장분석에서 공유]
13_News                         — scope 컬럼(market/brand)으로 구분
14_UTM

[종합분석하기]
15_SOV                          — 신규
16_Positioning                  — 신규
17_AI_Insights                  — 기존 10_AI_Insights에서 번호 이동
```

### 14-2. Canonical Ad Schema (`09_Ads` 시트 기준)

| 컬럼 | 설명 |
|---|---|
| `brand` | 브랜드명 |
| `platform` | 수집 출처 — `meta` / `google`. **`instagram`은 이 컬럼의 값이 아님**(§7-11-0) — 인스타그램에 노출된 광고도 `platform=meta`로 수집되고, 노출 위치는 아래 `publisher_platforms`로 구분 |
| `publisher_platforms` | (★ 신규) 광고가 노출된 지면 — 예: `facebook,instagram` (Meta Ads Library 응답 그대로, §7-11-0) |
| `ad_id` | 소재 고유 ID |
| `format` | `image` / `video` / `carousel` |
| `headline` | 메인 카피 |
| `body` | 본문 카피 |
| `cta` | CTA 문구 |
| `landing_url` | 랜딩 URL |
| `is_active` | 항상 `true` — 활성 광고만 수집하는 정책(§7-3) 반영, 컬럼으로도 명시해 후속 분석에서 혼동 방지 |
| `ad_delivery_start_time` | (★ 신규) 게재 시작일 — Phase 0 검증 결과에 따라 `unavailable`일 수 있음 |
| `ad_running_days` | (★ 신규) 오늘 기준 운영 일수 |
| `collected_at` | 수집 일시 (§13) |

`17_AI_Insights` 시트는 §8의 `insight`/`source`/`evidence`/`confidence` 4개 컬럼을 그대로 사용합니다.

`06_Brand_Profile` 시트는 §7-3 브랜드 프로필 필드셋(brand_search_volume·brand_website_facts·brand_news·naver_brand_search 등)을 자사+경쟁사 **하나의 시트**에 `is_own` 컬럼으로 구분해 담습니다(9차 개정 이전엔 04_OwnBrand/05_Competitors로 시트가 분리돼 있었으나, §7-3 스키마가 이미 완전히 동일해 시트 분리의 실익이 없어 병합). `13_News` 시트는 `scope` 컬럼(`market` / `brand`)으로 시장 카테고리 뉴스와 브랜드 언급 뉴스를 구분해 한 시트에 함께 담습니다. `10_CreativeAnalysis`/`11_Appeal_Distribution`/`12_LongRunning_Analysis`의 상세 컬럼은 §7-11-4 참고.

---

## 15. 기술 스택

* **Language/Framework**: Python 3.11+, Streamlit
* **AI Engine**: Google GenAI SDK (`google-genai` - Gemini 2.5 / 3.5 Flash)
* **Scraping & Automation**: Apify SDK (`ad_library.py`: Meta Ads Library + **Instagram 프로필/게시물** — §21-7 병렬 처리 / `google_ads.py`: Google Ads Transparency Center, **Phase 0 Go 판정 시에만 구현**, §21-6), YouTube Data API(공식), Playwright (**네이버 SERP/SA 캡처 전용** — 프록시 없이 시작·순차 실행, §21-1·§21-7), `ffmpeg` (영상 키프레임 추출)
* **Data & API**: 네이버 Search & DataLab API, `asyncio`
* **Exporters & Database**: `Jinja2` (HTML 리포트), `openpyxl` (Excel), `shutil` (ZIP 패키징), `SQLite` (이력 관리, §18 한계 참고)
* **(보류) Residential Proxy**: Bright Data / Oxylabs 등 — §21-1의 도입 트리거 충족 시에만 추가 검토. 2026-09-06 기준 예산·계정 미확보

---

## 16. 화면 구성 (UI Layout)

> ★ **12차 개정 — "입력→진행→결과→다운로드" 4화면 순차 모델을 "STEP 1 브랜드 설정 → STEP 2 분석 Workspace(5개 지속 탭)" 모델로 전면 재구성**합니다. 5개 기능이 독립 실행되고 순서 무관하며 통합 다운로드가 없다는 §5·§6-1·§10차 개정의 원칙은 전부 그대로 유지되며, 이번 개정은 그 원칙을 화면에 어떻게 구현할지를 구체화한 것입니다.
>
> **최종 UI 레퍼런스**: `adetect_reference/templates_sample/ui_ref.png`(§4) — 다크+골드 톤앤매너와 좌상단 브랜드설정/우상단 진행/하단좌측 결과/하단우측 상세의 4분할 레이아웃은 그대로 1차 기준으로 유지합니다. 다만 "하단좌측 결과" 영역의 **역할**이 바뀝니다 — "5개 기능을 한 번에 분석한 최종 결과"가 아니라 "Workspace의 5개 기능 현재 상태를 보여주는 작은 Overview 스트립"으로 재정의합니다(§16-4). 아래 ASCII 목업은 구조 설명용 보조 자료입니다.

### 16-1. STEP 1 — 브랜드 설정 (Brand Setup)

* **입력 폼** (★ 15차 개정 — 타겟 입력 필드 제거): 자사 브랜드명(필수), 산업 카테고리(선택), 경쟁사 브랜드명(선택, 개수 고정 없음) — **입력은 이 화면에서 1회만, 이후 STEP 2의 5개 기능 탭이 공통으로 사용. 타겟 입력 필드는 이 화면에 두지 않으며**, 타겟은 시장분석+브랜드분석 완료 후 타겟분석 탭에서 추천·확정됩니다(§7-2·§9-2).
* **자동 추천 확인 단계**: 미입력 항목은 §9 플로우대로 AI 추천 → 사용자가 확인/수정/확정 (일반 키워드 후보, 광고 페이지 후보, 대표키워드 확인 전부 이 화면에서 끝남, §9)
* **★ 이 화면에는 "전체 분석 시작하기"류의 버튼을 두지 않습니다.** 확인이 끝나면 아직 아무 기능도 실행되지 않은 상태로 바로 STEP 2(분석 Workspace)에 진입합니다.
* 화면 하단 CTA: `[수정]  [확정하고 Workspace로 이동]` (§9 플로우 4번 참고)

### 16-2. STEP 2 — 분석 Workspace

Workspace 상단에 **5개 1차 탭**을 항상 고정 노출합니다. 탭 = §6-1의 기능(function) 1개씩과 1:1 대응이며, 세션이 유지되는 한 화면 전환 없이 같은 화면 안에서 탭만 전환됩니다.

```
[ 시장분석 ○ ]  [ 타겟분석 🔒 ]  [ 브랜드분석 ○ ]  [ 소재분석 ○ ]  [ 종합분석 🔒 ]
```
★ 13차 개정 — 세션을 처음 열면 **타겟분석과 종합분석 2개 탭이 🔒 상태로 시작**합니다(둘 다 게이트형, §6-1). 시장분석·브랜드분석·소재분석 3개는 처음부터 바로 실행 가능합니다.

**탭 상태 아이콘** (기존 9차 개정의 ✓/△/✕/− 표기를 아래로 확정 대체):

| 아이콘 | 의미 |
|---|---|
| ○ | 미실행 (실패 아님 — 아직 "OO분석 시작하기"를 누르지 않은 정상 상태) |
| ● | 진행 중 |
| ✓ | 완료 |
| △ | 부분 실패 |
| ✕ | 전체 실패 |
| 취소 | 취소됨 |
| 🔒 | 실행 불가(게이트형 탭 전용, §6-1) — **종합분석**: 다른 4개 기능 중 최소 1개가 `완료`/`부분 실패` 상태가 될 때까지 잠금 / **타겟분석**(★ 13차 신규): 시장분석과 브랜드분석이 **각각** `완료`/`부분 실패` 상태가 될 때까지 잠금 |

사용자는 탭을 자유롭게 오가며, 한 기능이 끝나지 않아도 다른 탭에서 다른 기능을 실행할 수 있습니다. 단 §18의 "전사 동시 실행 1건" 정책은 그대로 적용됩니다 — 어떤 사용자가 어느 탭에서든 기능을 실행 중이면 다른 사용자의 새 실행 요청은 대기열에 들어갑니다.

#### 탭의 상태별 화면 (모두 같은 탭 안에서 전환 — 별도 화면으로 이동하지 않음)

**① 미실행(○)** — 그 기능이 무엇을 제공하는지 요약과 시작 버튼만 보여줍니다. 선택 수집 체크박스(§7-0)도 여기서 노출합니다.

```
[ 브랜드분석 ○ ]

이 기능은 다음을 분석합니다
· 브랜드 검색량         · 홈페이지 분석
· 브랜드 뉴스           · SNS 프로필
· 네이버 SA/브랜드검색   · 매체 운영 현황

선택 수집: ☐ Instagram  ☐ YouTube  ☐ 네이버 SA/브랜드검색  ☐ 뉴스

[ 브랜드분석 시작하기 ]
```
(요약 항목은 §7-11의 해당 기능 산출물 목록을 그대로 사용 — §16-3 매핑표 참고)

**①-a 잠금(🔒)** (★ 13차 신규 — 종합분석·타겟분석 공통) — "OO분석 시작하기" 버튼 대신 잠금 사유와 필요한 조건을 보여줍니다. 조건이 충족되는 즉시(다른 탭이 완료되는 즉시) 자동으로 🔒 → ○로 전환됩니다.

```
[ 타겟분석 🔒 ]

이 기능은 시장분석과 브랜드분석 결과를 근거로
핵심 타겟을 추천합니다.

잠금 해제 조건 (2/2 필요)
✕ 시장분석 완료 필요        → [시장분석 탭으로 이동]
✓ 브랜드분석 완료됨
```

★ 15차 개정 — 타겟분석 탭은 🔒 → ○ 전환 즉시(추가 클릭 없이) `recommended_target`을 자동으로 보여주고 [확정]/[수정] 버튼을 노출합니다. 사용자가 확정해야만 이 탭 안에 "타겟분석 시작하기" 버튼이 나타나며, 그 버튼을 눌러야 비로소 아래 "② 진행 중" 상태로 넘어갑니다(§7-2·§9-2).

**② 진행 중(●)** — 기존의 "실시간 분석 진행" 화면이 별도 화면이 아니라 **그 탭 안에** 그대로 나타납니다.
```
[ 소재분석 ● ]

✓ Meta Ads Library 활성 광고 수집 (42건, FB+IG 노출 포함)
● Gemini 소구포인트 태깅 28/42
○ 운영기간·장기소재 분석
○ 리포트 반영

예상 소요시간: 약 4분 남음   예상 비용: $0.15
[ 이 기능 취소 ]
```
취소 시 동작(★ 반드시 구현, §6 "분석 취소의 범위"와 동일):
1. 이미 수집한 데이터는 그대로 저장 (버리지 않음)
2. 진행 중이던 API 호출은 중단 (신규 호출 차단, 이미 발송된 요청은 응답을 기다렸다가 폐기)
3. 취소 시점까지의 결과를 **부분 결과**로 표시 (§6 "취소된 실행은 History 상태를 취소로 기록하며 부분 결과로 표시" 원칙 그대로)
4. 취소 시점까지 실제 발생한 비용을 History에 기록 (§10 "실제 비용" 필드에 반영)

**③ 완료(✓/△)** — 결과가 같은 탭 안에서 바로 보이고, 그 아래 이 기능만의 다운로드 버튼이 나타납니다.
```
[ 브랜드분석 ✓ ]

[개요] [브랜드 검색량] [홈페이지 분석] [광고 운영] [SNS 분석] [AI 인사이트]  ← 2차 세부탭, §16-3

(선택된 세부탭의 결과 표/차트/인사이트가 여기 표시됨)

[ HTML 다운로드 ]  [ Excel 다운로드 ]
[ 이 데이터만 새로 수집 ▾ ]  [ 이 기능 새로고침 ]   ← ★ 14차 신설, §12-5 범위 지정 캐시 무효화
```
FACT / AI INTERPRETATION / RECOMMENDATION 3색 배지(§8)는 결과 화면 어디서든 모든 인사이트 옆에 동일하게 표시됩니다. "이 데이터만 새로 수집"은 드롭다운으로 그 기능 안의 개별 소스(예: 브랜드분석의 뉴스만, SNS만)를 선택해 캐시를 부분 무효화하고, "이 기능 새로고침"은 §16-6 History의 `[재실행]`과 동일하게 그 기능 전체 캐시를 무효화합니다(§12-5). 세션 전체를 한 번에 무효화하는 `[전체 새로고침]`은 개별 탭이 아니라 세션 설정 메뉴에 둡니다.

### 16-3. 1차 탭 vs 2차 세부탭 (★ 반드시 구분)

**1차 탭(5개) = §6-1의 `function_run` 단위**이며, 실행·재실행·상태·비용·다운로드가 전부 이 단위로 관리됩니다. **2차 세부탭은 그 결과를 탐색하기 위한 화면 구성일 뿐, 별도의 `function_run`이 아닙니다** — 세부탭 이동은 새 실행을 만들지 않고, 이미 캐시된 결과를 다르게 보여줄 뿐입니다.

| 1차 탭 | 2차 세부탭 | §7-11 대응 |
|---|---|---|
| 시장분석 | 개요 · 검색량 추이 · 계절성 · 시장 뉴스 · 참고자료 | §7-11 (1) |
| 타겟분석 (★ 게이트형 + 추천/분석 2단계, §6-1·§7-2) | 개요(추천 타겟+확정 상태) · 확정 타겟 · 검색량 · 관심 키워드 · AI Persona(성향/선호상황/전환 트리거 + 고정 disclaimer, §7-2-a) | §7-11 (2) |
| 브랜드분석 | 개요 · 브랜드 검색량 · 홈페이지 분석 · 광고 운영(SA/매체매트릭스) · SNS 분석 · AI 인사이트 | §7-11 (3) |
| 소재분석 | 개요 · 소재 목록 · Creative Analysis · Appeal Distribution · Long Running | §7-11 (4) |
| 종합분석 | Overview · SOV · Positioning · White Space · AI Summary | §7-11 (5) |

각 탭의 "개요/Overview" 세부탭에는 §7-11의 "최종 인사이트"(`one_line` 헤드라인 + 펼침 상세)를 배치합니다.

### 16-4. Workspace 상태 스트립 (구 "결과 요약 대시보드"를 축소·재정의)

기존엔 별도 화면이었던 "결과 요약 대시보드"를 ★ 12차 개정으로 **Workspace 탭 바로 위의 작은 상태 스트립**으로 바꿉니다 — "5개 기능을 한 번에 분석한 결과"처럼 보이지 않도록, 상태 확인과 탭 이동 보조 역할만 하고 그 자체로 다운로드나 결과 상세를 담지 않습니다.

```
메리츠화재 · 손해보험 · 타겟 미확정     [시장 ✓] [타겟 ○] [브랜드 ✓] [소재 ○] [종합 🔒]
```
(★ 15차 개정) 세션 생성 직후에는 타겟이 아직 없으므로 "타겟 미확정"으로 표시되고, 타겟분석 탭에서 `confirmed_target`이 저장되는 순간 이 문자열이 그 값(예: "40~50대 남성")으로 갱신됩니다.

`종합분석`이 완료되면 이 스트립 아래에 `one_line_summary` 한 줄 배너가 추가됩니다(`AI Recommendation · Confidence: {level}` 캡션 병기, §8) — 종합분석 탭 안에서 보이는 것과 동일한 값을 요약 노출할 뿐 별도 데이터가 아닙니다. 이 화면의 디자인 원칙은 §17 참고(광고 소재 이미지 중심, 유리효과 최소화).

### 16-5. 다운로드 정책

| 기능 | 다운로드 |
|---|---|
| 시장분석 | HTML / Excel |
| 타겟분석 | HTML / Excel |
| 브랜드분석 | HTML / Excel |
| 소재분석 | HTML / Excel / ZIP (이미지·영상 원본) |
| 종합분석 | HTML / Excel |

각 탭이 완료되면 **그 탭 안에서만** 다운로드 버튼이 나타납니다(§16-2 ③). 독립 실행형 HTML의 외부 공유 가능 여부는 §20 별도 기준을 따릅니다. ★ **5개 기능을 한 번에 묶어 내려받는 "통합 다운로드" 버튼은 두지 않습니다**(10차 개정 원칙 그대로 유지) — 필요한 기능의 결과만 그때그때 받는 구조입니다.

### 16-6. History (세션 목록)

Workspace와는 별도의 진입점입니다 — "다운로드 목록"이 아니라 **예전 Workspace로 돌아가는 입구**입니다.

* 최근 N개(예: 10개) **분석 세션** 목록 (생성 일시, 브랜드명, 카테고리, 실행된 기능 수 "3/5", 세션 누적 예상/실제 비용, 비용 경고 배지 — §10). 이 행 자체에는 다운로드 버튼을 두지 않습니다(세션 통합 다운로드 없음, §16-5와 동일 원칙).
* 세션 행을 클릭하면 그 세션의 **Workspace로 진입**하고, 5개 탭이 각자의 마지막 상태(✓/△/✕/취소/○)로 복원됩니다. 실제 다운로드는 복원된 탭 안에서 §16-5 정책대로 이루어집니다.
* 실제 파일이 LRU로 삭제된 기능은 그 탭의 다운로드 버튼이 `파일 만료`로 비활성, 대신 `[재실행]` 제공 (§21-3, 기능 단위 재실행 — 세션 전체를 다시 돌릴 필요 없음)
* 다른 사용자가 이미 분석 중이면 상단에 대기 안내 표시: `현재 분석 중인 작업이 있습니다. 예상 대기 시간: 약 N분` (§18)

### 16-7. 최종 User Flow (요약)

```
브랜드 입력 (STEP 1)
  ↓
AI 추천 (카테고리·경쟁사·대표키워드·일반키워드·광고페이지 — 타겟은 이 단계에서 추천하지 않음, §9-2)
  ↓
사용자 확인/수정 → 확정
  ↓
분석 Workspace 진입 (STEP 2) — 시장/브랜드/소재 3개 탭은 ○, 타겟·종합 2개 탭은 🔒로 시작 (★ 13차)
  ↓
사용자가 시장분석 또는 브랜드분석 탭 선택 → [OO분석 시작하기] (순서 무관, 둘 다 언제든 실행 가능)
  ↓
그 탭 안에서 진행상태 표시(●) → 완료(✓/△/✕)
  ↓
같은 탭 안에서 2차 세부탭으로 결과 탐색 → 이 기능만 HTML/Excel(/ZIP) 다운로드
  ↓
(시장분석 AND 브랜드분석이 각각 완료/부분실패가 되면) 타겟분석 탭 🔒 → ○ 자동 전환
  ↓
타겟분석 탭 진입 → AI가 시장·브랜드 결과를 근거로 핵심 타겟을 자동 추천(`recommended_target`, 무료·즉시)
  ↓
사용자가 [확정] 또는 [수정 후 확정] → `confirmed_target` 저장
  ↓
[타겟분석 시작하기] → 확정된 타겟 기준 세그먼트 검색량·관심 키워드·성향/선호상황/전환 트리거(§7-2-a) 실제 분석 실행
  ↓
소재분석은 위 흐름과 무관하게 언제든 별도 실행 가능
  ↓
(시장/타겟/브랜드/소재 4개 기능 중 1개 이상 완료/부분실패 시) 종합분석 탭 🔒 → ○ 자동 전환 → 실행 가능
```

---

## 17. 디자인 가이드 (e-doa.co.kr 레퍼런스 스타일)

복잡한 잔기능이나 자잘한 UI 요소 대신, 레퍼런스 웹사이트(`e-doa.co.kr`)의 **크고 웅장하며 압도적인 시각적 분위기**를 최우선으로 반영합니다.

* **디자인 톤앤매너 (Vibe & Concept)**
* **Majestic & Cinematic Dark**: 다크 모드 기반의 고대비 럭셔리 스페이스 레이아웃 (`#08080A` ~ `#111115`)
* **웅장한 타이포그래피 (Oversized & Bold Typography)**: 대형 헤드라인과 굵은 폰트 사용으로 압도적인 인상 전달
* **최소화된 요소, 극대화된 여백 (Minimalist & Spacious)**: 자잘한 구획 선을 줄이고 넓은 패딩과 선명한 카드로 시각적 웅장함 확보


* **주요 UI 스타일링 규칙**
* **Primary Color**: Obsidian Dark (`#0B0C10`), Pure Gold/Bronze Accent (`#D4AF37` / `#C5A059`)
* **Background & Cards**: 유리 질감의 다크 글래스모피즘 (`background: rgba(20, 20, 25, 0.85); backdrop-filter: blur(12px)`)
* **CTA 버튼 스타일**: 크고 굵은 타이틀과 미세한 빛 발광(Glow) 효과가 들어간 웅장한 아웃라인/솔리드 버튼
* **FACT/AI/REC 배지 컬러**(§8·§16): FACT는 중립 톤(예: 실버/그레이), AI INTERPRETATION은 브랜드 골드 톤, RECOMMENDATION은 별도 강조색(예: 앰버) — 다크 배경에서도 명확히 구분되도록 대비비 확보
* **Glassmorphism 사용 범위 제한** (★ 추가): 모든 카드에 유리효과(`backdrop-filter: blur`)를 적용하지 않습니다. 상단 요약 배너(§16-4 상태 스트립) 등 일부에만 제한적으로 쓰고, **광고 소재 이미지가 들어가는 카드(§16-2 소재분석 탭 완료 화면)는 유리효과 없이 이미지 자체를 크게 노출**하는 것을 우선합니다. 목표 비율은 검정 70% + 큰 텍스트 + 금색 포인트 + 데이터 카드 + 광고 이미지 — 유리효과를 과용하면 일반적인 AI SaaS 대시보드 톤으로 흐를 위험이 있습니다.
* **레퍼런스 사이트 구조 참고**: e-doa.co.kr은 단순히 다크+골드 배색뿐 아니라 INSIGHT→STRATEGY→CREATIVE→EXECUTION→PERFORMANCE처럼 **큰 단계 구성**이 특징입니다. §16-2 Workspace의 5개 기능 탭(시장분석/타겟분석/브랜드분석/소재분석/종합분석)도 이런 큰 단계형 구성을 참고해 설계했습니다.

---

## 18. 멀티유저 동시 실행 정책

> ★ "사내 공유 웹 서비스"인데 여러 명이 동시에 무거운 분석을 돌리면, API quota를 서로 나눠 써서 한쪽 분석이 실패할 수 있습니다(§21-9). 이 정책은 MVP 단계의 구체적 결정입니다.

* **MVP 동시 실행 정책**: **전사 기준 동시 분석 1건만 허용.** 여기서 "1건"은 §6-1의 **기능 실행(function_run) 1건**을 의미합니다(★ 모듈형 기능 구조 반영, UI는 §16 Workspace 탭). 즉 어떤 사용자가 Workspace의 "소재분석" 탭을 실행 중이면, 다른 사용자는 (같은 브랜드든 다른 브랜드든) 5개 탭 중 아무거나 실행해도 대기열에 들어갑니다. 다른 사용자가 이미 분석 중이면 새 분석 요청은 대기열에 들어감.
  - 이후 실사용 패턴을 보고 "사용자별 1건 / 전체 최대 2건" 등으로 완화 검토(🟢 우선순위 낮음, §22).
* **대기 UI**: `현재 분석 중인 작업이 있습니다. 예상 대기 시간: 약 N분` (§16)
* **구현 방식(MVP)**: 정교한 작업 큐가 아니라 **DB 상태 플래그 또는 in-process 락(Lock)** 수준으로 시작 — Phase 4 범위(§22). 고도화된 큐(Celery/RQ 등)는 §5 선택 기능으로 후순위.
* **SQLite 한계 명시**: SQLite는 **MVP 및 단일 인스턴스 운영을 전제**로 합니다. 동시 쓰기(분석 결과 저장)와 동시 읽기(History 조회)가 빈번해지면 병목이 발생할 수 있습니다. **멀티유저 동시 실행이 실사용에서 빈번해지면 PostgreSQL 등 외부 DB로 전환을 검토**합니다(🟢 우선순위, §22 Phase 5 이후).

---

## 19. 인증 / 보안 / 데이터 보관 정책

> ★ 결과물에는 광고주 정보, 경쟁사 정보, 광고 소재, 내부 분석 결과, API 데이터가 담깁니다. 아래 항목은 Phase 4(§22) 착수 전 확정이 필요합니다.

### 19-1. 접근 정책

* 사내 계정만 접근 (SSO 또는 최소 사내 네트워크/IP 제한 — 구체 방식은 Phase 4 착수 전 확정 필요, 현재 미결정)
* 외부 공개 금지 (공인 도메인/포트 개방 금지)

### 19-2. 시크릿 관리

* API Key는 `.env` 또는 Secret Manager에서만 관리 — 코드/커밋/로그에 절대 노출 금지
* **로그에 API Key 출력 금지** — 로깅 유틸에서 마스킹 필수 (`adetect_reference/lib/env_loader.py` 이식 시 이 원칙 유지)
* **HTML/ZIP 결과물에 API Key 포함 금지** — exporter가 산출물 생성 직전 자동으로 키 패턴을 스캔해, 발견 시 빌드를 실패시키는 가드를 둘 것을 권장

### 19-3. 데이터 보관

* 분석 결과 저장 기간: §21-3 LRU 정책(최근 N개 + 용량 상한)과 연동. 기본 보관 기간은 **미결정 — Phase 4 착수 전 확정 필요** (가안: 90일)
* 사용자별 결과 접근 범위: MVP는 **전사 공용**(모든 사용자가 모든 History 조회 가능)으로 시작, 필요 시 사용자별 격리로 확장(🟢 우선순위, §22)
* 다운로드 URL: 로그인 세션 내에서만 유효하도록 하고, 직링크 공유 시 만료되는 서명 URL 방식을 권장 (공개 URL 상시 노출 금지)
* 서버 저장 파일 자동 삭제: §21-3 LRU 정책과 동일 기준 적용

---

## 20. 데이터 이용 조건 / 저작권 / 약관 검토 (★ 법무 확인 필요)

> ★ 이 챕터는 **실제 서비스 오픈 전 법무팀 확인이 필요한 항목 체크리스트**이며, PRD가 법적 결론을 내리는 문서가 아닙니다. §21-1(Residential Proxy)에서 다룬 네이버 ToS 이슈 외에도, 아래 항목들이 실제로는 더 넓은 범위의 검토 대상입니다.

| 데이터 | 수집 방법 | 저장 | 외부 공유 | 확인 필요 사항 |
|---|---|---|---|---|
| Meta 광고 소재 | Apify | O | △ | Meta Ads Library 이용약관 |
| Instagram 공개 데이터 | Apify | O | △ | Instagram 이용약관 |
| 뉴스 기사 | 공식 API | O | △ | 저작권 (본문 전문 저장은 지양, 요약+링크 권장) |
| 미디어 인사이트 리포트 (PDF) | Playwright 다운로드 | O | X/△ | 저작권 (재배포 금지가 일반적) |
| 광고 소재 ZIP 재배포 | Scraping/API | O | △ | 각 플랫폼 이용 조건 |
| 네이버 검색결과 캡처 | Playwright (직접) | O | △ | 네이버 이용약관 (§21-1 참고) |

**△(외부 공유 조건부) 항목에 대한 원칙**:
* 본 PRD는 "독립 실행형 HTML"이라는 **기술적 특성**(오프라인에서도 열림, §21-4)만 보장하며, "외부 공유 가능"이라는 **법적 판단**은 별도입니다.
* 경쟁사 소재·내부 분석 결과가 그대로 포함된 리포트를 사내 참고용을 벗어나 외부(예: 광고주에게 그대로 전달)로 공유하려면, 위 표 기준으로 건별 확인이 필요합니다.
* §16의 다운로드 버튼 문구도 이 원칙에 맞춰 "외부 공유 가능"이라는 표현 대신 "독립 실행형 HTML"로 수정했습니다.

---

## 21. 제약사항 및 시스템 고려사항

### 1. 스크래핑 차단 방지 — Residential Proxy 도입은 1차 MVP에서 보류

* **Residential Proxy란**: 가정용 회선에서 통신사가 발급한 실제 IP를 경유하는 프록시(Bright Data·Oxylabs 등). 데이터센터 IP보다 "일반 사용자처럼" 보여 차단 확률이 낮지만, 트래픽 GB당 과금이라 비용이 발생하고 애초에 탐지 회피 목적이라 대상 사이트 ToS와 충돌 소지가 있음.
* 네이버 SA/브랜드검색 캡처는 **로그인이 필요 없는 공개 검색결과 페이지(`search.naver.com`)** 를 대상으로 하며, `oliveyoung_crawler.py`가 다루던 로그인 세션 필요 내부 회원 API(비인가 직접 호출 시 403)와는 차단 강도가 다름.
* 1차 MVP는 **Residential Proxy 없이** 진행. 실사용 패턴(리포트 1건당 브랜드 1회 검색, 저빈도)이라면 즉시 차단될 위험은 낮다고 판단.
* 설계 원칙:
  - 캡차/인증 페이지 감지 시 **즉시 포기하고 "캡처 실패 — 수동 확인 필요"로 리포트에 표시**. 재시도 폭주 금지(반복 재시도가 오히려 IP를 태우는 원인).
  - 브랜드당 1회 순차 처리, 동시 병렬 요청 금지 (§21-7).
  - 클라우드 서버 IP보다 로컬/사무실 네트워크에서 실행 시 차단 위험이 낮음 — 사내 서버 배포(§22 Phase 5) 시점에 재검토.
* **Proxy 도입 트리거**: 실사용 중 캡처 실패율이 지속적으로 높게 관측되면(예: 30% 이상) 그 시점에 예산·계정을 확보해 도입 검토. 2026-09-06 기준 예산·계정 모두 미확보 상태.

### 2. AI API 비용 및 토큰 최적화

* Gemini 비전 파이프라인 입력 시, 고해상도 전체 영상 대신 `ffmpeg` 기반 키프레임(초당 1장, 영상당 최대 6장) 샘플링 추출 적용.
* 무료 티어 일일 한도(RPD) 초과 가능성이 실제로 높음(기존 `fetch_competitor_ads.py`에 이미 `GeminiQuotaExceeded` 전용 예외 처리가 있는 것이 그 방증) — 브랜드당 자사+경쟁사 4~5개를 한 번에 돌리면 이미지/영상 분석 호출이 수십~백 건 단위. 한도 초과 시 부분 결과 저장 후 graceful 종료, 다음 실행 시 미완료 건만 재시도(§11).

### 3. 로컬/서버 저장소 용량 관리 (LRU Cache)

* `storage/exports/` 및 `storage/temp/` 저장소의 용량 폭증을 막기 위해 **최근 N개(예: 최근 20개)** 이력만 유지하고 오래된 ZIP 자산은 자동 삭제.
* N은 개수 기준 외에 **총 용량 상한(예: 5GB)** 도 함께 두는 것을 권장 — 영상 키프레임·원본 이미지가 누적되면 개수보다 용량이 먼저 문제가 될 수 있음.
* **★ History 메타데이터와 실제 파일의 보관 정책은 분리** — §5/§6/§16의 "재다운로드"가 이 LRU 삭제와 충돌하지 않도록:
  - `database/db.py`의 **History 메타데이터**(분석 일시·브랜드명·상태·비용 등, §13)는 이 LRU 정책과 무관하게 별도 보관 기간(§19-3)까지 유지됩니다.
  - **HTML/Excel/ZIP 실제 파일**만 이 LRU·용량 상한 기준으로 삭제됩니다.
  - 실제 파일이 삭제된 History 항목은 `[재다운로드]` 버튼을 **비활성화**하고 `파일 만료`로 표시합니다 — 메타데이터는 남아있지만 다운로드는 불가능한 상태.
  - 이 경우 사용자가 다시 보고 싶으면 **`[재분석]`** 버튼으로 동일 조건 재실행을 제공합니다(§12 캐시 정책 적용 — 24시간 이내면 대부분 캐시 히트로 빠르게 재생성).

### 4. 결과 파일의 오프라인 독립성

* 최종 산출물인 `report.html`은 인라인 CSS/JS 및 Base64/로컬 상대경로 구조로 빌드하여 Python 환경이나 사내망이 아닌 곳(집, 모바일, 미팅)에서도 정상 동작.
* 구현 참고: `02_competitor/_scripts/build_dashboard.py`의 `window.DATA` 인라인 주입 방식(§4 참고).
* ★ "오프라인에서도 열린다"는 기술적 특성과 "외부에 공유해도 된다"는 법적 판단은 별개입니다 — §20 참고.

### 5. 아키텍처 성격 차이 (신규 인지 사항)

* 현재 `Claudecode_MarketingOS_student` 워크스페이스는 "Claude Code 슬래시 스킬이 그때그때 스크립트를 실행하고 결과를 마크다운/정적 HTML로 남기는" 구조이며, ADetect는 "상시 구동되는 독립 Streamlit 서버"입니다.
* 즉 §4의 레퍼런스 파일들에서 **Python 로직(스크래핑·분석·리포트 생성 함수)만 이식**하는 것이며, `.claude/skills/`의 스킬 자체(슬래시 커맨드 호출 구조)는 재사용 대상이 아닙니다.

### 6. Google Ads Library 지원 범위 미확정

* PRD 제목상 "Meta/Google Ads Library"이나, §4의 기존 레퍼런스 코드는 전부 Meta(Apify `facebook-ads-library-scraper`)만 지원. Google Ads Transparency Center는 별도 Apify 액터 또는 자체 개발이 필요 — §22 Phase 0 스파이크에서 가능 여부 확인 필요.
* **UI 반영 원칙**: 미확정 상태에서 UI가 앞서가지 않도록, §16 수집 옵션에는 이 항목을 "검증 중" 상태로 노출하되 **Phase 0에서 Go 판정이 나기 전까지는 체크 자체가 불가능하도록 비활성화**합니다. Go 판정 후에만 선택 가능한 옵션으로 전환.
* **코드 구조 반영 원칙**: UI뿐 아니라 코드 구조에서도 분리합니다 — Meta Ads Library+Instagram 프로필 수집은 `core/scrapers/ad_library.py` 하나로, Google Ads Transparency Center는 별도 파일 `core/scrapers/google_ads.py`로 둡니다. **★ (10차 개정) `google_ads.py`는 Phase 0 Go 판정이 나기 전까지 §3 파일 목록에 아예 올리지 않습니다** — "만들지는 않지만 트리에는 이름만 있는" 애매한 상태(빈 파일/플레이스홀더)도 두지 않습니다. §3에는 "[MVP 파일 목록에 없음]"으로 괄호 표기해 코드가 없다는 사실 자체를 명시합니다. 한 파일에 섞어두거나 빈 파일을 미리 만들어두면 둘 다 "Google까지 이미 구현됐다"는 오독을 유발할 수 있습니다.

### 7. 동시성 정책 — 병렬 처리와 순차 처리를 작업 성격별로 분리

> 원칙: **"우리 자체 IP로 직접 브라우저를 열어 페이지를 조작하는 행위"만 순차·저빈도로 제한**하고, 나머지(Apify에 위임한 작업, 공식 API 호출)는 각자의 문서화된 한도 안에서 최대한 병렬화한다.

| 작업 | 실행 방식 | 이유 |
|---|---|---|
| Meta Ads Library 수집 (Apify) | **병렬** (브랜드별 actor run 동시 실행) | Apify가 자체 프록시·큐로 스크래핑을 대행 — 동시성 부담이 우리가 아니라 Apify 인프라 쪽에 있음 |
| Instagram 프로필/게시물 수집 (Apify) | **병렬** | 위와 동일한 이유 |
| YouTube Data API 조회 | **병렬** (쿼터 내) | 공식 API, 문서화된 쿼터(1만 유닛/일) 안에서는 동시 호출 허용 |
| 네이버 DataLab·뉴스 검색 API | **병렬** (문서화된 초당·일일 호출 한도 내) | 공식 API — 한도를 넘기지 않는 선에서 병렬 허용 |
| 네이버 SA/브랜드검색 캡처 (Playwright, 자체 실행) | **순차** (브랜드당 1개씩 + 요청 간 지연) | 우리 IP에서 직접 브라우저 세션을 여는 유일한 항목 — 동시에 여러 세션을 열면 짧은 시간에 몰린 트래픽처럼 보여 차단 확률 상승 (§21-1) |
| Gemini 이미지/영상 분석 | **제한된 병렬** (동시 요청 수 상한 + 429 시 backoff) | 모델별 RPM(분당 요청 수) 한도가 있어 무제한 병렬은 quota 소진·429 폭증으로 이어짐 |

### 8. 네이버 오픈 API(DataLab·뉴스) 등록 및 쿼터 관리 (유의사항)

* "무료"이지만 사용 전 네이버 개발자센터(developers.naver.com)에 애플리케이션을 등록해 Client ID/Secret을 발급받아야 함.
* 검색 API와 DataLab API는 **각각 별도의 일일 호출 한도**를 가지며, DataLab 쪽이 더 빡빡함. 브랜드·카테고리 조회가 누적되면 한도 도달 가능성 있음.
* `brand_search_volume`(§7-9)에 쓰이는 **통합검색어트렌드 API는 요청 1건당 담을 수 있는 키워드 그룹 수·그룹당 키워드 수에 상한**이 있음 — Phase 1 구현 시 최신 공식 문서로 재확인 필요. 경쟁사 수가 많아 한도를 넘으면 분할 호출 로직이 필요.
* **미결정 사항**: 이 키를 누가 발급·관리하고, 한도 도달 시 알림을 누가 받을지 사전에 정해야 함(개인 계정 발급 시 담당자 퇴사·계정 이전 리스크도 고려).

### 9. 조직 공용 API 키의 동시성 경합 (유의사항)

* "사내 공유 서비스"인데 Apify·Gemini·네이버 API 키가 조직 전체 공용이면, 한 사람이 무거운 분석(경쟁사 4~5개 전수)을 돌리는 동안 다른 사람의 요청이 같은 quota를 나눠 쓰게 됨 — 한쪽이 quota를 소진하면 다른 사용자의 분석이 실패할 수 있음.
* → **MVP 정책은 §18(멀티유저 동시 실행 정책)에서 확정**: 전사 동시 분석 1건 제한으로 이 경합 자체를 원천 차단. 이후 사용 패턴을 보고 완화 여부 결정.

### 10. 네이버 검색광고(키워드도구) API — 별도 계정·인증 필요 (신규 유의사항)

* `brand_search_volume`(§7-9)의 "최근 30일 절대 검색량"은 DataLab이 아니라 **네이버 검색광고(광고주센터) 키워드도구 API**를 사용합니다 — §21-8의 DataLab/뉴스 API와는 **발급 경로 자체가 다릅니다.**
* 필요한 것: 네이버 검색광고 광고주 계정, API License(License Key + Secret Key), Customer ID. 요청마다 HMAC 서명이 필요해 DataLab보다 연동이 한 단계 더 복잡합니다.
* 이 계정은 원래 실제 광고 집행용으로 만들어진 것이라, 광고를 집행하지 않고 "키워드도구 조회"만 쓰더라도 계정 자체는 개설해야 합니다.
* **미결정 사항**: §21-8과 마찬가지로 이 계정을 누가 개설·관리할지 Phase 0.5(§22)에서 함께 확정.

---

## 22. 개발 로드맵 (Phase)

| Phase | 내용 | 산출물 | 비고 |
|---|---|---|---|
| **0. 스파이크** | (a) 네이버 검색 → 브랜드검색/파워링크 노출 여부 판별 + 캡처를 프록시 없이 시도, 차단률 체감 확인 (b) Google Ads Transparency 크롤 가능 여부 확인 (c) Apify Instagram 스크래퍼 액터 후보 검증 (d) **네이버 검색광고 키워드도구 API 계정 발급·인증·호출 검증 + 브랜드 표기 변형 합산이 실제로 가능한지 확인** (§7-9·§21-10) (e) **★ 신규 — Meta Ad Library API의 `ad_delivery_start_time` 필드가 한국向 일반 상업광고에도 안정적으로 오는지 검증** (§7-3 `ad_delivery_start_time`) | Go/No-go 메모 — `brand_search_volume`은 하위 요소별로 개별 판정: ① DataLab 상대 추이(위험 낮음, 기존 패턴과 유사) → 사실상 GO ② 검색광고 키워드도구 절대 검색량(별도 계정·HMAC 인증, 신규 리스크) → **GO/NO-GO 판정 필요** ③ 성별/연령 비중(정규화 문제로 % 환산 불확실) → 실측 후 "확정 지표" 또는 "참고용 지표로 다운그레이드" 결정 ④ `ad_delivery_start_time` → 안정적이면 GO, 불안정하면 §7-3 규정대로 선택 필드로 하향 | 리스크가 가장 큰 항목들을 먼저 검증. `brand_search_volume`은 PRD상 리스크가 상당한 신규 기능인데 기존엔 검증 대상에서 빠져 있었음 — 이번에 추가 |
| **0.5. 정책 확정** (신규) | §18(멀티유저 동시 실행 수), §19(접근 정책·보관 기간), §10(비용 상한), §20(법무 확인 요청 발송) 등 미결정 항목을 코드 작성 전에 확정 | 정책 확정 메모 | 코드부터 짜고 나중에 정책을 끼워맞추면 재작업 발생 — 반드시 Phase 1 이전에 |
| **1. 코어 리팩터링** | `02_competitor`+`ad_monitor`의 Apify+Gemini 로직을 `core/scrapers/ad_library.py`, `core/analyzers/gemini_vision.py`로 통합·승격. §8 FACT/AI 스키마 적용. `config/settings.py`(.env 로딩) 통합. **★ 신규 — `ad_library.py`에 활성 광고 필터링(`is_active` 강제) + `ad_delivery_start_time`/`ad_running_days` 계산 로직 추가** | 독립 실행 가능한 자사/경쟁사 광고 수집+분석 모듈 | 이미 검증된 코드 재사용 — 가장 빠르고 안전한 착수점. 여기서 §10 비용·시간 실측치 확보 |
| **2. 신규 스크래퍼** | `core/scrapers/naver_serp.py`(프록시 없는 Playwright, 브랜드검색+SA 감지·캡처·로테이션 체크, **대표키워드+일반키워드 다중 대상**, 순차·graceful fail, §21-7), `naver_api.py`(DataLab 검색량+성별/연령 필터+**일별 원본 보존(계절성 후처리용, §7-1-a)**, 뉴스, 검색광고 키워드도구, §21-8·§21-10 등록 필요), `brand_site.py`(신규, 브랜드 홈페이지 크롤링, §7-3), SNS 프로필 수집 — 유튜브는 YouTube Data API(공식), **인스타그램은 자체 Playwright 크롤러 대신 Apify Instagram 스크래퍼 액터로 대체**(§4, `ad_library.py`와 동일 통합 패턴 재사용) | 시장·자사·경쟁사 조사 원본 데이터 | Phase 0 결과 반영. Instagram은 신규 개발이 아니라 기존 Apify 통합 패턴 확장이라 리스크 낮음 |
| **3. Exporters** | `html_builder.py`(Jinja2, `period_report.py` 패턴 이식 + 오프라인 독립 HTML, **기능별 실행 결과만 채워도 정상 렌더링되도록**), `excel_builder.py`(openpyxl, §14 17개 시트 구조), `packager.py`(zip, 신규 — 소재분석 실행 시에만 트리거) | 리포트 3종 생성 파이프라인 | Excel/Zip은 단순 작업이라 빠르게 완성 가능 |
| **4. Streamlit 셸 + History** (★ 모듈형 기능 구조로 재설계, 12차 개정으로 Workspace+탭 UI 확정) | `app.py`(STEP 1 입력폼→§9 자동추천 확인(일반키워드 후보 포함)→STEP 2 **분석 Workspace의 5개 탭**(§16-2)→탭별 진행상태(취소 포함, §16-2)→탭별 다운로드(§16-5)), `database/db.py`(SQLite, `analysis_session`/`function_run` 2단 테이블 구조로 변경, §6-1·§13 메타데이터 포함, §18 동시 실행 락은 기능 실행 단위로 적용) | **PRD 풀스펙 로컬 MVP** (Residential Proxy만 제외) — §6 Acceptance Criteria 전체 충족 | 여기까지 완료 시 "브랜드명 넣고 Workspace에서 필요한 탭만 눌러 리포트 받는" 실사용 가능 상태 |
| **5. 배포** (보류) | Dockerfile/docker-compose, §19 인증 방식 확정 적용, (필요시) PostgreSQL 전환(§18) | 사내 서버 배포 | 로컬 MVP로 가치 검증 후 착수. Residential Proxy 재검토도 이 시점 |

---

## 23. 문서화 규칙 (레퍼런스 워크스페이스 컨벤션 계승)

`Claudecode_MarketingOS_student` 워크스페이스의 문서화 관례를 그대로 따릅니다 — **폴더를 처음 보는 사람도 무엇을 하는 폴더인지 즉시 알 수 있어야 합니다.**

* **루트 `README.md`**: 5분 안에 설치→실행까지 되는 퀵스타트. 명령어는 그대로 복붙 가능해야 함 (참고: 원 워크스페이스 루트 `README.md`의 "수강생 시작 가이드" 스타일).
* **루트 `SETUP_GUIDE.md`**: API 키 발급처(Apify, Gemini, 네이버 개발자센터 등)별 단계별 안내 + "왜 이 방식을 택했는지" 배경 설명(예: 왜 프록시를 안 쓰는지, 왜 User Access Token 방식인지). 이 문서를 **single source of truth**로 두고, 다른 문서·스크립트가 이 내용을 복붙하지 않고 그때그때 참조.
* **`docs/REFERENCE.md`**: 본 PRD §4 표를 그대로 옮겨 유지 — 어떤 로직이 어느 레거시 파일에서 이식됐는지 추적 가능하게. 레거시 파일이 바뀌면 이 문서도 갱신.
* **모듈별 짧은 `README.md`** (`core/scrapers/`, `core/analyzers/`, `core/exporters/`, `database/`, `config/` 등): 해당 폴더의 입력·출력·핵심 함수를 1페이지로 요약. 풀스펙 API 문서는 지양 — 상세 스펙은 함수 docstring, README는 "이 폴더가 하는 일" 요약만.
* **주석 원칙**: "무엇"이 아니라 "왜"만 남김 (예: quota 예외 처리·재시도 로직처럼 코드만 봐서는 이유를 알 수 없는 부분에만 주석 — 나머지는 코드 자체로 설명).
* **★ TECH_SPEC.md 분리 권장 (미실행)**: 이 PRD는 §3(폴더구조)·§4(레퍼런스)·§14(Excel 스키마)·§15(기술스택) 등 구현 세부사항과 §1~§2·§6~§9(목적·성공기준·요구사항) 같은 제품 요구사항이 한 문서에 섞여 있습니다. 1인 또는 소규모 개발 단계에서는 지금처럼 단일 문서로 유지하되, **개발팀이 커지거나 비개발 이해관계자(기획·법무·경영진)와 공유할 일이 잦아지면 구현 세부사항을 `TECH_SPEC.md`로 분리**하는 것을 권장합니다. 분리 시 권장 구조: PRD(목적·사용자·문제·핵심기능·Flow·입출력·성공기준·예외처리·정책) / TECH_SPEC.md(Architecture·Directory·API·Scraper·Gemini·DB·Queue·Deployment·Environment·Monitoring).

---

## 24. 개정 이력

| 날짜 | 변경 내용 |
|---|---|
| 2026-09-06 | 최초 검토 논의 반영: (1) §4 기존 워크스페이스 레퍼런스 소스 매핑 추가 (2) §21-1 Residential Proxy를 1차 MVP에서 보류하고 근거·트리거 조건 명시 (3) §22 개발 로드맵(Phase 0~5) 추가 (4) §23 문서화 규칙(README/SETUP_GUIDE/REFERENCE.md/모듈별 README) 추가 (5) §3 디렉터리 구조에 `docs/REFERENCE.md` 및 모듈별 `README.md` 반영 (6) 예상 비용에서 프록시 비용 제거 |
| 2026-09-06 (2차) | 레퍼런스 번들 `adetect_reference/` 생성 및 §4 표에 번들 내 실제 경로 컬럼 추가. 리스크 재검토 반영: (1) Instagram 크롤링을 자체 Playwright 대신 **Apify Instagram 스크래퍼 액터**로 전환, YouTube는 공식 API로 명확화 (2) §21-7 동시성 정책(병렬 vs 순차 실행 기준) 신설 (3) §21-8 네이버 오픈 API 등록·쿼터 관리 유의사항 신설 (4) §21-9 조직 공용 API 키 동시성 경합 유의사항 신설 |
| 2026-09-06 (3차) | 외부 리뷰 반영 — 전면 재구성 및 8개 신규 섹션 추가: (1) §6 MVP 완료 기준(Acceptance Criteria) + 전체실패/부분성공 정의 (2) §7 분석 결과 Output Schema (시장/타겟/자사/경쟁사/종합) (3) §8 AI 판단 vs 사실 데이터 구분(FACT/AI/REC + source·evidence·confidence 스키마) (4) §9 입력값 자동추천 & 사용자 확인 플로우 (5) §11 오류 처리 정책 표(재시도·backoff 포함) (6) §12 캐시 및 재실행 정책 (7) §13 데이터 수집일/출처 메타데이터 원칙 (8) §14 Excel 산출물 데이터 구조 (9) §18 멀티유저 동시 실행 정책(SQLite 한계·전환 조건 포함) (10) §19 인증/보안/데이터 보관 정책 (11) §20 데이터 이용조건/저작권/법무 검토. 그 외: §2·§10에 목표 실행 시간 현실화(5분→15~20분), §10 비용 산정 기준 근거 추가, §16 취소 기능 UI 및 HTML 외부공유 문구 정정, §22 로드맵에 Phase 0.5(정책 확정) 신설, §23에 TECH_SPEC.md 분리 권장 메모(미실행) 추가. 전체 섹션 번호 1~24로 재정렬 |
| 2026-09-06 (4차) | 내부 논리 충돌 정정: (1) §6·§7에 "필수의 정확한 의미"(데이터 없음=정상 vs 수집실패=부분실패) 및 상황별 처리표 추가 (2) §7-0 신설 — 핵심(항상 수집) vs 선택(체크박스) 항목을 명확히 분리하고 §7 전 테이블의 "필수" 칸을 이 기준으로 재표기, §16 수집 옵션 UI도 동일 구조로 재구성 (3) 경쟁사 §7-4의 `promotion`(FACT+AI 혼합)을 `promotion_fact`/`promotion_interpretation`으로 분리 — 3단계 분류 원칙 위반 해소 (4) §16·§21-6 Google Ads Transparency Center를 "검증 중·체크 불가" 상태로 명시해 UI가 미확정 기능을 앞서 노출하지 않도록 정정 (5) §7-2 `interest_keywords` 출처(네이버 연관검색어/검색량 API) 명시 (6) §16 한 줄 총평 배너 문구를 "AI 핵심 총평"으로 조정하고 Confidence 캡션 병기 (7) §10 History 비용 구조를 `estimated_cost`/`actual_cost`/`cost_breakdown`으로 세분화, Apify는 `estimated_actual_cost` 허용 (8) §16에 "3. 결과 요약 대시보드" 신설(다운로드 전 결과 미리보기) (9) §17 디자인 가이드에 Glassmorphism 사용 범위 제한 및 레퍼런스 사이트의 큰 단계 구성 참고 추가 |
| 2026-09-06 (5차) | 브랜드 검색량 지표 신설 — 그동안 빠져있던 "브랜드명 자체의 검색량" 항목 추가. (1) §7-3(자사)·§7-4(경쟁사)에 `brand_search_volume` 필드 추가(핵심·항상 수집) (2) §7-9(신규) 구현 노트 — 네이버 DataLab `keywordGroups`로 브랜드명 표기 변형(예: KB Pay/kbpay/케이비페이) 합산 조회, 응답이 절대 검색량이 아닌 상대 지수(0~100)임을 명시, 자사·경쟁사를 같은 API 호출에 그룹으로 함께 담아야 비교 가능함을 명시 (3) §9 자동추천 확인 화면에 "브랜드 검색 키워드" 확인 단계 추가(자사만 사용자 확인, 경쟁사는 자동) (4) §10 비용표에 `brand_search_volume` 행 추가(무료) (5) §21-8에 통합검색어트렌드 API의 그룹/키워드 수 상한 재확인 필요성 명시 |
| 2026-09-06 (6차) | 원본 기능 기획표 전수 대조 후 발견된 누락분 보강 + 브랜드 검색량 지표 확장. (1) §7-9에 브랜드 검색량 "앵커" 방식 명문화(그룹 상한 초과 시 자사를 모든 분할 호출에 공통 포함) (2) `brand_search_volume`에 최근 30일 절대 검색량(네이버 검색광고 키워드도구)·성별/연령 비중(DataLab 필터, 조건부·검증 필요) 추가 (3) §21-10 신설 — 검색광고 키워드도구 API는 DataLab과 별도 계정·HMAC 인증이 필요함을 명시 (4) **§7-3·§7-4를 "§7-3 브랜드 프로필(자사+경쟁사 공통 스키마)"로 통합** — 기존에 경쟁사 쪽에 없던 youtube/instagram/naver/google_ads, 자사 쪽에 없던 utm 등 비대칭이던 필드를 완전히 동일하게 맞춤 (5) 신규 필드 추가: `brand_website_facts`/`brand_website_target_message`(브랜드 홈페이지 크롤링, 이전엔 전혀 없었음), `brand_news`(브랜드명 언급 뉴스, 카테고리 시장 뉴스와 별도) (6) `naver_search`를 `naver_brand_search`(브랜드검색 캡처+링크+로테이션 체크)와 `naver_sa_ranking`(대표키워드 SA 순위+소재캡처)으로 세분화 — 원래 서로 다른 네이버 광고 상품인데 뭉뚱그려져 있던 것을 분리 (7) §7-1 `top_news`·§7-3 `brand_news`에 1년/3개월/1개월 3구간 집계 명시(기존엔 "TOP 10"으로만 뭉뚱그려짐) (8) §7-2 `core_target`을 "성별+나이" 조합으로, `target_search_volume`을 DataLab 인구통계 필터 메커니즘으로 명시 (9) §3에 `core/scrapers/brand_site.py` 신규 스크래퍼 추가, §10·§14·§16·§22 관련 항목 동기화, §10 합계 비용 $0.43→$0.45 (10) 사용자 제공 최종 UI 레퍼런스 이미지를 `adetect_reference/templates_sample/ui_ref.png`로 편입하고 §4·§16에서 "1차 기준"으로 명시 |
| 2026-09-06 (7차) | 최종 점검 6개 항목 반영. (1) §6에 "분석 취소의 범위" 신설(Streamlit·Apify 특성 반영: 신규 호출 차단/진행중 요청 abort/이미 시작된 외부 작업은 응답 폐기), History 상태를 완료·부분실패·전체실패 3종에서 **`취소` 포함 4종**으로 확장 (2) §6에 "4대 영역 상태 판정" 표 신설 — 영역별 핵심 데이터(예: OWN=brand_search_volume+brand_website_facts+meta_ads)를 명시해 완료/부분실패/데이터없음 판정을 코드로 바로 옮길 수 있게 함, "전체 실패" 정의 문구도 모호성 제거 (3) §3·§15·§21-6에 `ad_library.py`(Meta+Instagram)와 `google_ads.py`(Phase 0 Go 시에만 신규 생성)를 코드 구조 레벨에서 분리 (4) §10에 "비용 경고 기준 $1.00" 신설 — 예상비용(추정치)과 상한(경고 기준)을 구분하고, 초과해도 분석은 계속 진행 후 History에 경고만 표시 (5) §21-3에 History 메타데이터(계속 보관)와 실제 파일(LRU 삭제)의 보관 정책 분리 명시, 파일 삭제 시 `[재다운로드]` 비활성+`파일 만료`+`[재분석]` 버튼 제공 (6) §22 Phase 0에 네이버 검색광고 키워드도구 API 검증 추가, `brand_search_volume` 하위 요소별(상대추이/절대수치/성별연령) Go-No-Go 세분화 |
| 2026-09-06 (8차) | "올리브영"(다품목 리테일러) 입력 시나리오로 전체 플로우 드라이런 후 발견된 이슈 보강. (1) **§7-10 신설** — 브랜드명 텍스트만으로는 Meta/Instagram의 정확한 Page/계정을 특정할 수 없다는 신규 리스크를 명문화: 자사는 §9 확인 화면에서 후보 중 사용자가 선택, 경쟁사는 팔로워수+인증배지 기준 자동 선택(값은 투명 기록) (2) §9에 "경쟁사 개수는 4~5개 고정이 아님" + "추천 경쟁사 페이지를 못 찾으면 교체 안내" 원칙 추가 — 독과점적 카테고리에서 억지로 개수를 채우거나 폐업 업체를 추천하는 문제 방지 (3) §7-3 `naver_sa_ranking`에 "대표키워드"의 출처(§9에서 확정, 기본값=카테고리) 명시 — 기존엔 정의가 없었음 (4) §7-3 `brand_website_facts`에 리테일러형 브랜드를 위한 폴백 규칙 추가(대표 상세페이지 없으면 홈페이지/회사소개로 대체) (5) §7-3 `key_message`/`usp`/`creative_type`이 단일 값이 아니라 다품목 리테일러의 경우 복수 패턴 배열을 반환할 수 있음을 명시 (6) `adetect_reference/README.md`·PRD §4에 `oliveyoung_crawler.py`가 "올리브영 브랜드 분석"이 아니라 "올리브영 쇼핑몰 상품 리뷰 크롤러"임을 명확히 하는 경고 추가(이름 혼동 방지) (7) §9 자동추천 확인 목업에 "광고 페이지 확인"·"대표키워드" 블록 추가 |
| 2026-09-11 (9차) | 업그레이드 아이디어 17개 검토(외부 리서치 포함: Meta Ad Library 정치광고 외 이력 제한 확인, 네이버 브랜드검색이 CPC가 아닌 검색량 구간별 정액제 상품임을 확인, `notebooklm-mcp-cli` 오픈소스 실체 확인) 후 확정된 결정 반영. **(1) 아키텍처 전환 — 단일 파이프라인 → 5개 독립 기능 버튼(시장분석/타겟분석/브랜드분석/소재분석/종합분석)**: §5 재작성, §6-1 신설("분석 세션"/"기능 실행" 단위 정의, 종합분석 활성화 조건), §16 UI를 기능별 버튼·진행상태·배지·History로 재설계, §18 동시실행 락 단위를 기능 실행으로 재정의, §22 Phase 4 재설계. **(2) 자사·경쟁사 탭 통합**: §7-3는 이미 동일 스키마였으나 UI·Excel 시트(§14 `04_OwnBrand`+`05_Competitors` → `06_Brand_Profile` 단일 시트)까지 완전히 병합, "브랜드분석하기" 하나의 기능으로 통합. **(3) 네이버 SA 확장**: §7-3 `naver_sa_ranking`에 대표키워드 외 **일반 키워드**(카테고리 실사용 키워드) 모니터링 추가, §9 확인화면에 후보 제안 단계 추가. **운영 키워드 전체 목록 추정·브랜드검색 계약금액 추정은 신뢰할 수 있는 근사치가 불가능하다고 판단해 범위에서 제외(확정)**. **(4) Meta 소재 분석 강화**: §7-3에 "활성 광고만 수집, 우회 없음" 원칙 명문화, `ad_delivery_start_time`/`ad_running_days` 필드 신설(§22 Phase 0 검증 항목 추가), `appeal_tags`(소구포인트 다중 라벨+비중) 필드 신설, 장기운영 소재 분석(§7-11-4 `12_LongRunning_Analysis`) 추가. **(5) 소재분석 산출물 명세**: §7-11-4에 이미지/영상 ZIP + 4개 Excel 시트(Ads/CreativeAnalysis/Appeal_Distribution/LongRunning_Analysis) + 1~2줄 인사이트 포맷 구체화. **(6) 랜딩페이지 심화분석(카테고리 구조·리뷰) 범위 제외**, 대신 §7-3-c "랜딩페이지 수동 체크 팁" 고정 텍스트 블록으로 대체. **(7) 카테고리별 분석 프리셋 시스템은 1차 보류** — §5 선택기능에 명시, 공통 §7 스키마 우선 출시 후 재검토. **(8) SOV 3개 지표 신설**: §7-4에 `share_of_search`/`share_of_ads`/`share_of_voice_news` 추가(진짜 지출 기반 SOV는 불가능함을 명시). **(9) 매체-브랜드 운영 매트릭스 신설**: §7-3 `media_operation_matrix` — 네이버 SA/브랜드검색·Meta·Instagram·YouTube·(조건부)Google Ads만 포함, 카카오·당근 등 조사불가 매체는 명시적으로 "조사불가" 처리. **(10) 검색량 계절성 신설**: §7-1 `search_seasonality`(월별+평일/주말) 및 §7-1-a 구현 노트(기존 DataLab 일별 응답 후처리, 추가 비용 없음). **(11) 전문 통계자료 출처 매핑**: §7-1 `reference_sources` 및 §7-1-b 매핑 원칙(카테고리→고정 출처 테이블, 링크만 제공·본문 미수집). **(12) "예정된 변화" 검증 원칙 강화**: §7-1 `upcoming_changes`를 뉴스 원문에 날짜·공식발표가 명시된 경우로만 한정, 추측 생성 금지. **(13) 포지셔닝맵**: §7-4 `positioning_map` 신설 — 자체 HTML 리포트 내 시각화로 확정, 외부 도구 연동은 보류. **(14) NotebookLM 연동 보류**: §5에 근거와 함께 명시(자체 수집 능력 없음, 비공식 자동화 리스크 대비 이득 제한적, 도입 시 Drive 자동동기화 방식 우선 검토). §7-11(신규) — 5개 기능별 최종 산출물(Excel 시트·컬럼·인사이트 포맷) 전체 명세, §14 Excel 시트 구성을 17개로 재편(§7-11과 1:1 대응). |
| 2026-09-11 (10차) | 9차 개정에서 남아있던 구(舊) "세션 전체를 하나로 합산" 로직 정리 요청 반영. (1) **§6 "4대 영역 상태 판정" 완전 폐기 → "기능별 상태 판정"으로 대체**: 시장·타겟·자사·경쟁사 4영역 기준을 시장/타겟/브랜드/소재/종합 5개 **기능 실행(function_run)** 기준으로 재정의(핵심 데이터 표 포함), "리포트 전체 상태를 4개 영역 종합해 산출" 문장 삭제 — 세션 전체를 하나의 상태값으로 합치는 로직 자체를 폐지 (2) §6 "전체 실패 vs 부분 성공 정의"·"데이터 없음과 수집실패" 규칙을 전부 기능 실행 단위 문구로 수정 (3) Acceptance Criteria 표의 "인사이트"/"결과물" 행에서 "4개 영역 모두"/"3종 모두 생성" 표현을 "실행한 기능마다"로 수정하고, **"여러 기능을 하나로 묶는 별도의 종합 다운로드 버튼은 두지 않음"을 명문화** (4) **§16 통합 다운로드 UI 제거** — 결과 요약 대시보드의 `[HTML][EXCEL][ZIP]` 일괄 버튼 삭제(배지 클릭 시 해당 기능으로 이동하는 방식으로 대체), 다운로드 화면의 "즉시 다운로드 구역"(HTML/Excel/ZIP 통합 3버튼)을 "기능별 다운로드"로 대체, History 세션 행에서 다운로드 버튼 제거(기능별 행에만 `[재다운로드]` 유지) |
| 2026-09-11 (11차) | 9차 개정의 6개 정합성 이슈 수정. **(1) Excel 시트 번호 충돌 해소** — §7-11의 시트 번호가 §14와 1칸씩 밀려있던 문제를 §14 기준(01~17)으로 통일: `15_UTM→14_UTM`, `16_SOV→15_SOV`, `17_Positioning→16_Positioning`, `18_AI_Insights→17_AI_Insights`. **(2) `google_ads.py` 존재 여부 모순 해소** — §3 폴더 구조에서 `google_ads.py`를 트리 항목 자체에서 제외하고 "[MVP 파일 목록에 없음 — Phase 0 Go 이후 생성]"으로 괄호 표기, §21-6에 "빈 파일도 만들지 않는다"를 명문화. **(3) Meta Ads/Instagram 용어 3종 정리** — §7-11-0(신규) 신설: Meta Ads(광고, FB+IG 노출 통합, 별도의 "Instagram Ads Library"는 없음) / Instagram Profile(SNS 운영현황, 광고 아님) / "Instagram Ads라는 별도 수집원"은 불필요함을 확정. `09_Ads` 시트의 `platform` 컬럼에서 `instagram` 값을 제거하고 `publisher_platforms`(노출 지면) 컬럼 신설, `08_Media_Matrix`의 `instagram_ads` 열을 `instagram_profile`로 정정, §5·§6·§16 등 전 영역의 "Meta/Instagram" 표현을 용도에 맞게 재구분. **(4) "핵심 데이터 최소 1개=완료" 단일규칙 폐기** — §6 "기능별 상태 판정"을 완료/부분실패/전체실패 조건을 기능마다 각각 명시하는 표로 교체(특히 브랜드분석은 자사 핵심 데이터 AND 조건 + 경쟁사 OR 조건으로 정확히 재정의, 자사 데이터만 있어도 무조건 완료로 오판정되는 문제 해소). **(5) 취소 vs 부분결과 관계 명문화** — §6 분석 취소의 범위에 "취소된 실행은 History 상태를 취소로 기록하며, 취소 시점까지 데이터는 부분 결과로 표시한다"를 한 문장으로 확정. **(6) `core_target`과 사용자 입력 타겟의 관계 정리** — §7-2에 `user_target`(사용자 입력 원문) 필드 신설, `core_target`은 "AI가 데이터 기반으로 독립 판단한 값"으로 범위 재정의, 리포트에 입력 타겟과 데이터 기반 핵심 타겟을 나란히 병기하는 표시 예시 추가. |
| 2026-09-11 (12차) | UI 전면 재구성 — "입력→진행→결과→다운로드" 4화면 순차 모델을 **"STEP 1 브랜드 설정 → STEP 2 분석 Workspace(5개 지속 탭)" 모델**로 교체(§16 전체 재작성, §16-1~§16-7 신설). (1) STEP 1: 입력+§9 확인만 담당, "전체 분석 시작" 버튼 폐지, 확정 시 CTA를 `[확정하고 Workspace로 이동]`으로 변경(§9도 동일 반영) — 이 시점의 타겟 입력은 §7-2 `user_target`으로 저장됨을 명시 (2) STEP 2: 5개 기능을 화면 전환 없는 **지속 탭**으로 전환, 탭 상태 아이콘을 ○(미실행)/●(진행중)/✓/△/✕/취소/🔒(종합분석 잠금) 6종으로 확정 (3) 기존의 "진행 트래커"·"결과 요약 대시보드"·"다운로드 화면"을 별도 화면이 아니라 **각 탭 안의 미실행/진행중/완료 3가지 상태 화면**으로 흡수 (4) **1차 탭(5개, `function_run` 단위) vs 2차 세부탭(결과 탐색용, `function_run` 아님)을 명확히 구분**하고 §7-11 산출물과 1:1 매핑표 신설(§16-3) — 세부탭 이동이 새로운 실행을 유발하지 않음을 명문화 (5) 구 "결과 요약 대시보드"를 Workspace 탭 위의 작은 "상태 스트립"(§16-4)으로 축소해 "5개를 한 번에 분석한 결과"처럼 보이는 인상을 제거 (6) History를 "다운로드 목록"에서 "세션 클릭 시 Workspace를 복원해 재진입하는 입구"로 재정의(§16-6) (7) 최종 User Flow 다이어그램 신설(§16-7) (8) §5·§6-1·§7-11·§18·§22·§4의 "5개 기능 버튼"류 표현을 "5개 기능(Workspace에서는 탭으로 표현)"으로 용어 통일, §6·§17의 "§16 결과 요약 대시보드" 참조를 §16-4/§16-2로 교정. **(9) 미해결 — 타겟분석 탭의 "AI Persona" 세부탭을 시장분석·브랜드분석 완료 후에만 심화 제공할지(2단 하이브리드안)는 별도 확정 필요, 대화 진행 중** |
| 2026-09-11 (13차) | 12차의 미해결 사항(타겟분석 게이팅) 확정 — **B안(완전 게이트형)으로 결정**. (1) **§6-1에 "타겟분석 탭 활성화 조건" 신설** — 종합분석(4개 중 최소 1개)과 다르게, 타겟분석은 **시장분석 AND 브랜드분석이 각각** 최소 `부분 실패` 이상이어야 잠금 해제. 소재분석은 게이트 조건에 미포함(있으면 보조 근거로만 사용) (2) **§7-2 전면 개정** — `core_target`을 "카테고리만 보고 추측"에서 "시장분석+브랜드분석 결과를 반드시 근거로 삼는 추천"으로 범위 재정의, `insight`가 항상 "경쟁사는 OOO / 시장상황은 OOO / 그래서 우리는 OOO 타겟을 선점·탈환·고수해야 한다" 서술 템플릿을 따르도록 명시 (3) **§7-2-a 신설 — 타겟 성향/선호상황/결제 트리거 분석 방법**: `target_disposition`(근거: interest_keywords)·`preferred_situation`(근거: search_seasonality)·`purchase_trigger_context`(근거: news/market_issues) 3개 필드 신설, 소재분석의 appeal_tags 분포를 선택적 보조 근거로 추가. **ADetect에 설문·패널·구매 데이터가 없다는 한계를 명시하고, 이 3개 필드에는 항상 "실제 소비자 조사가 아닌 검색행동·시의성 기반 간접 추정" 고정 disclaimer를 동반**하도록 규정 (4) §5·§6-1(핵심 데이터 표)·§7-11-(2)(테이블 1개→2개, `05b_AI_Persona` 신설)·§14(시트 목록에 `05b_AI_Persona` 추가)·§16-2(잠금 아이콘을 종합분석 전용에서 게이트형 탭 공통으로 일반화, "①-a 잠금" 상태 화면 신설, 초기 탭 다이어그램에서 타겟분석도 🔒로 시작하도록 수정)·§16-3(타겟분석 세부탭 설명 갱신)·§16-7(User Flow에 타겟분석 게이트 해제 단계 반영) 전부 동기화 |
| 2026-09-11 (14차) | 별도 사본(`PRD_UD.md`)에서 진행된 캐시/비용최적화 작업을 검증 후 본 문서에 병합, 사본은 삭제. **검증 결과**: 3단계 캐시(Raw/Creative/AI) 구조·소스별 TTL·캐시 키 설계·범위 지정 강제새로고침·캐시 히트/미스 기록·비용 절감 우선순위·부분실패 재실행·캐시-History-Export 분리 원칙 모두 기존 §6-1/§7-4/§11/§21-3 원칙과 충돌 없이 타당하다고 판단해 그대로 반영. **(1) §12 전면 확장** — 기존 4줄짜리 캐시 정책을 §12-1~§12-10(3단계 캐시, TTL표, 캐시키 설계, 범위지정 새로고침, 히트/미스 기록, 절감 우선순위, 부분실패 재실행, 캐시/History/Export 분리, 비용흐름 다이어그램)으로 확장 (2) §10 비용 JSON 스키마에 `cache_summary`/`cache_saving_estimate` 통합 — "최초 실행은 기존 $0.45 추정치에 근접, 캐시 적중 시 하락, 9~13차 신규 필드는 아직 이 추정치에 미반영"이라는 3가지 조건을 명시 (3) §16-2 완료 탭 화면에 `[이 데이터만 새로 수집]`/`[이 기능 새로고침]` 버튼 추가(§12-5와 연결, `[전체 새로고침]`은 세션 설정 메뉴로 분리) (4) **★ 설계 판단 — 사본에 있던 신규 "§13 비용 최적화 데이터 구조 요약"을 별도 최상위 섹션으로 만들지 않고 §12-10 하위 섹션으로 접어 넣음** — 최상위 섹션을 하나 끼워 넣으면 §13(데이터수집일)부터 §24(개정이력)까지 전부 번호를 밀어야 하는데, 사본(`PRD_UD.md`)에서 바로 이 작업을 하다가 11개 섹션 헤더가 전부 "25"로 깨지는 등 실제로 재넘버링 사고가 났던 것을 확인했음 — 동일한 리스크를 피하기 위해 기존 §13~§24 번호를 전혀 건드리지 않는 이 방식을 선택 (5) `PRD_UD.md` 삭제 — 그 파일에서 발견된 회귀(11~13차 미반영, Excel 시트 재충돌, 헤더 번호 붕괴, 멀티유저 동시실행 정책 소실, 개정이력 "10차" 중복 등)는 모두 본 문서(정본)에는 존재하지 않는 문제였으므로 캐시 정책만 이관하고 사본 자체는 폐기 |
| 2026-09-12 (15차) | **두 가지 축의 전면 개정: (A) 브랜드 범용화, (B) 타겟 후추천 구조 확정.** 상세 내용은 §25 참고. **(A) 브랜드 범용화**: (1) §1·§7-3-a **신규** — `brand_context`(brand_type/business_model/offering_type/analysis_scope/conversion_objective/confidence) 내부 판단 필드 도입. 사용자에게 브랜드 유형을 선택하게 하는 화면·입력은 두지 않고, AI가 이미 수집한 데이터로 내부적으로만 판단 (2) `analysis_scope` MVP 고정값을 `brand`로 명시 — 나이키처럼 멀티카테고리 브랜드도 브랜드 전체 기준으로만 분석, `brand→category/product/service` 계층은 데이터 모델에만 여지를 둠 (3) §7-2-a `purchase_trigger_context` → `conversion_trigger_context`로 명칭 범용화("결제 트리거"→"전환 트리거"), §7-3 `promotion_fact` 설명에 구매 외 가입·상담 혜택도 포함됨을 명시 (4) §7-10에 경쟁사 내부 분류(direct_competitor/adjacent_competitor/alternative_channel, 비노출) 및 "사용자 입력 경쟁사 우선" 원칙 추가 (5) §5 선택기능에 "브랜드 유형별 프리셋·화면·프롬프트 체계는 만들지 않는다"를 명문화. **(B) 타겟 후추천 구조 확정**: (6) **STEP 1에서 타겟 입력 필드 완전 제거** — §5·§16-1 입력 폼, §9 자동추천 플로우·확인 화면 목업에서 타겟 관련 문구·블록 전부 삭제 (7) §7-2 **전면 개정** — 기존 `user_target`(STEP 1 사용자 입력) 폐기, `core_target`을 `recommended_target`(AI 추천)으로 개명하고 `confirmed_target`(사용자 확정값)을 신설, `target_status`(not_set/recommended/confirmed) 상태모델 도입 (8) **"타겟 추천"(게이트 통과 시 자동, 무료)과 "타겟분석"(확정 후 실행, 유료·`function_run`)을 명확히 두 단계로 분리** — §6-1·§6 기능별 상태 판정·§7-11(2)·§16-2·§16-7에 전부 반영, §9-2(신규) 타겟 추천 플로우 섹션 추가 (9) §16-4 상태 스트립의 예시를 세션 생성 직후에도 타겟이 존재하는 것처럼 보이던 표기("30~50대")에서 "타겟 미확정"으로 수정 (10) §24 뒤에 §25(신규) — 변경 요약·기존 충돌 정리·올리브영/메리츠화재/나이키 3개 브랜드 시뮬레이션·MVP 제외 항목을 별도 섹션으로 정리 |

---

## 25. 15차 개정 검증 노트 — 범용화 및 타겟 후추천 구조 최종 점검

> ★ 이 섹션은 15차 개정에서 실제로 무엇을 바꿨고, 왜 바꿨고, 3개 브랜드(올리브영/메리츠화재/나이키)에 실제로 넣었을 때 끝까지 자연스럽게 도는지를 별도로 검증한 기록입니다. 위 §1~§24 본문이 "무엇을 하라"는 규정이라면, 이 섹션은 "그 규정이 실제로 충돌 없이 작동하는가"에 대한 답입니다.

### 25-1. 변경 요약 (핵심 8개)

1. **STEP 1에서 타겟 입력 필드를 완전히 제거**했습니다. 입력은 브랜드명(필수)·카테고리(선택)·경쟁사(선택)만 남습니다.
2. **타겟은 시장분석+브랜드분석이 끝난 뒤에만 추천**됩니다(게이트 조건은 13차 그대로, §6-1).
3. **"타겟 추천"과 "타겟분석"을 서로 다른 단계로 분리**했습니다 — 추천(`recommended_target`, 게이트 통과 시 자동·무료)과 분석(확정 후 실행하는 `function_run`, §7-2·§9-2)을 같은 것으로 섞어 쓰지 않습니다.
4. **`user_target`/`core_target`을 `target_status`/`recommended_target`/`confirmed_target` 3분 모델로 교체**했습니다 — "사용자가 최초 입력한 타겟"이라는 의미를 완전히 제거했습니다.
5. **`brand_context`(내부 판단 필드) 신설**로 브랜드 유형(제품/서비스/리테일/플랫폼/금융 등)을 AI가 내부적으로만 판단하게 했습니다 — STEP 1이나 다른 어떤 화면에도 브랜드 유형 선택 UI는 없습니다.
6. **"구매" 중심 표현을 범용 용어로 교체**했습니다 — `purchase_trigger_context` → `conversion_trigger_context`, `promotion_fact` 설명에 가입·상담 혜택 포함, `brand_context.conversion_objective`가 브랜드별 실제 전환 행동을 표현.
7. **경쟁사 추천 우선순위 규칙 신설** — 사용자가 STEP 1에서 직접 입력한 경쟁사는 AI 추천보다 우선 반영되고, AI는 부족한 슬롯만 보완합니다. 내부적으로만 direct/adjacent/alternative-channel 분류를 사용할 수 있게 했습니다.
8. **`analysis_scope` 필드를 신설**하고 MVP 기본값을 `brand`로 고정 — 나이키 같은 멀티카테고리 브랜드도 브랜드 전체 단위로만 분석하며, 카테고리/제품 단위 세분 분석 UI는 만들지 않았습니다.

### 25-2. 기존 PRD에서 발견한 충돌과 수정 내용

| 충돌 지점 | 기존 PRD의 문제 | 이번에 수정한 내용 |
|---|---|---|
| **타겟 최초 입력** | §5·§16-1에 "타겟(선택)"이 STEP 1 입력 항목으로 남아 있었고, §7-2 `user_target`이 "STEP 1에서 사용자가 입력·확정한 타겟"으로 정의되어 있었음 — 13차의 "타겟분석 게이트형" 원칙과 정면 충돌(입력은 STEP 1에서 받는데 분석은 나중에 게이트가 열려야 가능하다는 게 앞뒤가 안 맞음) | STEP 1 입력 항목에서 타겟을 완전히 삭제(§5·§16-1). `user_target` 필드 자체를 폐기(§7-2) |
| **타겟 자동추천 시점** | §9 플로우에 "AI가 카테고리·**타겟**·경쟁사를 추천"이 STEP 1 확인 화면에 그대로 남아 있어, "타겟은 게이트 통과 후에만 추천된다"는 §7-2 13차 개정 문장과 충돌 | §9 플로우·확인 화면 목업에서 타겟 추천 관련 문구·입력 블록 전부 제거, §9-2(신규)에 게이트 통과 후의 타겟 추천 플로우를 별도 명시 |
| **경쟁사 추천 시점** | 경쟁사는 STEP 1에서 입력 가능하면서 동시에 §9에서 AI가 추천하는데, 사용자가 입력한 값과 AI 추천값의 우선순위가 명시돼 있지 않았음 | §7-10에 "사용자 입력 경쟁사 우선" 원칙을 명문화 — AI는 빈 슬롯만 보완 |
| **브랜드 유형** | 기존 PRD에는 브랜드 유형 선택 UI가 없었지만, §7-3·§7-3-c의 일부 설명("대표 상세페이지", "카테고리 depth")이 암묵적으로 상품 판매형 브랜드를 전제하고 있어 메리츠화재 같은 서비스 브랜드에 그대로 적용하면 어색했음 | §7-3-a `brand_context` 신설 + §7-3-c에 "이 체크리스트는 커머스/리테일형 브랜드 예시이며 서비스 브랜드는 해당없음이 정상"이라는 캐벗 추가 |
| **커머스/금융 서비스 용어** | §7-2-a `purchase_trigger_context`("결제 트리거"), §7-2 예시 문구 등이 "구매"를 유일한 전환 행위로 전제 | `conversion_trigger_context`로 개명, `brand_context.conversion_objective`가 브랜드별 실제 전환(구매/상담/가입/계약/재방문)을 표현하도록 스키마 추가 |

### 25-3. 3개 브랜드 시뮬레이션

공통 전제: 세 브랜드 모두 **동일한 STEP 1 폼**(브랜드명 필수, 카테고리 선택, 경쟁사 선택)과 **동일한 5개 탭 구조**(시장/타겟/브랜드/소재/종합)를 그대로 통과합니다. 아래는 각 브랜드에서 그 공통 구조가 내부적으로 어떻게 채워지는지를 보여줍니다.

#### 올리브영
1. **입력**: 브랜드명="올리브영"만 입력, 카테고리·경쟁사 미입력.
2. **분석(시장+브랜드 기본 데이터 수집)**: AI가 카테고리를 "H&B(헬스&뷰티) 스토어/유통" 계열로 추천. `brand_context` 내부 판단 — `brand_type`="멀티브랜드 리테일 플랫폼", `offering_type`="복합(다품목 물리적 상품)", `conversion_objective`="상품 탐색/구매/재방문".
3. **경쟁사 추천**: AI가 직접 경쟁(세포라·시코르), 대체 채널(쿠팡), 인접 경쟁(무신사)을 내부적으로 구분해 후보 산출 → 사용자에게는 구분 없이 체크리스트로만 노출(§9). 대표 상세페이지가 없는 리테일러 특성상 §7-3 `brand_website_facts`는 홈페이지/브랜드스토리로 폴백.
4. **브랜드분석**: `key_message`/`usp`가 단일 값이 아니라 "무료배송 프로모션"/"신제품 입점 알림"/"시즌 세일" 등 복수 패턴 배열로 산출(§7-3, 기존 규정 그대로 유지).
5. **타겟 추천**: 시장+브랜드 데이터(검색 세그먼트, 경쟁사 대비 검색 관심도)를 근거로 예컨대 "20~30대 여성"을 추천, 근거 문구는 `conversion_objective`="구매/재방문" 톤으로 서술.
6. **타겟분석**: 사용자 확정 후 세그먼트 검색량·관심 키워드(예: "올영세일", "쿠폰") 분석.
7. **종합분석**: SOV·포지셔닝맵·White Space 산출 — 별도 입력값이나 별도 화면 없이 공통 스키마로 완결. **별도 입력값·별도 UI 불필요 — brand_context와 공통 필드만으로 해결됨.**

#### 메리츠화재
1. **입력**: 브랜드명="메리츠화재"만 입력.
2. **분석**: AI가 카테고리="손해보험"으로 추천. `brand_context` — `brand_type`="손해보험 서비스 브랜드", `offering_type`="금융상품", `conversion_objective`="상담/견적/가입/계약".
3. **경쟁사 추천**: 직접 경쟁(삼성화재·현대해상·DB손해보험·KB손해보험)을 추천(§9 기존 목업 그대로 유지 가능 — 이번 개정에서는 타겟 블록만 제거).
4. **브랜드분석**: `promotion_fact`가 "첫 구매 20% 할인" 대신 "다이렉트 가입 시 보험료 5% 할인" 같은 문구로 채워짐 — 스키마·화면은 동일, 내용만 다름.
5. **타겟 추천**: "40~50대 남성" 등 추천, 근거 서술이 `conversion_objective`="상담/가입" 톤(예: "경쟁사는 30~40대 남성에 신뢰 소구로 집중 중이나, 40~50대 남성 구간은 상담 전환 여지가 상대적으로 열려 있음").
6. **타겟분석**: `conversion_trigger_context`(구 결제 트리거)가 "보험료 인상 뉴스", "실손보험 개정" 같은 제도 변경 뉴스를 근거로 산출 — 필드명이 일반화됐을 뿐 로직은 기존과 동일.
7. **종합분석**: `brand_website_facts`의 §7-3-c 체크리스트(리뷰 수·재구매 UI 등)는 "해당없음"으로 정상 처리되고, 나머지(SOV·포지셔닝·White Space)는 동일하게 산출. **별도 입력값·별도 UI 불필요 — "해당없음" 처리만으로 스키마 재사용 가능.**

#### 나이키
1. **입력**: 브랜드명="나이키"만 입력.
2. **분석**: AI가 카테고리="스포츠웨어/신발" 계열로 추천. `brand_context` — `brand_type`="멀티카테고리 제품 브랜드", `analysis_scope`="brand"(러닝/농구/키즈 등 하위 카테고리로 쪼개지 않고 브랜드 전체로 처리), `conversion_objective`="구매".
3. **경쟁사 추천**: 아디다스·뉴발란스·언더아머 등 직접 경쟁 브랜드를 추천 — 카테고리가 다양해도 경쟁사 추천 로직은 브랜드 전체 기준으로 동일하게 동작.
4. **브랜드분석**: `key_message`/`usp`가 카테고리별(러닝/농구/라이프스타일)로 나뉘지 않고 브랜드 전체 소재를 합산한 상위 패턴으로 산출 — §7-3 "복수 패턴 배열 반환" 규정이 여기서도 그대로 재사용됨(카테고리 리테일러 전용이 아니라 범용 규칙이었음이 확인됨).
5. **타겟 추천**: 검색 세그먼트·경쟁 밀도를 근거로 예컨대 "10~20대 남성"을 추천, `conversion_objective`="구매" 톤으로 서술.
6. **타겟분석**: 관심 키워드가 "나이키 에어맥스", "나이키 신상" 등 제품명이 섞여 나올 수 있으나, `analysis_scope`가 `brand`이므로 이를 제품별로 쪼개 별도 분석하지 않고 브랜드 타겟 분석 결과에 그대로 포함.
7. **종합분석**: 동일 스키마로 SOV·포지셔닝맵 산출. **별도 입력값·별도 UI 불필요 — 카테고리가 많다는 사실은 `brand_context`의 서술과 관심 키워드 다양성으로만 드러나고, 화면 구조는 동일.**

**공통 결론**: 세 브랜드 모두 STEP 1 폼, 경쟁사 추천 로직, §7 스키마, §16 탭 구조를 그대로 통과했습니다. 브랜드마다 달라지는 것은 오직 `brand_context`가 채우는 **내용**(brand_type/conversion_objective 등)과 그로부터 파생되는 **서술 톤**뿐이며, 화면·입력 항목·Excel 시트 구조는 세 브랜드가 완전히 동일합니다.

### 25-4. MVP에서 추가하지 않은 것

범용성을 위해 검토했지만, 이번 개정에서 의도적으로 MVP에 넣지 않은 것들입니다.

* **브랜드 유형 선택 UI** — `brand_context`는 AI 내부 판단으로만 존재하며, STEP 1이나 다른 화면에서 사용자가 브랜드 유형(Product Brand/Service Brand/Retailer 등)을 고르게 하지 않습니다.
* **브랜드 유형별 별도 화면·별도 Excel·별도 프롬프트 체계** — §5 선택기능에 명시적으로 보류. 카테고리별 프리셋과 동일한 이유로 제외.
* **`brand → category/product/service` 계층형 분석 UI** — `analysis_scope` 필드는 신설했지만 MVP 고정값은 항상 `brand`이며, 나이키의 카테고리별 세분 분석 화면은 만들지 않습니다.
* **경쟁사 유형(직접/인접/대체채널) 사용자 입력 UI** — 내부 판단 필드로만 존재하고, 사용자가 이 3개 값을 직접 고르는 화면은 없습니다.
* **타겟 추천을 위한 새 상위 탭 신설** — "타겟 추천"과 "타겟분석"을 개념적으로는 분리했지만, 화면상 새로운 6번째 탭을 만들지 않고 기존 "타겟분석" 탭 안의 상태 전이(🔒 → 추천 표시 → 확정 → 분석 실행)로 흡수했습니다.
* **제품/리뷰 단위 자동 분석** — 기존 PRD에서 이미 제외됐던 범위(§7-3-c)를 유지, 이번 개정에서 다시 추가하지 않았습니다.
* **복잡한 taxonomy 관리 시스템** — `brand_type`은 고정 선택지 목록이 아니라 AI 자유서술+태그로 두어, 별도의 유형 마스터 데이터 관리 체계를 만들지 않았습니다.

**최종 방향**: 범용성은 `brand_context`라는 단일 내부 필드와 필드 명칭 일반화(conversion_trigger_context 등)로만 확보하고, 사용자가 입력해야 하는 항목은 STEP 1 기준 4개(브랜드명·카테고리·경쟁사·[없음, 타겟 삭제])에서 3개로 오히려 줄였습니다.
